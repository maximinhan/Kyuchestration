# 저장소 루트가 모든 텍스트를 LF 로 고정하지만, Windows 배치 파일만은 예외다.
# cmd.exe 는 LF 만 있는 .bat 에서 label·goto 를 어긋나게 읽는 경우가 있어 gradlew.bat 이
# 조용히 실패한다. wrapper 는 "새 머신에서 그냥 되는 것" 이 존재 이유이므로 여기서 막는다.
*.bat text eol=crlf

# wrapper jar 는 이진 파일이다. 줄바꿈 변환이 닿으면 실행 자체가 깨진다.
gradle/wrapper/gradle-wrapper.jar binary
