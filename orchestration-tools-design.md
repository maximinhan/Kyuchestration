# 메인이 레포 세션을 부린다 — 오케스트레이션 도구 설계

> 사용자가 메인과 대화하기만 하면, 필요한 레포에서 세션이 저절로 떠서 **그 레포의 설정을 전부 로드한 채로** 고친다. 엔진이 MCP 서버가 되어 메인 세션에 위임 도구를 쥐여주는 것으로 그것을 한다.

- 작성일: 2026-09-01
- 상태: **설계 확정 전 — 실측 완료, 사용자 승인 대기.** 구현 미착수
- 선행 문서: [workdir-orchestrator-design.md](workdir-orchestrator-design.md) — 특히 1.3(근본 제약), 3.1(서브에이전트 대안), 원칙 2·3·6, **11절 열린 질문 1**
- 선행 문서: [app-owned-sessions-design.md](app-owned-sessions-design.md) — 특히 5.2(`kyu session-command`), 5.4(두 곳에서 오는 사실), 5.5(대화 이어가기), 원칙 11
- 전제: **CLI 세션 기능이 은퇴한 뒤의 엔진.** `kyu` 는 순수 엔진이고 세션 백엔드를 갖지 않는다. 그 전환은 PR #49 로 머지됐고, 이 설계는 그 위에 선다
- 이 문서가 닫는 것: **선행 문서 11절 1번** — *"헤드리스 세션 실행 … `.mcp.json` 승인이 어떻게 처리되는지, 파일 수정 권한을 어떻게 부여하는지 확인되지 않았다"*. 2026-09-01 에 쟀고 3절이 그 답이다

---

## 1. 배경 — 무엇이 아직 불편한가

### 1.1 사용자가 말한 것

> "수동으로 카드를 눌러 레포 세션을 띄우는 게 아니라, 그냥 메인과 대화하면 필요에 따라 알아서 클론 레포에 세션이 뜨면서 **해당 레포의 MCP 설정 등을 다 활용한 채로** 수정이 되면 좋겠어."

이 문장에 요구가 셋 들어 있다.

| 요구 | 지금 |
|---|---|
| **사람이 카드를 고르지 않는다** — 무엇을 어느 레포에서 할지는 대화에서 정해진다 | 사람이 카드를 누른다 |
| **세션이 저절로 뜬다** | 사람이 띄운다 |
| **레포의 설정이 전부 살아 있다** | 카드를 눌러 뜬 세션에서는 이미 성립한다 |

**세 번째는 이미 되고 있다.** 앱이 레포 cwd 에서 `claude` 를 직접 띄우므로(app-owned 5.3) 그 세션은 레포의 설정을 그대로 받는다. 남은 것은 앞의 둘이고, 그 둘의 공통점은 **사람이 손으로 하는 일**이라는 것이다.

### 1.2 근본 제약은 그대로다

선행 문서 1.3 이 이 도구의 존재 이유로 적어둔 표는 이번에도 그대로 성립한다.

> **설정은 오직 세션을 시작한 디렉토리에서만 온다.**

그래서 "메인이 레포를 고친다" 는 길은 없다. 메인의 cwd 는 워크디렉토리이고, `--add-dir` 은 파일 접근만 준다. 메인이 `proj-a/src/Foo.java` 를 직접 고칠 수는 있지만 그때 `proj-a` 의 MCP 서버도 지침도 훅도 붙지 않는다 — **사용자 요구의 핵심인 "MCP 설정 등을 다 활용한 채로" 가 정확히 성립하지 않는 자리다.**

그러므로 답은 하나뿐이다. **레포 cwd 에서 도는 진짜 프로세스가 그 일을 해야 한다.** 이 설계가 정하는 것은 "누가 그것을 띄우는가" 이고, 답은 "사람 대신 메인 세션의 Claude 가" 다.

### 1.3 선행 문서 3.1 이 남겨둔 자리

선행 문서 3.1 이 "워크디렉토리 단일 세션 + 서브에이전트" 를 검토하고 세 가지 이유로 기각했다. 그중 둘이 이 설계에 그대로 이어진다.

| 3.1 의 기각 사유 | 이 설계에서 |
|---|---|
| (1) 레포가 늘 때마다 에이전트 정의 파일을 만들어야 한다 | **사라진다.** 위임은 도구 호출 한 번이고 정의 파일이 없다. 레포는 스캔으로 발견된다(선행 문서 5.3) |
| (2) **레포의 나머지 설정은 여전히 안 따라온다** — MCP 를 inline 으로 해결해도 `CLAUDE.md`·커스텀 에이전트·훅은 로드되지 않는다 | **넘는다.** 서브에이전트가 아니라 **레포 cwd 의 진짜 프로세스**라서 넷 전부가 로드된다. 3.1 이 이 설계의 근거다 |
| (3) 위임마다 MCP 서버가 부팅된다 | **남는다.** 다만 크기를 이제 안다 — 3.6 |

**(2)가 이 설계의 뼈대다.** 3.1 은 "MCP 는 inline 으로 되지만 나머지 셋은 안 된다" 에서 멈췄다. 레포 cwd 에서 프로세스를 띄우면 넷이 한꺼번에 해결된다 — 그것이 애초에 이 도구가 각 레포에서 세션을 띄우는 이유였고(선행 문서 1.3), 위임은 그 세션을 사람 대신 띄우는 일일 뿐이다.

**그런데 그 넷이 헤드리스에서도 정말 사는지는 아무도 재본 적이 없다.** 선행 문서 11절 1번이 그것을 열린 질문으로 남겼고, v1 범위에서 "세션에 작업 자동 주입" 을 제외한 사유가 바로 그것이었다(선행 문서 7절). 이 문서는 먼저 그것을 잰다.

---

## 2. 핵심 문제 — 위임이 진짜 세션과 같아야 한다

선행 문서 2절이 *"프로세스는 격리, 지식은 공유"* 였다. 이번 문서의 같은 자리는 이렇다.

**위임된 실행이 사람이 손으로 띄운 세션과 같지 않으면, 이 기능은 편의가 아니라 함정이다.** 다르다면 사용자는 "카드를 눌러 하면 되는데 위임하면 안 되는 일" 을 하나씩 발견하게 되고, 그때마다 왜 다른지를 알아내야 한다.

같아야 하는 것이 넷이다 — 선행 문서 1.3 의 표 그대로다.

| 항목 | 위치 |
|---|---|
| MCP 서버 설정 | `<repo>/.mcp.json` |
| 프로젝트 지침 | `<repo>/CLAUDE.md` |
| 커스텀 에이전트 | `<repo>/.claude/agents/` |
| 프로젝트 설정·훅 | `<repo>/.claude/settings.json` |

그리고 다를 수밖에 없는 것이 둘이다.

| 다른 것 | 왜 |
|---|---|
| **사람이 없다** | 승인 프롬프트에 답할 사람이 없다. 권한을 미리 정해야 한다 |
| **화면이 없다** | 진행을 볼 수 없다. 결과를 문서로 받아야 한다 |

3절이 앞의 넷을 재고, 뒤의 둘도 함께 잰다.

---

## 3. 실측 — 선행 문서 11절 1번을 닫는다

**2026-09-01, `claude` 2.1.252, WSL2(Linux).** 격리된 스크래치 디렉토리에 가짜 레포를 만들어 쟀다. 재료는 전부 가짜다 — 지문 문구를 담은 `CLAUDE.md`, 부팅 사실을 로그에 남기고 지문 하나를 답하는 초소형 stdio MCP 서버, `SessionStart` 훅 하나, 측정용 서브에이전트 하나. 실제 레포도 실제 토큰도 쓰지 않았다. 재현 방법은 부록 B 다.

**판정 방식**: 모델의 말을 믿지 않는다. MCP 는 **서버 프로세스가 남긴 부팅 로그**로, 훅은 **훅이 쓴 파일**로, 에이전트는 **종료 코드**로 갈랐다. 모델이 지어낼 수 없는 것만 근거로 쓴다.

### 3.1 레포 설정 넷이 헤드리스에서 사는가 — 전부 산다

레포 cwd 에서 `claude -p` 를 실행했을 때다.

| 항목 | 결과 | 무엇으로 갈랐나 |
|---|---|---|
| `CLAUDE.md` | ✅ 로드된다 | 지문 `KYUFP-7431` 을 답했다 |
| `.mcp.json` | ✅ 붙는다 | 프로브 서버가 **부팅되고** `initialize`·`tools/list` 를 받았다 |
| `.claude/settings.json` (훅) | ✅ 로드된다 | `SessionStart` 훅이 로그 파일에 한 줄 적었다 |
| `.claude/agents/` | ✅ 발견된다 | 레포 cwd 에서 `--agent probe-agent` 종료 코드 0. **같은 명령이 레포 밖에서는 종료 코드 1** 과 `--agent 'probe-agent' not found` |

**선행 문서 1.3 의 표가 헤드리스에서 통째로 뒤집힌다.** 그 표는 "워크디렉토리 세션에서" 넷이 전부 ❌ 라는 것이었고, 여기서는 레포 cwd 라서 넷이 전부 ✅ 다. 같은 제약의 앞뒷면이다 — 설정은 세션을 시작한 디렉토리에서만 오고, 위임은 그 디렉토리에서 시작한다.

**에이전트 항목은 갈라 적어둔다.** 종료 코드는 발견됐다고 말했지만, 그 에이전트가 답한 내용은 `AGENTFP-5150` 이 아니라 `KYUFP-7431` 이었다 — 에이전트 지문과 `CLAUDE.md` 지문이 서로 다른 답을 지시하도록 측정 재료를 잘못 만들었고, 모델이 `CLAUDE.md` 를 따랐다. **발견 여부는 종료 코드로 확정됐고, 우선순위는 재지 못했다.** 재료의 결함이지 발견의 결함이 아니다.

### 3.2 `.mcp.json` 승인 — 헤드리스는 관문을 타지 않는다

**이것이 선행 문서 11절 1번이 물은 바로 그 질문이고, 답이 예상과 다르다.**

승인 이력이 전혀 없는 새 프로젝트에서 쟀다(`~/.claude.json` 에 이 경로가 없는 것을 먼저 확인했다).

| 잰 것 | 결과 |
|---|---|
| `claude mcp list` | `kyu-probe: … - ⏸ Pending approval (run \`claude\` to approve)` — **연결하지 않고, 서버가 부팅되지도 않는다** |
| 같은 상태에서 `claude -p` | **붙는다.** 프로브 서버가 부팅되고 `tools/list` 를 받는다 |
| `-p` 를 돌린 **뒤** 다시 `claude mcp list` | 여전히 `⏸ Pending approval` — **`-p` 실행은 승인을 저장하지 않는다** |

**즉 승인 관문은 대화형에만 있다.** `claude --help` 가 `-p` 항목에 적어둔 것과 같은 계열이다 — *"The workspace trust dialog is skipped when Claude is run in non-interactive mode."*

**설계에 미치는 영향 (좋은 쪽)**: 위임은 아무 준비 없이 레포의 `.mcp.json` 을 받는다. 엔진이 승인을 미리 심을 필요가 없고, 승인 상태를 관리할 필요도 없다. **설계에서 "승인 처리" 절이 통째로 필요 없어졌다.**

**설계에 미치는 영향 (나쁜 쪽)**: 선행 문서 1.3 이 그 관문에 대해 적어둔 문장이 여기서 되돌아온다.

> 이 제약은 버그가 아니라 보안 설계다. `.mcp.json` 은 임의 명령을 실행하는 파일이므로(`command`, `args` 필드), 추가 디렉토리의 것을 자동 로드하면 레포를 클론하는 것만으로 코드가 실행된다.

**위임은 그 관문이 없는 길이다.** `kyu clone` 으로 레포를 받고 메인이 그 레포에 위임하는 순간, 그 레포의 `.mcp.json` 에 적힌 명령이 사람에게 한 번도 보이지 않은 채 돈다. 5.6 이 이것을 다룬다.

### 3.3 권한 — 무엇을 켜야 위임이 일을 하는가

같은 프롬프트(MCP 도구 하나 호출 + 파일 하나 수정)를 플래그만 바꿔 돌렸다.

| 플래그 | MCP 도구 호출 | 파일 수정 |
|---|---|---|
| (없음 — 기본) | ❌ | ❌ |
| `--permission-mode acceptEdits` | ❌ | ✅ |
| `--permission-mode dontAsk` | ❌ | ❌ |
| `--permission-mode auto` | ✅ | ✅ |
| `--allowedTools "mcp__kyu-probe__repo_fingerprint Edit Write"` | ✅ | ✅ |
| `--dangerously-skip-permissions` | ✅ | ✅ |

**`acceptEdits` 가 답이 아니라는 것이 이 표의 요점이다.** 이름만 보면 "편집을 허용" 이라 위임에 알맞아 보이는데, **MCP 도구 호출을 막는다.** 그것을 고르면 사용자 요구의 *"해당 레포의 MCP 설정 등을 다 활용한 채로"* 가 성립하지 않는다 — 서버는 붙어 있는데 부를 수가 없다.

**막힌 실행이 실패로 끝나지 않는다.** 이것이 이 절에서 가장 중요한 사실이다.

```
exit=0
subtype = 'success'
is_error = False
permission_denials = [{"tool_name": "mcp__kyu-probe__repo_fingerprint", …},
                      {"tool_name": "Edit", …}]
result = 'repo_fingerprint 도구와 target.txt 파일 편집에 권한이 필요합니다. 사용자님의 승인을 기다리고 있습니다.'
```

**종료 코드도 0 이고 `subtype` 도 `success` 다.** 아무것도 하지 못한 위임이 성공한 위임과 종료 코드로 구별되지 않는다. `permission_denials` 배열을 읽는 것 말고는 아는 길이 없다 — 5.4 가 이것을 계약으로 고정한다.

**`--allowedTools` 로 이름을 나열하는 길도 된다.** 다만 그러려면 엔진이 그 레포의 MCP 서버가 무슨 도구를 갖고 있는지를 미리 알아야 하고, 그것은 서버를 띄워봐야 아는 것이다. 위임할 때마다 서버를 두 번 띄우게 된다. **쓰지 않는다.**

### 3.4 `--output-format json` — 엔진이 읽을 것이 다 있다

최상위 키가 23 개다. 엔진이 쓸 것만 적는다.

| 키 | 쓰임 |
|---|---|
| `result` | 위임의 답 본문 |
| `is_error` · `subtype` | 성공/실패. **권한 거절은 여기 안 잡힌다**(3.3) |
| `permission_denials` | 거절된 도구 호출 목록. **반드시 읽어야 한다** |
| `session_id` | 이 위임의 대화 ID |
| `total_cost_usd` | 위임 한 번의 비용 |
| `duration_ms` · `duration_api_ms` | 걸린 시간 |
| `num_turns` | 위임이 몇 턴을 돌았나 |
| `usage` · `modelUsage` | 토큰 |

전문은 부록 A 다.

### 3.5 대화 이어가기 — `-p` 에서도 된다

| 잰 것 | 결과 |
|---|---|
| `-p` 에 `--session-id <uuid>` | **동작한다.** 답 문서의 `session_id` 가 정해준 값과 같다 |
| `-p` 에 `--resume <uuid>` | **동작한다.** 앞 회차에 기억시킨 숫자 `4721` 을 답했다 |
| 없는 uuid 로 `--resume` | 즉시 종료 코드 1, stderr `No conversation found with session ID: <uuid>` |
| **이미 쓴 uuid 로 `--session-id` 재사용** | **종료 코드 1, `Error: Session ID <uuid> is already in use.`** |
| 전사 위치 | `~/.claude/projects/<cwd 에서 유도한 이름>/<uuid>.jsonl` |

**마지막에서 둘째 줄이 덤으로 다른 문서의 열린 질문을 닫는다.** app-owned 8절 6번이 *"`--session-id` 를 이미 쓰인 ID 로 두 번 부르면 어떻게 되는지가 확인되지 않았다 — 거절인지, 이어가기인지, 전사가 섞이는지"* 였다. **거절이다.** 그리고 그 대화가 이미 끝난 뒤에도 거절이므로, 동시성 문제가 아니라 **한 번 쓴 ID 는 다시 `--session-id` 로 쓸 수 없다**는 뜻이다.

그래서 이어가기는 반드시 두 걸음으로 갈린다 — **첫 회는 `--session-id`, 이후는 `--resume`.** app-owned 5.5.3 의 흐름이 그대로 위임에도 맞는다.

### 3.6 고정 비용 — 선행 문서 3.1 의 (3) 을 숫자로 바꾼다

위임 한 번의 고정 비용이다. 같은 프롬프트를 3 회씩 돌려 **벽시계에서 `duration_ms`(API 시간) 를 뺐다.**

| 구성 | 고정 비용 (3 회 평균) |
|---|---|
| 사용자 전역 MCP 5 개 + 레포 `.mcp.json` | **3860 ms** |
| 레포 `.mcp.json` 만 (`--strict-mcp-config`) | **2039 ms** |
| MCP 없음 | **1794 ms** |

쪼개면 이렇다.

| 조각 | 크기 |
|---|---|
| 프로세스 기동 | ~1.8 초 |
| 초소형 stdio 서버 하나 | ~0.25 초 |
| 사용자 전역 MCP 집합(원격 3 + 플러그인 2) | ~1.8 초 |

**선행 문서 3.1 의 (3) — *"코드 분석 MCP 는 언어 서버를 띄우므로 부팅이 무겁다. 위임을 잘게 쪼개는 방식과 상성이 나쁘다"* — 은 여전히 맞다.** 다만 무거운 것은 도구가 더하는 층이 아니라 **레포가 이미 갖고 있는 서버**다. 초소형 서버는 0.25 초이고, 언어 서버를 띄우는 MCP 라면 그 자리가 몇 초가 된다. 그리고 그 비용은 사람이 카드를 눌러 세션을 띄울 때도 똑같이 낸다 — **위임이 새로 만드는 비용이 아니라, 위임을 잘게 쪼갤 때만 여러 번 내게 되는 비용이다.** 5.7 이 그것을 다룬다.

### 3.7 동시 위임 — 격리는 성립한다

지문이 서로 다른 가짜 레포 셋을 만들어 동시에 위임했다.

| 잰 것 | 결과 |
|---|---|
| 셋 다 성공했나 | ✅ 전부 종료 코드 0 |
| 각자 자기 레포의 `CLAUDE.md` 지문을 답했나 | ✅ `FP-A-777` · `FP-B-777` · `FP-C-777` |
| 각자의 MCP 프로브가 따로 부팅됐나 | ✅ 레포마다 `tools/call` 1 회 |
| **다른 레포의 지문이 섞였나** | ✅ **아니오** |
| 벽시계 총합 | 28.4 초 (가장 느린 하나의 API 시간이 13.5 초) |

**격리는 성립하고, 완전한 병렬은 아니다.** 셋을 동시에 띄웠는데 총합이 가장 느린 하나보다 길다. API 쪽에서 줄을 서는 것으로 보인다 — 잰 것은 "간섭하지 않는다" 까지이고, "N 배 빨라진다" 는 재지 않았다. **동시 위임은 정확성을 위해 쓰는 것이지 속도를 위해 쓰는 것이 아니다.**

### 3.8 `--mcp-config` 는 관문을 타지 않는다

이 측정은 5.2 의 등록 방식을 정하려고 뒤늦게 더한 것이다.

| 잰 것 | 결과 |
|---|---|
| `.mcp.json` 이 **전혀 없는** 빈 디렉토리에서 `-p --mcp-config <설정>` | 서버가 붙고 도구가 실제로 불린다. `permission_denials` 비어 있음 |
| `--mcp-config` 가 JSON **문자열**도 받나 | ✅ 받는다 (`--mcp-config '{"mcpServers":{}}'` 로 MCP 를 통째로 끈 측정이 3.6 의 셋째 줄이다) |
| 워크디렉토리 `.mcp.json` 을 설정으로 미리 허용할 수 있나 | ❌ **없다** — 아래 |

미리 허용하는 길을 `.claude/settings.local.json` 에 두고 `claude mcp list` 로 쟀다.

| 설정 | 결과 |
|---|---|
| `{"enableAllProjectMcpServers": true}` | 여전히 `⏸ Pending approval` |
| `{"enabledMcpjsonServers": ["kyu-probe"]}` | 여전히 `⏸ Pending approval` |
| `{"disabledMcpjsonServers": ["kyu-probe"]}` | **듣는다** — 목록에서 사라진다 |

**끄는 키는 듣고 켜는 키는 안 듣는다.** 잰 범위를 정확히 적어둔다 — `claude mcp list` 기준이고, **대화형 세션이 그 키를 어떻게 읽는지는 재지 못했다**(대화형 승인을 자동화로 재는 길이 없다). 그러므로 이 설계는 **미리 허용하는 길에 기대지 않는다.** 5.2 가 `--mcp-config` 를 고르는 근거가 이것이다.

---

## 4. 설계 원칙 — 상속과 추가 하나

선행 문서 4절의 여섯 원칙과 app-owned 4절의 원칙 11·12 를 그대로 상속한다. 특히 이 설계가 계속 부딪히는 셋을 먼저 적어둔다.

> 2. **레포 안에는 아무것도 넣지 않는다** — 오케스트레이션 MCP 서버의 등록 자리가 이것에 걸린다(5.2).
> 3. **세션 내부에 개입하지 않는다** — 위임은 프롬프트를 넣고 답을 받는 것이지 세션의 입출력을 가로채는 것이 아니다.
> 6. **상태는 관찰해서 얻는다** — 위임의 진행 상태를 파일에 선언하지 않는 근거다(6.2).

**추가**

13. **위임이 메인보다 넓은 권한을 갖지 않는다** — 위임은 메인의 손이지 별개의 주체가 아니다. 메인이 승인 없이 못 하는 일을 위임이 대신 해주는 길이 생기면, 사용자는 권한을 좁혀둔 채로 넓은 권한을 행사하게 된다. 그 사실을 사용자가 알아채는 자리는 이미 벌어진 일 앞이다.

---

## 5. 아키텍처

### 5.1 배치

```
┌──────────────────────────────────────────────────────────────┐
│ [앱]  카드 · 임베디드 터미널 (JediTerm)                        │
└──────────────────────────────────────────────────────────────┘
        │ kyu session-command --json          ▲ kyu list --json
        ▼                                     │
┌──────────────────────────────────────────────────────────────┐
│ [메인 세션]  claude  (cwd = 워크디렉토리)                      │
│    --add-dir proj-a proj-b …                                 │
│    --mcp-config '{"mcpServers":{"kyu":{…}}}'   ← 새로 붙는 것  │
│                                                              │
│    도구:  list_repos()      run_in_repo(repo, prompt)         │
└──────────────────────────────────────────────────────────────┘
                              │ stdio (MCP)
                              ▼
┌──────────────────────────────────────────────────────────────┐
│ [kyu mcp]  오케스트레이션 MCP 서버 — 엔진의 한 명령            │
│    스캔 · 조립 · 헤드리스 실행 · 기록                          │
└──────────────────────────────────────────────────────────────┘
                              │ 프로세스 실행
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
   claude -p             claude -p             claude -p
   cwd=proj-a            cwd=proj-b            cwd=proj-c
   proj-a 설정 전부       proj-b 설정 전부       proj-c 설정 전부
```

**조율 계층은 여전히 엔진에 한 벌이다.** 선행 문서 5.1 이 *"이 배치의 목적은 조율 계층을 두 번 쓰지 않는 것이다"* 라고 적은 것이 그대로다. 새로 생기는 것은 표시 계층이 하나 더 늘어난 것이다 — **메인 세션의 Claude 가 세 번째 표시 계층이 된다.** CLI 가 사람에게 표를 보이고, 앱이 사람에게 카드를 보이고, MCP 서버가 모델에게 도구를 보인다. 셋 다 같은 조율 계층을 부른다.

**재귀가 구조적으로 막힌다.** 위임된 실행의 cwd 는 레포이고, 엔진은 그 실행에 `--mcp-config` 를 주지 않는다. 그리고 레포의 `.mcp.json` 에는 `kyu` 가 없다(원칙 2 — 레포 안에 아무것도 안 넣는다). **위임된 세션은 `run_in_repo` 를 볼 수 없다.** 깊이 제한이라는 숫자를 둘 필요가 없다.

### 5.2 등록 — `--mcp-config` 에 JSON 문자열로

**문제**: 메인 세션이 `kyu` MCP 서버를 어떻게 보게 하는가.

**골격의 안은 `kyu init` 이 워크디렉토리 `.mcp.json` 에 등록하는 것이었다. 쓰지 않는다.**

| 기각 사유 | 근거 |
|---|---|
| **메인 세션이 승인 프롬프트를 탄다** | 메인 세션은 대화형이고, 대화형에는 관문이 있다(3.2). 워크디렉토리를 만들 때마다 사용자가 승인 대화를 한 번 지나야 한다 |
| **미리 허용하는 길이 없다** | `enableAllProjectMcpServers` · `enabledMcpjsonServers` 가 듣지 않았다(3.8). 듣는지 확인되지 않은 설정에 기대어 설계할 수 없다 |
| **워크디렉토리에 파일이 하나 더 생긴다** | 그 파일에 `kyu` 바이너리 절대경로가 들어가고, 바이너리를 옮기거나 올리면 조용히 낡는다 |

**결정: `kyu session-command` 의 답에 `--mcp-config` 와 JSON 문자열을 함께 싣는다.**

```json
{
  "schemaVersion": 1,
  "command": ["claude",
              "--session-id", "d1b9d634-…",
              "--add-dir", "/home/me/work/WorkDir-featureX/proj-a",
              "--add-dir", "/home/me/work/WorkDir-featureX/proj-b",
              "--mcp-config",
              "{\"mcpServers\":{\"kyu\":{\"type\":\"stdio\",\"command\":\"/usr/local/bin/kyu\",\"args\":[\"mcp\",\"/home/me/work/WorkDir-featureX\"]}}}"],
  "cwd": "/home/me/work/WorkDir-featureX",
  "env": { "CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD": "1" }
}
```

**이 안이 좋은 이유**

| 얻는 것 | 왜 |
|---|---|
| **승인 프롬프트가 없다** | `--mcp-config` 는 관문을 타지 않는다(3.8) |
| **파일이 늘지 않는다** | 문자열이라 낡을 자리가 없다. `kyu init` 을 고칠 것도 없다 |
| **바이너리 경로가 늘 맞는다** | 답하는 `kyu` 가 `os.Executable()` 로 자기 경로를 적는다. 앱이 `KYU_BINARY_PATH` 로 어느 엔진을 겨누든 그 엔진이 자기를 등록한다 |
| **조립 지식이 한 곳에 남는다** | 원칙 11. 앱은 이 문자열을 만들지도 이해하지도 않고 그대로 실행한다 |
| **레포 안에 아무것도 안 들어간다** | 원칙 2. 워크디렉토리에도 안 들어간다 |
| **메인 세션에만 붙는다** | 레포 세션은 `session-command` 의 답을 받지만 워크디렉토리를 인자로 받지 않는다 — 5.2.1 |

**대가 하나를 적어둔다.** JSON 문자열이 프로세스 인자에 들어가므로 `ps` 에 보인다. 담기는 것은 `kyu` 절대경로와 워크디렉토리 절대경로뿐이라 비밀이 아니다. 비밀이 생기면 이 자리를 다시 정해야 한다.

#### 5.2.1 레포 세션에는 붙이지 않는다

`kyu session-command proj-a --json` 도 같은 표면이지만, 그 답에는 `--mcp-config` 를 넣지 않는다.

**근거**: 레포 세션이 `run_in_repo` 를 가지면 레포 세션이 다른 레포에 위임할 수 있게 된다. 그것은 조율을 메인 한 곳에 두는 이 도구의 운용 방침(선행 문서 3.2 — *"메인이 판단을 확정하고 각 레포는 적용만 한다"*)을 정면으로 어긴다. **위임의 시작점은 메인 하나다.**

### 5.3 도구 표면 — 둘

```
list_repos()
  → { workDir, repos: [ { name, absolutePath, state, task, recentDelegation } ] }

run_in_repo(repo: string, prompt: string, newConversation?: boolean)
  → { result, permissionDenials, conversationId, resumed, costUsd, durationMs, numTurns, logPath }
```

**`list_repos`**

`kyu list --json` 이 이미 답하는 것을 모델에게 그대로 넘긴다. 이 도구가 있어야 하는 이유는 두 가지다 — 모델이 **어떤 레포 이름이 유효한지** 알아야 `run_in_repo` 를 부를 수 있고, 워크디렉토리에 레포가 도중에 클론될 수 있어서 도구 스키마의 열거형으로 고정할 수 없다(선행 문서 5.3 — *"클론하면 곧바로 인식되는 것이 요구사항이다"*).

**`run_in_repo`**

| 인자 | 뜻 |
|---|---|
| `repo` | 레포 디렉토리 이름. 스캔 결과에 없으면 거절한다 |
| `prompt` | 그 레포에서 할 일. 메인이 쓴다 |
| `newConversation` | 참이면 이어가지 않고 새 대화로 시작한다. 기본 거짓 |

#### 5.3.1 plan 도구를 두지 않는다

골격은 도구 최소 집합에 *"plan 읽기/쓰기"* 를 넣었다. **두지 않는다.**

**근거**: 메인 세션의 cwd 가 워크디렉토리이므로, 메인은 `.coord/plan.md` 를 **내장 Read·Edit 도구로 이미 읽고 쓴다.** MCP 도구를 하나 더 두면 같은 파일에 이르는 길이 둘이 되고, 둘이 다르게 굴 자리(잠금·형식 검사·경고)가 생긴다. 선행 문서 6.2 가 계획을 파일로 둔 이유로 적은 것이 *"사람이 직접 읽고 고칠 수 있다"* 였고, 모델도 같은 자격으로 읽고 고치면 된다.

**원칙 4(새 추상화는 두 번째 사용처 후)의 직접 적용이다.** 두 번째 사용처가 나타나면 — 예를 들어 위임이 끝날 때 엔진이 `status` 를 자동 전이시키는 기능이 생기면(선행 문서 10절 v3) — 그때 파싱·쓰기가 엔진 안에 필요해지고 도구로 낼지 다시 정하면 된다.

### 5.4 헤드리스 실행 조립

`run_in_repo` 가 실제로 실행하는 것이다. 3 절의 실측만으로 세운다.

```
claude -p <프롬프트>
       --output-format json
       --permission-mode auto            ← 또는 --dangerously-skip-permissions (아래)
       --session-id <새 UUID>            ← 첫 위임
       --resume <기록된 UUID>            ← 이어가는 위임
cwd = <워크디렉토리>/<레포>
env = 바탕 환경
```

**`--add-dir` 을 붙이지 않는다.** 위임은 그 레포 안의 일이다. 다른 레포를 봐야 하는 일이라면 그것은 메인이 할 일이거나 다른 레포에 대한 별도 위임이다. 선행 문서 2절의 *"프로세스는 격리"* 가 위임에도 그대로 적용된다.

#### 5.4.1 권한 정책

**기본은 `--permission-mode auto` 다.**

| 후보 | 판단 |
|---|---|
| (없음) | ❌ 아무것도 못 한다(3.3) |
| `acceptEdits` | ❌ **MCP 도구 호출을 막는다**(3.3). 사용자 요구의 핵심이 성립하지 않는다 |
| `dontAsk` | ❌ 둘 다 막는다(3.3) |
| **`auto`** | ✅ **채택.** MCP 도구 호출과 파일 수정이 되면서 bypass 는 아니다 |
| `allowedTools` 나열 | ❌ 레포의 MCP 도구 이름을 미리 알아야 한다 — 서버를 두 번 띄우게 된다(3.3) |
| `dangerously-skip-permissions` | 조건부 — 아래 |

**bypass 는 메인이 켰을 때만 전파한다.** 원칙 13 이다.

전달 통로는 이미 있는 표면을 쓴다. `kyu session-command <repo> --bypass-permissions --json` 이 그 뜻을 받으므로(app-owned 5.2), **그 경우 메인 세션의 `env` 에 표식을 하나 더 넣는다.** stdio MCP 서버는 자기를 띄운 `claude` 의 환경을 물려받으므로 `kyu mcp` 가 그것을 읽고, 위임에 `--dangerously-skip-permissions` 를 붙인다.

```
앱이 bypass 로 메인을 연다
  → kyu session-command --bypass-permissions --json
  → 답의 env 에 KYU_BYPASS_PERMISSIONS=1
  → 메인 claude 가 그 환경으로 뜬다
  → 그 환경을 kyu mcp 가 물려받는다
  → run_in_repo 가 --dangerously-skip-permissions 를 붙인다
```

**위임이 스스로 bypass 를 켜는 인자는 두지 않는다.** 모델이 부르는 도구에 그 스위치를 두면 원칙 13 이 도구 설명 한 줄로 무너진다.

**`auto` 에 대해 잰 범위를 정직하게 적어둔다.** 이번 실측은 사소한 경우(초소형 MCP 도구 하나, 파일 한 줄 수정)만 쟀다. `auto` 는 분류기로 판단하는 모드이고(`claude auto-mode` 하위 명령이 그 설정을 다룬다), **파괴적 명령에 분류기가 어떻게 구는지는 재지 않았다.** 9절 1번이다.

#### 5.4.2 결과 반환 — 거절을 삼키지 않는다

**3.3 이 찾아낸 것이 이 계약을 정한다.** 권한에 막힌 위임은 종료 코드 0, `subtype` `success` 로 끝난다. 엔진이 종료 코드만 보면 **아무것도 하지 않은 위임을 성공으로 메인에게 보고한다.** 메인은 그것을 믿고 다음 걸음으로 간다.

**그래서 `run_in_repo` 는 `permission_denials` 가 비어 있지 않으면 그것을 답 본문에 실어 올린다.** 거절된 도구 이름을 그대로 적고, 위임이 완결되지 않았다는 것을 문장으로도 적는다. 모델이 그것을 읽고 사용자에게 물어야 한다.

| 상황 | `run_in_repo` 의 답 |
|---|---|
| 정상 | `result` 와 비용·시간·대화 ID |
| **권한 거절** | 위와 함께 **거절된 도구 목록과 "완결되지 않았다"** |
| 종료 코드 ≠ 0 | stderr 와 종료 코드. `--resume` 실패는 여기로 온다(3.5) |
| 시간 초과 | 얼마 만에 끊었는지와 로그 파일 자리 |

#### 5.4.3 원출력을 남긴다

`--output-format json` 의 전문을 `.coord/runs/<레포>-<시각>-<대화ID 앞 8 자>.json` 에 그대로 적는다.

**근거**: 위임은 이 도구가 사용자를 대신해 외부 명령을 실행하는 자리이고, 그 결과가 도구의 답 안에서 요약되어 사라지면 나중에 "그때 무엇이 거절됐나" 를 알 길이 없다. 원본 응답까지 남기는 것이 사후 추적의 유일한 재료다.

**`.coord/` 에 두는 근거는 app-owned 5.5.2 의 표 그대로다** — 워크디렉토리를 버리면 함께 사라지고, 옮기면 함께 가고, 사람이 열어볼 수 있다. 레포 안에는 넣지 않는다(원칙 2).

**정리 규칙을 v1 에 두지 않는다.** 워크디렉토리가 버려지는 물건이라 함께 사라진다(선행 문서 부록 B). 실제로 쌓여서 불편해지면 그때 정한다.

### 5.5 대화 이어가기

3.5 가 `-p` 에서도 `--session-id`·`--resume` 이 도는 것을 확인했으므로, app-owned 5.5.3 의 흐름을 위임에 그대로 쓴다.

```
run_in_repo("proj-a", …)
    │
    ├─ newConversation 이 참인가 → 새 UUID 로 --session-id
    │
    ├─ delegations 에 "proj-a" 기록이 있는가?
    │     ├─ 없다 → 새 UUID 를 만들어 기록하고 --session-id
    │     └─ 있다 → --resume <기록된 UUID>
    │
    └─ --resume 이 종료 코드 1 로 실패하면 → 기록을 버리고 새 대화로 한 번 다시 연다
```

**폴백이 여기서는 앱보다 쉽다.** app-owned 5.5.4 는 앱이 PTY 안의 실패를 가려내야 해서 열린 질문이 됐는데(그 문서 8절 2번), 위임은 엔진이 프로세스를 직접 실행하므로 **종료 코드와 stderr 를 그대로 본다.** `No conversation found with session ID:` 를 stderr 에서 확인하고 한 번 다시 여는 것으로 끝난다.

#### 5.5.1 기록 자리 — `conversations.json` 안의 별도 키 공간

```json
{
  "schemaVersion": 1,
  "conversations": {
    "main":   "d1b9d634-…",
    "proj-a": "211f6974-…"
  },
  "delegations": {
    "proj-a": "8f2c1a05-…"
  }
}
```

**같은 파일에 두되 키 공간을 가른다.**

| 판단 | 근거 |
|---|---|
| 같은 파일 | `.coord/` 에 파일을 하나 더 만들 이유가 없다. 필드를 더하는 변경은 판을 올리지 않는다(`internal/workdir/conversations.go:43` 의 판 규약) |
| **다른 키 공간** | **반드시 갈라야 한다.** 같은 레포에 앱 세션 대화와 위임 대화가 동시에 있을 수 있고, 하나의 UUID 를 둘이 쓰면 뒤에 오는 쪽이 `already in use` 로 거절된다(3.5) |
| 접두사를 쓰지 않는다 | `"delegate:proj-a"` 같은 키를 섞으면 한 지도에 두 종류의 키가 산다. 객체를 나누면 그럴 일이 없다 |

**기존 코드가 자리를 이미 갖고 있다.** `AssignConversation(workDirPath, label)`(`internal/workdir/conversations.go:85`) 과 `StartNewConversation`(`:114`) 이 라벨 하나를 받아 배정한다. 위임용은 같은 파일의 다른 지도를 보게 하는 인자 하나를 더하는 일이다.

### 5.6 레포의 `.mcp.json` 이 사람에게 보이지 않는 채로 돈다

**3.2 가 찾아낸 나쁜 쪽이다.** 대화형에는 승인 관문이 있고 헤드리스에는 없다. 위임은 헤드리스다. 그러므로 이 도구는 **선행 문서 1.3 이 보안 설계라고 부른 관문을 우회하는 길을 새로 낸다.**

구체적으로 이렇게 된다.

```
kyu clone 으로 레포를 받는다
  → 메인이 그 레포에 위임한다
  → 레포의 .mcp.json 에 적힌 command 가 돈다
  → 사용자는 그 파일을 한 번도 본 적이 없다
```

**후보**

| 후보 | 판단 |
|---|---|
| (가) 첫 위임 전에 그 레포의 `.mcp.json` 을 사용자에게 보이고 승인받는다. 승인은 `.coord/` 에 남기고, 파일 내용이 바뀌면 다시 묻는다 | **추천** |
| (나) `--strict-mcp-config` 로 레포 `.mcp.json` 을 아예 안 붙인다 | ❌ 사용자 요구(*"MCP 설정 등을 다 활용한 채로"*)가 성립하지 않는다 |
| (다) 그대로 둔다 — 사용자가 자기가 고른 레포만 클론하므로 | ❌ 클론한 것과 그 안의 `.mcp.json` 을 읽은 것은 다른 일이다 |

**(가)를 고른다.** 이유는 이것이 `claude` 자신의 자세와 같기 때문이다 — 대화형이 하는 일을 이 도구가 헤드리스에 대해 대신한다. 우회로를 내는 쪽이 관문도 함께 낸다.

**모양**: 승인 상태를 `.coord/` 에 레포 이름과 `.mcp.json` 내용 해시로 적는다. 해시가 다르면 다시 묻는다. **레포 안에는 넣지 않는다**(원칙 2) — 팀 공용 레포에 우리 도구의 승인 기록이 남으면 안 된다.

**묻는 자리가 어디인가는 갈린다.** 앱이 메인을 열었으면 앱의 대화 상자이고, 터미널이면 CLI 프롬프트다. **`.mcp.json` 이 없는 레포는 묻지 않는다** — 물을 것이 없다. 그리고 이것이 대부분의 레포다.

**그래서 사용자 요구가 깨지지 않는다.** *"알아서 세션이 뜨면서"* 가 관문 때문에 멈추는 것은 그 레포에 `.mcp.json` 이 있는 첫 위임 한 번뿐이다.

### 5.7 동시 위임

3.7 이 격리를 확인했으므로 막지 않는다. 메인이 `run_in_repo` 를 세 번 병렬로 부르면 세 프로세스가 뜬다.

**같은 레포에 동시 위임 둘은 막는다.** 두 프로세스가 같은 디렉토리의 파일을 동시에 고치면 서로의 수정을 덮는다. 그리고 대화 ID 가 하나뿐이라 뒤에 오는 쪽이 `already in use` 로 거절된다(3.5) — **어차피 거절되므로, 알아들을 수 있는 말로 먼저 거절한다.**

**전체 동시 실행 수에 상한을 두지 않는다.** 워크디렉토리의 레포 수가 이미 상한이고(레포당 하나), 그 수는 사람이 클론한 만큼이라 폭주하지 않는다. 숫자를 하나 두면 그 숫자가 맞는지를 계속 다시 정해야 한다(YAGNI).

---

## 6. 앱 통합

### 6.1 엔진이 띄운 실행이 카드에 보이는 길

`kyu list --json` 에 필드를 하나 더한다. **판은 오르지 않는다** — 필드를 더하는 변경으로는 올리지 않는다는 규약이 이미 적혀 있다(`internal/cli/list_json.go:56` 의 주석).

```json
{
  "name": "proj-a",
  "state": "DIRTY",
  "task": { … },
  "hasRecordedConversation": true,
  "recentDelegation": {
    "finishedAt": "2026-09-01T14:22:31Z",
    "exitCode": 0,
    "hadPermissionDenials": false,
    "logPath": "/home/me/work/WorkDir-featureX/.coord/runs/proj-a-20260901-142231-8f2c1a05.json"
  }
}
```

**엔진은 `.coord/runs/` 를 읽어서 답한다.** 별도의 상태 파일을 두지 않는다 — 원출력 기록이 이미 그 자리에 있고(5.4.3), 그것을 읽는 것이 곧 관찰이다.

### 6.2 "진행 중" 을 v1 에 두지 않는다

**골격은 *"앱은 엔진이 띄운 레포 실행을 카드에 표시"* 였다. v1 이 표시하는 것은 끝난 위임이고, 도는 중인 위임이 아니다.**

**근거는 원칙 6 이다.** 도는 중이라는 것을 알리려면 시작할 때 그것을 어딘가에 적어야 하고, 그 순간 그것은 **관찰된 상태가 아니라 선언된 상태**가 된다. 그리고 그 프로세스가 `SIGKILL` 로 죽으면 끝 줄이 안 적히고, 그때부터 그 파일은 거짓말을 한다 — 죽은 위임을 "진행 중" 이라고 말하고 고칠 사람이 없다.

**app-owned 5.4 가 앱 세션에 대해 같은 이유로 같은 결정을 했다.**

> 앱이 자기 세션 목록을 파일에 적고 엔진이 그것을 읽게 하면, 그 파일은 **선언된 상태**가 되어 원칙 6 을 어긴다. 그리고 앱이 크래시하면 그 파일은 그 순간부터 거짓말을 한다.

**v2 의 길은 있고, 값이 얼마인지도 안다.** 시작 줄에 자식 PID 를 함께 적고, "진행 중" 을 **그 PID 가 살아 있는가**로 판정하면 선언이 아니라 관찰이 된다. 대가는 PID 생존 확인이 플랫폼 의존부라는 것이다 — 그리고 PR #49 가 방금 `internal/session/` 을 통째로 걷어냈다. **플랫폼 의존 코드를 없앤 직후에 다시 들이는 값이 "도는 중 표시" 하나보다 크다고 본다.**

**그리고 v1 에 없어도 견딜 만하다.** 위임은 15~60 초짜리다(3.6·3.7). 그 사이 메인 세션 화면에는 도구 호출이 도는 것이 이미 보이고, 앱을 쓰는 사람은 그 화면을 보고 있다.

### 6.3 출력 열람

**v1 은 로그 파일 자리를 보이는 것으로 충분하다.** 카드가 최근 위임 줄을 보이고, 누르면 `.coord/runs/` 의 그 파일을 연다.

**근거**: 앱은 이미 기록 파일 자리를 화면에 안내하는 자세를 갖고 있다 — `desktop/src/main/kotlin/com/kyuchestration/desktop/DiagnosticLogPathNotice.kt` 가 오류 화면마다 진단 기록 파일 자리를 알려준다. 같은 자세를 여기에도 쓰면 새로 만들 화면이 없다. 위임 출력 전용 뷰어는 실제로 불편해진 뒤에 만든다(YAGNI).

---

## 7. 검토한 대안과 기각 사유

**이 절은 다른 세션에서 "이거 그냥 X 하면 되는 거 아냐?" 를 다시 묻지 않기 위한 기록이다**(선행 문서 3절 · app-owned 3절과 같은 목적).

### 7.1 서브에이전트 정의를 레포마다 자동 생성한다

선행 문서 3.1 의 계보. `kyu` 가 레포마다 `.claude/agents/<repo>.md` 를 만들고 그 frontmatter 의 `mcpServers` 에 레포의 `.mcp.json` 내용을 inline 으로 옮긴다. 메인 세션이 그 서브에이전트에게 위임한다.

**기각.** 선행 문서 3.1 의 기각 사유 (2) 가 **이번 실측으로 더 강해졌다.**

3.1 이 적은 것은 *"MCP 는 inline 으로 해결해도 그 레포의 `CLAUDE.md`·커스텀 에이전트·훅은 로드되지 않는다"* 였다. 그때는 "로드되지 않는다" 가 아쉬움이었는데, **3.1 실측이 레포 cwd 프로세스에서는 그 셋이 전부 산다는 것을 보였다.** 즉 이 대안을 고르는 것은 이제 "얻지 못한다" 가 아니라 **"버린다"** 다.

부차적으로 3.1 의 기각 사유 (1)(레포마다 파일을 만들어야 한다)도 그대로다. 그리고 그 파일을 레포 안에 만들면 원칙 2 위반이고, 워크디렉토리에 만들면 서브에이전트로 발견되지 않는다.

### 7.2 대화형 레포 세션에 작업을 주입한다

앱이 이미 보유한 레포 세션의 PTY 에 프롬프트를 써 넣는다. 세션이 이미 떠 있으므로 부팅 비용이 없고(3.6), 사람이 그 화면을 보고 있다.

**기각 사유 (1) — 선행 문서 원칙 3 정면 위반.** *"도구는 세션을 띄우고, 보여주고, 진입시킬 뿐 세션의 입출력을 가로채지 않는다."* 주입은 입력을 가로채는 일이다.

**기각 사유 (2) — 답을 받을 길이 없다.** PTY 로 돌아오는 것은 사람이 읽는 화면이지 기계가 읽는 문서가 아니다. 메인이 위임의 결과를 알려면 그 화면을 파싱해야 하고, 그것이 app-owned 3.4 가 기각한 바로 그 경주다 — *"이 도구가 `claude` 를 따라잡는 경주에 들어간다."*

**기각 사유 (3) — 세션이 떠 있어야 한다.** 사용자 요구는 *"알아서 클론 레포에 세션이 뜨면서"* 다. 이 안은 사람이 먼저 카드를 눌러야 한다 — 요구의 출발점을 그대로 남긴다.

### 7.3 앱이 위임 UI 를 갖는다

카드에 "이 레포에 시킬 일" 입력란을 두고, 앱이 `kyu` 를 불러 헤드리스로 실행한다. MCP 서버가 필요 없다.

**기각 사유 — 사용자 요구를 정면으로 어긴다.** 요구는 *"수동으로 카드를 눌러 … 가 아니라, 그냥 메인과 대화하면"* 이다. 이 안은 카드를 누르는 일을 "카드에 글을 쓰는 일" 로 바꿀 뿐이고, **무엇을 어느 레포에서 할지를 사람이 정하는 구조가 그대로다.** 요구의 첫 두 줄(1.1 의 표)이 남는다.

**남는 것**: 위임의 결과를 앱이 보이는 것은 이 기각과 무관하게 필요하고, 6절이 그것이다.

### 7.4 메인이 `--add-dir` 로 직접 고친다

`CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` 을 켜면 추가 디렉토리의 지침까지 읽는다(선행 문서 5.4). 메인이 파일을 직접 고치면 위임이 아예 필요 없다.

**기각 사유 — MCP 는 끝내 안 따라온다.** 그 환경변수가 여는 것은 지침 파일이지 MCP 서버가 아니다(선행 문서 부록 A). 사용자 요구의 문장에서 유일하게 이름이 나온 것이 **MCP** 다 — *"해당 레포의 MCP 설정 등을 다 활용한 채로"*. 이 안은 그 한 낱말을 정확히 못 준다.

부차적으로, 레포 수만큼 컨텍스트가 늘고(선행 문서 5.4 가 기본값을 끈 이유), 훅과 커스텀 에이전트도 안 따라온다.

### 7.5 `--strict-mcp-config` 로 사용자 전역 MCP 를 떨어뜨린다

위임에 `--strict-mcp-config --mcp-config <레포>/.mcp.json` 을 주면 고정 비용이 3860 ms 에서 2039 ms 로 준다(3.6).

**보류 — 기각은 아니다.** 1.8 초는 진짜 이득이다. 그런데 그렇게 하면 **위임이 사람이 손으로 띄운 세션과 달라진다** — 2절이 이 설계의 핵심 문제로 세운 바로 그 자리다. 사람이 카드를 눌러 띄운 세션에는 사용자 전역 MCP 가 붙어 있고, 위임에는 없게 된다. 위임이 "전역 MCP 도구를 쓰는 일" 을 못 하는 것을 사용자는 나중에 발견하게 된다.

**기본은 붙이지 않는다(= 전역 MCP 를 그대로 둔다).** 실제로 느려서 불편해지면 옵션으로 연다. 9절 4번.

---

## 8. 구현 단계

각 단계가 **PR 하나**이고, 각 단계 끝에 손으로 확인 가능한 상태가 된다(선행 문서 9.2 · app-owned 7절과 같은 자세).

| 단계 | 내용 | 완료 확인 |
|---|---|---|
| **1** | `kyu mcp <워크디렉토리>` — stdio MCP 서버 골격과 `list_repos`(5.3). `kyu session-command` 가 답에 `--mcp-config` 를 싣는다(5.2), 레포 세션에는 싣지 않는다(5.2.1) | 앱에서 메인 카드를 열면 **승인 프롬프트 없이** `kyu` 서버가 붙는다. 세션 안에서 `/mcp` 가 그것을 보이고, `list_repos` 가 레포 목록을 답한다. 레포 카드를 열면 그 세션에는 `kyu` 서버가 없다. **`--mcp-config` 가 대화형에서도 관문을 안 타는지를 여기서 처음 눈으로 본다**(3.8 은 `-p` 로만 쟀다) |
| **2** | `run_in_repo` — 조립(5.4)·실행·결과 반환(5.4.2)·원출력 기록(5.4.3). 권한은 `auto` 고정, 대화 이어가기는 아직 없다 | 메인에게 *"proj-a 에 README 한 줄 추가해줘"* 라고 말하면 그 레포에서 실행이 돌고 파일이 바뀐다. `.coord/runs/` 에 원출력이 남는다. **레포에 `.mcp.json` 을 두고 그 도구를 쓰라고 시키면 실제로 불린다** — 이 도구의 존재 이유가 성립하는지를 직접 재는 항목이다 |
| **3** | 권한 전파 — `--bypass-permissions` 가 `env` 표식으로 위임까지 닿는다(5.4.1). 거절이 답 본문에 실리는 것을 확인 | bypass 없이 연 메인의 위임은 `auto` 로 돌고, bypass 로 연 메인의 위임은 bypass 로 돈다. **막힌 위임이 "성공" 으로 보고되지 않는다** — 종료 코드 0 인데 거절이 있는 경우를 만들어 확인한다 |
| **4** | 대화 이어가기 — `conversations.json` 의 `delegations`(5.5.1), 첫 회/이후 갈래, `--resume` 실패 폴백 | 같은 레포에 두 번 위임하면 두 번째가 첫 번째를 기억한다. `delegations` 의 UUID 를 손으로 망가뜨리면 새 대화가 한 번 열리고 기록이 새 값으로 바뀐다. **같은 레포에 동시 위임 둘은 알아들을 수 있는 말로 거절된다**(5.7) |
| **5** | `.mcp.json` 관문(5.6) — 첫 위임 전 승인, `.coord/` 기록, 해시가 바뀌면 다시 묻기 | `.mcp.json` 이 있는 레포에 처음 위임하면 그 내용이 사람에게 보이고 승인을 받는다. 두 번째 위임은 묻지 않는다. 파일을 고치면 다시 묻는다. **`.mcp.json` 이 없는 레포는 한 번도 묻지 않는다** |
| **6** | 앱 통합 — `kyu list --json` 의 `recentDelegation`(6.1), 카드 표시, 로그 파일 열기(6.3) | 위임이 끝난 레포의 카드에 그 사실과 시각이 보이고, 눌러서 원출력을 연다. 거절이 있었던 위임은 갈라 보인다 |

**1 단계가 먼저인 이유**: 이 설계에서 유일하게 "실측이 대화형까지 닿지 못한" 자리가 `--mcp-config` 의 승인 거동이다(3.8). 그것이 틀렸다면 5.2 의 등록 방식을 통째로 다시 정해야 하고, 그 판정은 서버가 아무것도 못 해도 할 수 있다. **가장 싼 걸음에서 가장 큰 미지수를 끝낸다.**

**5 단계가 뒤인 이유**: 관문은 위임이 실제로 도는 것을 본 뒤에 세워야 값을 정할 수 있다. 그리고 관문이 없는 동안에도 위험이 커지지 않는다 — 그 사이 위임을 시켜보는 레포는 우리가 만든 것뿐이다.

---

## 9. 열린 질문

구현을 막지는 않지만 해당 단계에서 정해야 하는 것들이다.

1. **`--permission-mode auto` 의 분류기가 파괴적 명령에 어떻게 구는가** — 3.3 은 사소한 경우만 쟀다. `rm -rf`·`git push --force`·`DROP TABLE` 같은 것을 분류기가 막는지, 막는다면 그것이 `permission_denials` 로 오는지. **2 단계에서 잰다.** 결과에 따라 기본 권한을 `auto` 에서 `allowedTools` 나열로 옮겨야 할 수도 있다.

2. **위임을 도중에 멈추는 길** — 지금 설계에 취소 통로가 없다. 메인이 위임을 걸어두고 사용자가 "아니 그만" 이라고 말하면 할 수 있는 일이 없다. 후보: (가) `run_in_repo` 가 도는 동안 MCP 도구 호출을 취소하면 자식 프로세스를 죽인다 (나) 별도의 `cancel_delegation` 도구 (다) 두지 않는다. **(가)를 먼저 본다** — MCP 클라이언트가 도구 호출 취소를 어떻게 알리는지 확인이 필요하다.

3. **위임의 시간 상한** — 숫자를 하나 정해야 한다. 3.6·3.7 에서 잰 것은 15~60 초짜리 사소한 일이고, 진짜 일은 몇 분이 걸린다. 상한이 없으면 메인이 영원히 기다리고, 짧으면 진짜 일이 잘린다. **2 단계.** `--max-budget-usd` 로 비용에 상한을 두는 길도 함께 본다.

4. **사용자 전역 MCP 를 위임에서 떨어뜨릴 것인가** — 7.5. 1.8 초 대 "진짜 세션과 같음". 실제로 느려서 불편한지 보고 정한다.

5. **위임이 만든 변경을 메인이 어떻게 확인하는가** — 지금은 `result` 텍스트가 전부다. 위임이 "고쳤다" 고 말했는데 안 고쳤을 수 있다. `list_repos` 의 `state` 가 `DIRTY` 로 바뀌는 것이 하나의 신호지만 무엇이 바뀌었는지는 말하지 않는다. 후보: 위임 뒤 `git diff --stat` 을 답에 함께 싣는다. **2 단계 이후.**

6. **`run_in_repo` 의 프롬프트를 누가 쓰는가** — 메인의 모델이 쓴다. 그런데 그 프롬프트가 `.coord/plan.md` 의 확정 인터페이스를 담고 있지 않으면 위임이 딴 것을 만든다(선행 문서 6.1 이 *"요약하지 않는다"* 라고 적어둔 그 자리). 도구 설명에 "계획의 확정 인터페이스를 그대로 옮겨 적어라" 를 넣을지, 엔진이 `plan.md` 를 프롬프트에 자동으로 덧붙일지. **후자는 원칙 3 에 걸린다** — 엔진이 위임의 내용에 개입하게 된다. **2 단계에서 정한다.**

7. **진행 중 표시를 언제 여는가** — 6.2. PID 관찰이 길이고, 값은 플랫폼 의존부를 다시 들이는 것이다. 위임이 길어지는 일이 잦아지면 그때.

8. **`kyu mcp` 라는 이름** — `claude mcp` 는 설정 명령이고 서버는 `claude mcp serve` 다. 이 도구에서 `mcp` 라는 낱말 아래 있을 것이 서버 하나뿐이라 지금은 `kyu mcp` 로 둔다. 두 번째가 생기면 `kyu mcp serve` 로 옮긴다.

---

## 부록 A. 실측 원출력 요약

**환경**: 2026-09-01, `claude` 2.1.252, Linux(WSL2), 모델 `haiku`(권한 매트릭스·시간)·`sonnet`(도구 호출 확정). 부모 세션의 `CLAUDE*` 환경변수를 전부 걷어낸 뒤 실행했다.

### A.1 `claude mcp list` — 승인 없는 새 프로젝트

```
kyu-probe: python3 …/probe_mcp_server.py - ⏸ Pending approval (run `claude` to approve)
```

부팅 로그: 없음. **`-p` 를 한 번 돌린 뒤 다시 불러도 같다.**

### A.2 `-p` 에서 프로브 서버가 부팅된 흔적

```
1788247351.181 boot pid=979914 cwd=…/fake-repo
1788247351.245 recv initialize
1788247351.306 recv notifications/initialized
1788247351.306 recv tools/list
```

`tools/call` 은 없다 — 붙기는 했고 **권한에 막혀 부르지 못했다**(3.3 의 첫 줄).

### A.3 `--dangerously-skip-permissions` 에서 실제로 불린 흔적

```
1788247483.670 boot pid=982002 cwd=…/fake-repo
1788247492.266 recv tools/call
1788247492.266 tools/call repo_fingerprint
```

답: `repo_fingerprint 결과: MCPFP-9182 | target.txt 편집: ✓ "before" → "EDITED" 성공`

### A.4 `--output-format json` 의 최상위 키 (23 개)

```
api_error_status, duration_api_ms, duration_ms, fast_mode_disabled_reason,
fast_mode_state, is_error, modelUsage, num_turns, permission_denials,
queued_turn_count, result, session_id, stop_reason, subagent_stats, subtype,
terminal_reason, time_to_request_ms, total_cost_usd, ttft_ms, ttft_stream_ms,
type, usage, uuid
```

### A.5 권한에 막힌 실행의 답 (기본 모드)

```
exit=0   subtype='success'   is_error=False   num_turns=5
total_cost_usd=0.0417…   session_id='71d7d2aa-…'
permission_denials=[
  {"tool_name":"mcp__kyu-probe__repo_fingerprint","tool_use_id":"toolu_019Yj…","tool_input":{}},
  {"tool_name":"Edit","tool_use_id":"toolu_013xU…",
   "tool_input":{"file_path":"…/target.txt","old_string":"before","new_string":"EDITED"}}]
result='repo_fingerprint 도구와 target.txt 파일 편집에 권한이 필요합니다. 사용자님의 승인을 기다리고 있습니다.'
```

**`target.txt` 는 `before` 그대로였다.**

### A.6 대화 이어가기

```
1 회차  --session-id aa97f289-…   → 돌려준 session_id 가 같음.  result='알겠습니다.'
2 회차  --resume     aa97f289-…   → result='4721'          (1 회차에 기억시킨 숫자)
3 회차  --resume     <없는 uuid>  → exit=1
                                    stderr: No conversation found with session ID: 887b1e33-…
4 회차  --session-id aa97f289-…   → exit=1
                                    stderr: Error: Session ID aa97f289-… is already in use.
전사    ~/.claude/projects/-tmp-…-fake-repo/aa97f289-….jsonl
```

### A.7 고정 비용 (각 3 회, 벽시계 − `duration_ms`)

```
(1) 전역 MCP + 레포 .mcp.json   3828 / 3987 / 3767 ms   평균 3860
(2) 레포 .mcp.json 만            1948 / 2175 / 1996 ms   평균 2039
(3) MCP 없음                     1866 / 1752 / 1765 ms   평균 1794
```

### A.8 동시 위임 셋

```
동시 실행 벽시계 총합 = 28405 ms      (셋 다 exit=0)
repo-a  duration_ms=2275   자기 지문 FP-A-777 ✅  MCP 지문 ✅  다른 레포 지문 섞임 ❌  tools/call=1
repo-b  duration_ms=13481  자기 지문 FP-B-777 ✅  MCP 지문 ✅  다른 레포 지문 섞임 ❌  tools/call=1
repo-c  duration_ms=12258  자기 지문 FP-C-777 ✅  MCP 지문 ✅  다른 레포 지문 섞임 ❌  tools/call=1
```

### A.9 `--agent` 로 가른 에이전트 발견

```
레포 cwd 에서   claude -p --agent probe-agent   → exit=0
레포 밖에서     claude -p --agent probe-agent   → exit=1
   stderr: --agent 'probe-agent' not found. Available agents: claude, coding-agent,
           Explore, general-purpose, Plan, statusline-setup
```

---

## 부록 B. 실측 재현 방법

측정 스크립트는 커밋하지 않는다 — 한 번 답을 얻으면 그 답이 이 문서에 남고, 스크립트는 그 답을 다시 얻는 길일 뿐이다. 다시 재야 할 때 만들 수 있도록 재료만 적어둔다.

**재료 넷을 격리된 스크래치 디렉토리에 만든다.**

| 재료 | 내용 |
|---|---|
| `CLAUDE.md` | 지문 코드 한 줄 (`KYUFP-7431`) 과 "지문을 물으면 그것만 답하라" |
| `.mcp.json` | 아래의 초소형 stdio 서버를 `type: "stdio"` 로 등록. `env` 에 부팅 로그 경로 |
| 초소형 stdio MCP 서버 | `initialize` · `tools/list` · `tools/call` · `ping` 만 답하는 JSON-RPC 루프. **모든 수신을 로그 파일에 적는다** — 이것이 판정 근거다 |
| `.claude/settings.json` · `.claude/agents/probe-agent.md` | `SessionStart` 훅이 로그 파일에 한 줄 적게. 에이전트는 이름만 있으면 된다 |

**판정할 때 지킨 것 셋**

1. **모델의 말을 근거로 쓰지 않는다.** MCP 는 서버가 남긴 로그로, 훅은 훅이 쓴 파일로, 에이전트는 종료 코드로 가른다. 모델은 도구를 안 부르고 답을 지어낼 수 있다 — 실제로 한 번 그랬다(3.1 의 마지막 문단).
2. **부모 세션의 환경을 걷어낸다.** `CLAUDECODE`·`CLAUDE_CODE_*` 를 `env -u` 로 전부 지우고 실행한다.
3. **승인 이력이 없는 새 경로에서 잰다.** `~/.claude.json` 의 `projects` 에 그 경로가 없는 것을 먼저 확인한다. 있으면 3.2 의 측정이 성립하지 않는다.

**시간을 잴 때**: 벽시계에서 답 문서의 `duration_ms` 를 뺀다. 그것이 프로세스 기동 + MCP 부팅이다. API 시간은 회차마다 크게 흔들리지만 그 차이는 안정적이다(A.7).

---

## 부록 C. 확인된 사실

구현 중 다시 조사하지 않도록 모아둔다. 선행 문서들의 부록에 있는 사실은 되풀이하지 않는다.

| 사실 | 근거 |
|---|---|
| 레포 cwd 의 `claude -p` 는 `CLAUDE.md` · `.mcp.json` · `.claude/settings.json`(훅) · `.claude/agents/` 를 **전부** 로드한다 | 2026-09-01 실측 (3.1) |
| `.mcp.json` 승인 관문은 **대화형에만 있다.** `-p` 는 승인 없는 프로젝트의 서버에도 붙는다 | 2026-09-01 실측 (3.2) |
| `-p` 실행은 승인을 **저장하지 않는다** — 뒤에 `claude mcp list` 를 불러도 여전히 `Pending approval` | 2026-09-01 실측 (3.2) |
| `--mcp-config` 로 넘긴 서버는 승인 관문을 타지 않고, **JSON 문자열**도 받는다 | 2026-09-01 실측 (3.8). **대화형에서는 재지 못했다 — 구현 1 단계에서 확인** |
| `enableAllProjectMcpServers` · `enabledMcpjsonServers` 는 `claude mcp list` 기준으로 듣지 않았다. `disabledMcpjsonServers` 는 듣는다 | 2026-09-01 실측 (3.8) |
| `--permission-mode acceptEdits` 는 파일 수정을 허용하지만 **MCP 도구 호출은 막는다** | 2026-09-01 실측 (3.3) |
| `--permission-mode auto` 는 MCP 도구 호출과 파일 수정을 둘 다 허용한다 (사소한 경우에 한해 확인) | 2026-09-01 실측 (3.3) |
| **권한에 막힌 실행도 종료 코드 0 · `subtype: success` 로 끝난다.** `permission_denials` 배열이 유일한 단서다 | 2026-09-01 실측 (3.3) |
| `-p` 에서 `--session-id` 와 `--resume` 이 모두 동작한다 | 2026-09-01 실측 (3.5) |
| **한 번 쓴 대화 ID 는 `--session-id` 로 다시 쓸 수 없다** — `Error: Session ID … is already in use.` 그 대화가 끝난 뒤에도 그렇다 | 2026-09-01 실측 (3.5). **app-owned 8 절 6 번을 닫는다** |
| 위임 한 번의 고정 비용은 MCP 없이 ~1.8 초, 초소형 서버 하나에 ~0.25 초, 사용자 전역 MCP 집합에 ~1.8 초 | 2026-09-01 실측 (3.6) |
| 레포 셋에 동시 위임해도 설정이 섞이지 않는다. 다만 벽시계가 가장 느린 하나보다 길다 — 완전한 병렬은 아니다 | 2026-09-01 실측 (3.7) |
| `claude --help`: `-p` 는 workspace trust 대화를 건너뛰고, **검증에 실패한 설정 파일을 조용히 무시한다** | `claude --help` (2.1.252). 위임이 레포의 깨진 `settings.json` 을 만나도 알려주지 않는다는 뜻이다 |

## 부록 D. 용어

선행 문서 부록 B · app-owned 부록 B 에 더한다.

| 용어 | 의미 |
|---|---|
| **위임** (delegation) | 메인 세션이 `run_in_repo` 로 레포에 시키는 헤드리스 실행 한 번. 세션이 아니다 — PTY 가 없고 사람이 붙지 않는다 |
| **오케스트레이션 MCP 서버** | `kyu mcp` 가 stdio 로 여는 서버. 메인 세션에만 붙는다 |
| **관문** | 사람에게 한 번 보이고 승인을 받는 자리. `claude` 의 것은 대화형에만 있고(3.2), 이 도구가 헤드리스에 대해 같은 것을 세운다(5.6) |
