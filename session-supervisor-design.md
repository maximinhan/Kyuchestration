# 세션 감독 — 설계 문서

> 세션이 tmux 없이 살게 한다. 도구를 꺼도 작업이 안 끊긴다는 성질은 그대로 두고, 그 성질을 남의 프로그램이 아니라 `kyu` 자신이 떠받친다. **세션 하나에 감독 프로세스 하나.**

- 작성일: 2026-08-31
- 상태: **1~3 단계 구현 완료 (PR #36 · #37 · #38), 3.5 단계 착수 전 중단.** 아래 상자를 읽고 본문으로 들어올 것
- 선행 문서: [workdir-orchestrator-design.md](workdir-orchestrator-design.md) — 특히 5.2(SessionBackend), 8.2(tmux 위임으로 얻는 것), 원칙 5
- 대상 플랫폼: macOS, Linux(WSL 포함) — 윈도우 네이티브는 8절
- 사용자 결정 (2026-08-31): **tmux 의존을 제거한다.** 이 문서는 그 구현의 근거다
- 사용자 결정 (2026-08-31): **세션별 감독 프로세스를 채택하고 중앙 데몬 하나를 기각한다.** 근거는 크래시 반경 최소화, 그리고 기동 경쟁·자동 종료 정책·프로토콜 전부-아니면-전무가 설계에서 사라지는 것 — 기각된 중앙 데몬 안의 전문은 3.1 에 남긴다

---

## 0. 지금 이 문서의 처지 (2026-08-31 추가)

**이 작업은 3.5 단계에 착수하기 전에 멈췄다.** 아래 세 줄이 그 전부다.

| 어디까지 왔나 | 무엇이 남았나 | 지금 어떤 처지인가 |
|---|---|---|
| 1~3 단계가 머지되어 있다 (PR #36 · #37 · #38). 감독은 `KYU_SESSION_BACKEND=supervisor` 로 실제로 돈다 | **3.5 단계(감독 안의 화면 모델)에 착수하지 않았다.** 그것이 없으므로 4 단계(기본값 전환)도 그대로 걸려 있다 | **CLI 옵션으로 존치.** 기본값은 tmux 이고, 동등성 매트릭스가 CI 에서 계속 두 백엔드를 돈다 |

**왜 멈췄나 — 두 가지가 겹쳤다.**

1. **관문 실측이 불통과했다** (5.7.1). 링이 한 바퀴 감긴 뒤의 재접속은 화면만이 아니라 터미널 모드(입력)를 잃고, `SIGWINCH` 다시 그리기가 그것을 되돌리지 못한다. 이 문서의 5.7 이 미리 정해둔 갈림길대로 3.5 단계가 4 단계 앞에 들어갔다.
2. **그 3.5 단계 앞에서 방향이 바뀌었다** (사용자 결정, 2026-08-31). 데스크톱 앱은 이미 JediTerm 위젯이라는 완성된 화면 모델을 갖고 있어서, 앱이 `claude` 를 직접 띄우면 재접속·재생이라는 문제가 **풀리는 것이 아니라 성립하지 않는다.** 그래서 GUI 의 세션 계층을 앱이 보유하는 쪽으로 간다 — [app-owned-sessions-design.md](app-owned-sessions-design.md).

**중단이지 기각이 아니다.** CLI 에는 여전히 같은 요구가 있다 — 터미널 창은 앱과 달리 세션을 보유하지 않으므로, tmux 없이 "창을 닫아도 세션이 산다" 를 주는 길은 감독뿐이다. 3.5 단계가 들어가면 관문은 통과한다. 재개 조건과 그 판단의 근거는 새 문서 3.1 · 3.2 · 6 절에 있다.

**본문은 소급해서 고치지 않는다.** 2 절 아래의 모든 절은 이 결정 **이전**에 쓰인 것이고, 그 판단들은 감독 자체에 대해서는 지금도 유효하다. 새 문서가 뒤집는 것은 감독의 설계가 아니라 **GUI 가 감독을 기다린다는 전제** 하나다. 특히 3.3("GUI 가 세션을 보유" — 기각)은 그때의 근거를 그대로 두되, 그 기각이 뒤집힌 경위는 [app-owned-sessions-design.md 1.4](app-owned-sessions-design.md#14-무엇을-잃는가--원칙-5-개정) 에서 읽는다.

---

## 1. 배경 — 왜 tmux 를 걷어내는가

### 1.1 지금 구조

세션 계층은 tmux 에 위임되어 있다. `internal/session/tmux.go` 의 다섯 함수가 tmux 명령 다섯 줄에 그대로 대응하고, 그 바깥에서는 tmux 를 부르지 않는다(`internal/session/tmux.go:48`·`71`·`98`·`116`·`138`).

이 위임은 선행 문서 8.2 의 판단이었고, 그 판단은 지금도 맞다. tmux 는 PTY 관리·리사이즈·스크롤백·detach 재접속·도구와 독립적인 세션 생존을 전부 대신 해준다. 그 다섯 줄이 없었다면 v1 은 훨씬 늦게 나왔을 것이다.

문제는 위임 자체가 아니라 **위임한 상대가 사용자의 머신에 따로 설치되어야 하는 외부 프로그램**이라는 데서 나온다.

### 1.2 tmux 가 물리는 지점

**(1) 설치 요구가 도구의 첫인상을 정한다**

README 요구사항 표의 첫 줄이 tmux 다 — "없으면 `kyu init` 외의 명령이 안내와 함께 종료". 릴리스 바이너리는 한 줄로 받을 수 있게 만들어 두었는데(`install.sh`), 받은 사람은 곧바로 패키지 관리자로 돌아가 tmux 를 설치해야 한다. 데스크톱 앱은 엔진까지 동봉해 "앱만 설치하면 끝"을 표방하지만, 그 앱도 tmux 앞에서는 같은 자리에 선다(README 의 설치 안내가 `sudo apt install tmux` 를 시스템 패키지·관리자 권한과 함께 요구하는 이유).

**(2) 윈도우 네이티브가 이 한 줄에 막혀 있다**

로드맵 v4(윈도우 네이티브 백엔드)가 미착수인 이유가 tmux 다. `desktop/src/main/kotlin/com/kyuchestration/desktop/engine/githubrelease/EngineReleaseTarget.kt:7` 은 그 사실을 사용자에게 "WSL 안에서 앱을 띄우세요"로 옮기고 있고, 릴리스 워크플로는 윈도우 msi 를 아예 만들지 않는다. **tmux 를 걷어내는 것은 윈도우 지원의 전제다** — 이 작업이 끝나야 v4 가 "포팅"이 되고, 끝나기 전에는 "다시 만들기"다.

**(3) 세션 계층의 지식이 표시 계층으로 새어 있다**

`internal/cli/attach.go:22` 의 `detachKeyBinding = "Ctrl-b d"` 는 tmux 의 키다. 그 자리의 주석이 스스로 밝히고 있다 — *"백엔드가 아직 하나뿐이어서다. 두 번째 백엔드가 생기면 그때 세션 계층이 답하도록 옮긴다."* 두 번째 백엔드가 생기는 것이 이 문서다.

**(4) 화면의 주인이 우리가 아니다**

`kyu attach` 는 자기 표준 입출력을 tmux 에게 넘긴다(`internal/session/tmux.go:83`). 즉 사용자가 세션에서 보는 화면과 키 입력은 전부 tmux 의 것이고, 이 도구는 그 사이에 아무것도 놓을 수 없다. 지금은 그것으로 충분하지만, 세션 화면 위에 이 도구의 것을 하나라도 얹으려면(진입 배너, 상태 줄, 세션 간 이동) tmux 설정을 프로그램으로 만지는 길밖에 없고, 그 길은 사용자의 `~/.tmux.conf` 와 충돌한다.

**(5) 사용자는 tmux 를 몰라도 된다고 했지만, 키 하나는 알아야 한다**

선행 문서 8.2 의 "도구가 감춘다"는 완전하지 않다. `Ctrl-b d` 는 감춰지지 않고, 사용자가 `~/.tmux.conf` 에서 prefix 를 바꿔 두었다면 이 도구의 안내가 틀린 키를 알려준다. 남의 프로그램을 감싸는 한 그 프로그램의 설정까지 감쌀 수는 없다.

### 1.3 무엇을 잃게 되는가

선행 문서 8.2 의 표를 그대로 되읽는다. 오른쪽 열이 전부 돌아온다.

| tmux 가 대신 해주던 것 | 직접 구현했을 때의 비용 | 이 문서에서 |
|---|---|---|
| PTY 관리 | pty 할당, 입출력 펌프 | 5.3 · 감독이 쥔다 |
| 리사이즈 대응 | SIGWINCH 처리 | 5.6 · RESIZE 프레임 + 클라이언트 SIGWINCH 핸들러 |
| 스크롤백 | 버퍼 관리 | 5.7 · **가장 큰 위험** |
| detach / 재접속 | 데몬 프로세스 설계 | 5.3 · 5.6 |
| 도구와 독립적인 세션 생존 | 프로세스 분리 | 5.3 · setsid |

**이 표를 지우려고 tmux 를 썼는데 이제 이 표를 채우겠다는 것이다.** 그러므로 이 작업의 성패는 "되는가"가 아니라 **"tmux 만큼 되는가"** 에 달려 있다. 3 단계를 동등성 테스트로 두고, 4 단계(기본값 전환)에 실측 선행 조건을 거는 것이 그 때문이다(9절).

---

## 2. 핵심 문제 — 누군가는 PTY 마스터를 쥐고 있어야 한다

세션이 살아있다는 것은 그 안의 프로그램이 계속 돈다는 뜻이다. 대화형 프로그램이 계속 돌려면 자기 표준 입출력이 터미널이어야 하고(그래야 화면을 그리고 키를 읽는다), 그 터미널은 PTY 쌍의 슬레이브다. 슬레이브가 살아있으려면 **마스터를 연 채로 쥐고 있는 프로세스가 있어야 한다.** 마스터를 쥔 마지막 프로세스가 죽으면 슬레이브 쪽에는 EOF 와 SIGHUP 이 가고 세션 안의 프로그램은 끝난다.

터미널 창에서 `kyu attach` 를 실행한 프로세스가 마스터를 쥐면, 그 창을 닫는 순간 세션도 죽는다. 그러면 detach 라는 개념이 성립하지 않는다.

**그러므로 "붙었다 떼었다 할 수 있는 세션"은 마스터를 쥔 채 사용자의 터미널과 무관하게 사는 프로세스를 반드시 요구한다.** tmux 서버가 하던 일이 정확히 이것이고, 걷어낸 자리에 같은 성질의 것이 들어가지 않으면 세션 생존 자체가 사라진다.

이 결론은 우회 가능한 종류가 아니다. 3절의 대안들은 전부 "그 프로세스를 무엇으로 둘 것인가"에 대한 서로 다른 답이지, "그 프로세스 없이 가능한가"에 대한 답이 아니다.

**확정된 답: 세션 하나에 그런 프로세스 하나.** 이것을 **감독**(supervisor)이라 부른다. 감독은 자기 세션의 PTY 마스터와 스크롤백을 쥐고, 자기 소켓 하나를 듣고, 자기 세션이 끝나면 함께 끝난다.

---

## 3. 검토한 대안과 기각 사유

이 절은 **다른 세션에서 "이거 그냥 X 쓰면 되는 거 아냐?" 를 다시 묻지 않기 위한 기록**이다(선행 문서 3절과 같은 목적).

### 3.1 중앙 데몬 하나 (기각 — 2026-08-31 사용자 결정)

사용자당 데몬 하나가 모든 세션의 PTY 마스터와 스크롤백을 쥐고, 소켓 하나(`.../daemon.sock`)로 모든 요청을 받는 안. tmux 서버와 같은 배치다. **이 문서의 초안이 채택했던 안이고, 검토 끝에 기각되었다.**

**기각 사유 (1) — 크래시 반경**

데몬 하나가 죽으면 그 위의 세션이 **전부** 죽는다. 감독을 아무리 단순하게 유지해도(원칙 9) 반경 자체를 줄이지는 못한다. 세션별 감독은 반경을 하나로 만든다 — 워크디렉토리 하나에 세션이 넷이면, 잃는 것이 넷에서 하나가 된다.

**기각 사유 (2) — 기동 절차가 통째로 사라진다**

중앙 데몬은 "없으면 띄운다"를 클라이언트가 해야 하고, 그것이 다음을 요구했다.

```
연결 시도 → 실패 → 전역 잠금 파일을 flock → 다시 연결 시도(기다리는 동안 남이 띄웠을 수 있다)
→ 죽은 소켓이면 지운다 → setsid 로 띄운다 → 소켓이 받을 때까지 마감 시각을 두고 기다린다 → 잠금 해제
```

**모든 `create` 가 통과하는 전역 직렬화 지점이 하나 생긴다.** 세션별 감독에서는 감독이 자기 이름의 잠금을 잡는 것이 곧 배타성이고, 그 잠금이 **세션 이름별**이다(5.3). 서로 다른 세션의 `create` 는 서로를 기다리지 않고, 같은 이름의 두 번째 `create` 도 기다리지 않는다 — 잠금을 못 잡는 것이 곧 답이라 그 자리에서 `ErrSessionExists` 로 끝난다.

**기각 사유 (3) — 자동 종료 정책이라는 숫자가 사라진다**

중앙 데몬은 "마지막 세션이 끝나면 언제 죽는가"를 정해야 했다. 즉시 종료하면 마침 연결하려던 클라이언트와 경합하고, 유예를 두면 유예 시간이라는 숫자가 하나 생긴다. 감독은 자기 세션이 끝나면 죽는다 — **정할 것이 없다.**

**기각 사유 (4) — 프로토콜 불일치가 전부-아니면-전무였다**

`kyu` 를 새 판으로 갈아도 돌던 데몬은 옛 판이다. 중앙 데몬이면 그 순간 **모든 세션**이 새 클라이언트와 말이 안 통한다. 감독은 세션마다 자기를 띄운 판이 서빙하므로, 옛 세션은 옛 감독이 계속 맡고 새로 만드는 세션부터 새 판이다. 열화가 전면적이지 않고 점진적이다.

**중앙 데몬의 이점이었던 것과, 그것이 상쇄되는 근거**

| 이점 | 상쇄 |
|---|---|
| `List()` 이 왕복 한 번 | 세션별이면 소켓 디렉토리를 훑고 N 개에 붙어본다. N 은 워크디렉토리의 레포 수라 한 자릿수이고, 유닉스 소켓 연결은 커널 안에서 끝난다. **더 결정적으로, `kyu list` 의 실제 비용은 세션 조회가 아니라 레포마다 도는 git 상태 판정(`git status --porcelain`·`git rev-list`)이다** — 그쪽은 프로세스 실행이라 자릿수가 다르다. 소켓 N 개는 측정되지 않는다 |
| 죽은 소켓을 치우는 곳이 한 군데 | 치우는 **지점**이 늘어나는 것이 아니라 규칙 하나가 N 개 파일에 적용될 뿐이다. 규칙은 5.4 한 곳에 있다 |
| tmux 와 같은 심상(서버 하나 + 세션 여럿)이라 설명이 싸다 | 문서로 갚는다 — 이 문서가 그것이다. 그리고 실은 **심상이 더 단순해진다**: "세션 하나 = 프로세스 하나"에는 서버라는 중간 개념이 없다. `ps` 에 세션 수만큼 감독이 보이는 것이 "데몬 하나가 넷을 품고 있다"보다 사실에 가깝다 |

기각되었지만 이 안의 나머지 설계는 대부분 살아남았다 — 프로토콜의 모양(5.5·5.6), 스크롤백(5.7), 인터페이스 확장(5.8), 윈도우 전망(8절)은 감독이 하나든 여럿이든 같다.

### 3.2 screen · dtach · abduco 위임

tmux 대신 다른 세션 프로그램에 위임하는 안.

**기각 사유**: **1.2 의 문제가 하나도 풀리지 않는다.** 설치 요구는 그대로이고(오히려 tmux 보다 덜 깔려 있다), 윈도우 네이티브는 여전히 막히고, detach 키는 여전히 남의 것이다. 바꾸는 값에 비해 얻는 것이 없다.

세부적으로도 손해다. `dtach`·`abduco` 는 가볍지만 스크롤백을 아예 갖지 않아서(붙는 순간부터의 출력만 본다) 재접속하면 화면이 비어 있다. 지금 tmux 로 되는 것이 안 되게 된다. `screen` 은 스크롤백이 있지만 tmux 보다 배포판 커버리지가 낫지 않고 유지보수도 느리다.

다만 **`dtach`·`abduco` 의 배치는 이 문서가 채택한 것과 같다** — 세션마다 프로세스 하나에 소켓 하나. 그 배치가 오래 굴러온 전례라는 점은 확정 안의 근거로 세어둘 만하다.

### 3.3 GUI 가 세션을 보유

데스크톱 앱이 직접 PTY 를 열어 세션을 소유하고, CLI 는 앱에 붙는 안. 앱은 이미 pty4j 로 PTY 를 연다(`desktop/.../terminal/pty/PtyKyuSessionTerminalAttacher.kt:81`).

**기각 사유**: **선행 문서 원칙 5("도구가 죽어도 작업이 안 끊긴다") 정면 위반이다.** 앱을 끄면 세션이 죽는다. README 임베디드 터미널 절이 "터미널 닫기·다른 카드로 이동·앱 종료 — 전부 kill 이 아니라 detach 다"라고 약속한 것이 그 자리에서 거짓이 된다.

더해서 CLI 만 쓰는 사람은 GUI 를 띄워야 세션을 가질 수 있게 된다. 표시 계층이 "교체 가능한 껍데기"라는 3층 구조(선행 문서 5.1)가 뒤집혀서, 껍데기가 핵심 자산을 소유하게 된다.

**원칙 5 의 "도구"는 CLI 와 GUI 를 가리키지 세션 계층을 가리키지 않는다.** 세션 계층은 세션이 사는 곳이므로 세션과 생사를 같이하는 것이 정의상 맞다 — 감독이 죽으면 그 세션이 죽는 것은 결함이 아니라 정의다(5.9).

### 3.4 systemd user service / launchd agent

세션 프로세스를 직접 기동하지 않고 OS 의 사용자 서비스 관리자에 맡기는 안.

**기각 사유**: **플랫폼별로 완전히 다른 것을 두 번 만들게 된다.** systemd 는 리눅스 전용이고 macOS 는 launchd 다. 대상 플랫폼이 macOS 와 리눅스 둘인데 그 둘이 공유하는 것이 없으므로, 선행 문서 원칙 4("플랫폼 의존을 한 겹에 가둔다")를 지키려고 하는 일이 오히려 플랫폼 의존을 한 겹 더 만든다.

리눅스 쪽만 봐도 비용이 있다. 사용자 서비스는 로그인 세션에 묶여서, SSH 로 붙어 세션을 띄우고 나갔을 때 `loginctl enable-linger` 없이는 로그아웃과 함께 정리된다. 그것을 도구가 사용자 대신 켜는 것은 실행 권한 범위를 넘는다(tmux 자동 설치를 하지 않는 것과 같은 이유 — `internal/session/tmux.go:12` 의 주석).

세션마다 유닛을 동적으로 만들어야 한다는 점도 더해진다. 세션이 워크디렉토리마다 뜨고 사라지는 것이라 유닛 파일의 수명이 세션의 수명과 같아지는데, 그것은 서비스 관리자가 잘하는 일이 아니다.

**setsid 로 직접 떼어내는 것이 두 플랫폼에서 같은 코드다.** 그것이 5.3 의 선택이다.

### 3.5 SCM_RIGHTS 로 PTY 마스터 fd 를 클라이언트에 넘기기

감독은 PTY 를 열어두기만 하고, attach 할 때 유닉스 소켓의 fd 전달로 마스터 파일 디스크립터를 클라이언트에게 건네는 안. 그러면 데이터가 감독을 경유하지 않아 프로토콜의 attach 스트림(5.6)이 통째로 사라진다.

**기각 사유**: **스크롤백의 주인이 없어진다.** 마스터에서 읽어간 바이트는 사라지므로, 클라이언트가 읽은 것을 감독은 보지 못하고 링 버퍼에 쌓을 수 없다. 재접속하면 화면이 빈다(3.2 의 dtach 와 같은 결말).

두 번째로 **두 클라이언트가 동시에 붙을 수 없다.** 같은 마스터를 두 프로세스가 읽으면 바이트가 무작위로 갈린다. 지금은 한 번에 하나만 붙지만(README 임베디드 터미널 절), 그 제약이 정책이 아니라 물리가 되어 되돌릴 수 없게 된다.

fd 전달 자체는 흥미로운 수단이지만 이 문제에 맞지 않는다.

### 3.6 nohup · setsid 로 백그라운드 실행 + 출력을 파일로

세션을 그냥 백그라운드 프로세스로 띄우고 출력을 로그 파일에 적는 안.

**기각 사유**: **대화가 안 된다.** 세션 안에서 도는 것은 사람과 주고받는 대화형 프로그램이라 표준 입력이 필요하고, 파일로 리다이렉트된 출력에는 터미널이 없어 그 프로그램은 화면 그리기를 포기한다(`isatty` 가 거짓이 되는 순간 색도 커서 이동도 멈춘다). 붙었다 떼는 것도 불가능하다 — 붙을 대상이 없다.

이 안은 "세션"이 아니라 "배치 실행"이다. 다른 문제를 푼다.

### 3.7 tmux 를 동봉

데스크톱 패키지가 엔진 `kyu` 를 동봉하듯 tmux 바이너리도 함께 넣는 안(README "앱은 엔진을 들고 온다").

**기각 사유**: **윈도우가 안 풀린다.** 1.2 의 (2)가 이 작업의 큰 이유 중 하나인데, tmux 를 동봉해도 윈도우 네이티브에는 넣을 것이 없다.

부수적으로도 무겁다. tmux 는 C 로 짜여 libevent 와 ncurses 에 링크되므로, 플랫폼·아키텍처마다 정적 링크 빌드를 만들고 그 빌드를 CI 에서 유지해야 한다. Go 단일 바이너리로 얻은 배포 단순성(선행 문서 8.1)을 도로 내놓는 셈이다. CLI 만 쓰는 사람(`go install` 로 받는 경로)에게는 동봉할 자리조차 없다.

---

## 4. 설계 원칙

선행 문서 4절의 여섯 원칙을 그대로 상속한다. 이 문서가 더하는 것은 넷이다.

7. **tmux 를 지우지 않는다, 강등한다** — 검증된 폴백을 손에 남긴다. 감독 백엔드가 어떤 환경에서 무너져도 사용자는 환경변수 하나로 돌아갈 자리가 있다. 지우는 것은 두 백엔드가 오래 같이 산 뒤에 따로 판단한다.
8. **의미론을 새로 정하지 않는다** — 감독 백엔드는 tmux 백엔드가 하던 것과 **같게** 동작해야 한다. 더 낫게 만들 여지가 보여도 이번에는 하지 않는다. 동등성이 증명되지 않은 백엔드는 기본값이 될 수 없고, "같음"은 다름보다 증명하기 쉽다.
9. **감독은 세션 안을 모른다** — 원칙 3("세션 내부에 개입하지 않는다")이 감독에 오면 이 형태가 된다. 감독은 바이트를 나르고 화면 크기를 전하고 자기 자식을 지켜볼 뿐, 출력을 해석해 무언가를 판단하지 않는다. 5.7 의 스크롤백이 **바이트 링**이지 화면 모델이 아닌 것이 이 원칙의 직접적인 결과다.
10. **감독은 자기 세션만 안다** — 감독은 다른 세션의 존재를 모르고, 알 필요도 없다. 세션 목록이라는 사실은 어느 프로세스도 소유하지 않고 파일 시스템이 갖는다(5.4). 조율 계층이 워크디렉토리의 레포 목록을 설정 파일이 아니라 스캔으로 얻는 것과 같은 자세다(선행 문서 5.3) — **손으로 유지하는 목록은 한 번 잊는 순간 조용히 어긋난다.**

---

## 5. 아키텍처

### 5.1 배치

3층 구조(선행 문서 5.1)는 그대로다. 세션 계층 안이 갈라질 뿐이다.

```
┌─────────────────────────────────────────────────────────┐
│ [표시 계층]  CLI · 데스크톱 앱                            │  ← 안 바뀐다 (7절)
├─────────────────────────────────────────────────────────┤
│ [조율 계층]  WorkDir 스캔 · plan 파싱 · 상태 추론          │  ← 안 바뀐다
├─────────────────────────────────────────────────────────┤
│ [세션 계층]  SessionBackend                              │
│    ├── TmuxBackend        (기존 — 폴백으로 남는다)        │
│    └── SupervisorBackend  (신규 — 프로토콜 클라이언트)    │
└─────────────────────────────────────────────────────────┘
        │                    │                    │
        ▼ 소켓               ▼ 소켓               ▼ 소켓
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│ 감독           │   │ 감독           │   │ 감독           │
│ kyu-fx-main   │   │ kyu-fx-proj-a │   │ kyu-fx-proj-b │
│ PTY · 스크롤백 │   │ PTY · 스크롤백 │   │ PTY · 스크롤백 │
│   └ claude    │   │   └ claude    │   │   └ claude    │
└───────────────┘   └───────────────┘   └───────────────┘
```

**감독은 `kyu` 바이너리 자신이다.** 별도 실행 파일을 내지 않는다 — 숨은 하위 명령(`kyu __supervise`)으로 자기를 다시 실행한다. 릴리스 자산이 늘지 않고, 앱이 동봉하는 엔진 하나에 감독이 이미 들어 있고, 클라이언트와 감독의 프로토콜 코드가 같은 패키지에 있어 어긋날 자리가 없다.

숨은 하위 명령은 `usage` 에 싣지 않는다. 사용자가 부를 명령이 아니고, 부를 필요도 없다 — 기동은 5.3 이 알아서 한다.

**중앙 조정자가 없다.** 감독들은 서로를 모르고 서로에게 말하지 않는다(원칙 10). 이것은 선행 문서 5.5 가 세션 간 직접 통신을 만들지 않은 것과 같은 성질이고, 같은 이점을 준다 — **세션이 언제 뜨고 죽어도 무방하다.**

### 5.2 런타임 경로

```
$XDG_RUNTIME_DIR/kyu/          없으면 → $HOME/.local/state/kyu/
    sessions/
        kyu-featureX-main.sock       감독의 소켓 — 존재와 응답이 곧 세션의 생존
        kyu-featureX-main.lock       감독이 사는 내내 쥐는 이름 잠금 — 이 이름의 임자가 누구인지 (5.3)
        kyu-featureX-main.log        그 세션의 생명주기 기록
        kyu-featureX-proj-a.sock
        …
```

**세션 이름이 곧 파일 이름이다.** 이름은 이미 `kyu-<workdir>-<repo>` 로 만들어지고 `.`·`:` 가 치환되어 있다(`internal/session/name.go:30`·`38`). 워크디렉토리 이름과 레포 이름은 둘 다 디렉토리 이름에서 오므로 `/` 가 들어갈 수 없고, 따라서 이름이 경로를 벗어날 방법이 없다. **그래도 소켓을 만들기 전에 이름에 경로 구분자와 `..` 가 없음을 확인한다** — 이름을 만드는 곳과 파일로 쓰는 곳이 멀어지면 그 사이에 규칙이 바뀔 수 있고, 확인 한 줄이 그 어긋남을 기동 시점에 끊는다.

**`$XDG_RUNTIME_DIR` 을 먼저 보는 이유**: 리눅스에서 이 디렉토리는 정확히 이 용도로 존재한다 — 사용자 소유 0700, tmpfs, 로그인 세션이 끝나면 정리된다. 재부팅 후 남은 소켓 파일을 우리가 치울 일이 없다.

**폴백이 예외가 아니라 macOS 의 정상 경로다.** macOS 에는 `XDG_RUNTIME_DIR` 이 없다. 그래서 폴백 경로는 "드문 경우"가 아니라 두 대상 플랫폼 중 하나의 기본값이고, 그 경로에서 벌어지는 일(재부팅 후에도 소켓 파일이 남는다)이 오히려 흔한 쪽이다. **죽은 소켓 처리(5.3·5.4)를 예외 취급하면 안 되는 것이 이 때문이다.**

`$TMPDIR` 을 쓰지 않는다. macOS 의 `$TMPDIR` 은 사용자별로 갈려 있어(`/var/folders/…/T/`) 그 점은 맞지만, 시스템 정리 작업이 오래된 파일을 지운다. 잠금 파일과 로그가 함께 사는 자리로는 맞지 않는다.

**`KYU_RUNTIME_DIR` 로 이 경로를 덮어쓸 수 있게 한다.** 테스트가 사용자의 세션을 건드리지 않고 자기 세션을 띄우기 위한 것이다 — `internal/session/tmux_test.go:19` 의 `TMUX_TMPDIR` 격리와 정확히 같은 자리다. 그 테스트가 격리에 실패하면 사용자의 작업 중인 세션을 죽인다는 사실이 주석에 적혀 있고, 감독 쪽도 사정이 같다.

디렉토리는 0700, 소켓 파일은 0600 으로 만든다. 유닉스 소켓의 접근 제어는 파일 권한으로 이뤄지고, 이 소켓에 연결할 수 있다는 것은 **그 세션에 키를 보낼 수 있다는 뜻**이다. 같은 머신의 다른 사용자에게 열어줄 이유가 없다.

**경로가 길어졌다는 것을 적어둔다.** 소켓 경로는 `sun_path` 에 들어가고 그 크기가 macOS 104 · 리눅스 108 바이트인데, 중앙 데몬의 `daemon.sock` 과 달리 이제 경로에 세션 이름이 들어간다. `$HOME` 이 깊고 워크디렉토리·레포 이름이 길면 한도에 닿을 수 있다. 10절 6번.

### 5.3 감독의 삶 = 세션의 삶

**감독은 자기 세션이 뜰 때 태어나고 자기 세션이 끝날 때 죽는다.** 이 한 줄이 중앙 데몬 안에서 생명주기·자동 종료 정책·기동 경쟁으로 갈라져 있던 것을 대체한다.

**기동 — `Create` 가 감독을 띄운다**

`SupervisorBackend.Create(name, cwd, cmd)` 는 프로토콜 요청을 보내지 않는다. 받을 상대가 아직 없기 때문이다. 대신 자기를 다시 실행한다.

```
kyu __supervise --name <세션 이름> --cwd <절대 경로> -- <명령...>
```

`setsid` 로 떼어내고(부모의 터미널·프로세스 그룹에서 분리), 표준 입출력은 그 세션의 로그 파일로 돌린다. 환경은 부모의 것을 그대로 물려준다 — `SessionBackend.Create` 의 시그니처에 env 인자가 없고, 환경변수를 얹어야 하는 호출부는 이미 명령을 `env` 로 감싸는 방식을 쓴다(`internal/cli/start.go` 의 `envCommandName`).

**감독이 뜬 뒤의 순서가 중요하다.**

```
1. <이름>.lock 을 flock 으로 잡는다   ← 이것이 배타성 판정이다. 놓지 않고 죽을 때까지 쥔다
2. 소켓을 바인딩한다                   ← 남아 있던 죽은 소켓은 이 잠금 아래에서 치운다
3. PTY 쌍을 열고 자식을 실행한다
4. 준비 신호를 부모에게 보낸다
5. 서빙을 시작한다
```

**1 번이 먼저인 이유**: 잠금을 잡는 것이 곧 "이 이름의 세션은 내 것"이라는 선언이다. 실패하면 자식을 띄우기 전에 끝내야 한다 — 순서가 뒤집히면 중복 생성이 `claude` 를 하나 띄웠다 죽이는 일이 된다.

**`ErrSessionExists` 는 이름 잠금의 배타성에서 온다.** `flock` 은 한 번에 한 프로세스만 쥔다. 잡히면 그 이름은 내 것이고, `EWOULDBLOCK` 이면 임자가 따로 있다. 그 답이 곧 "같은 이름의 세션이 이미 있다"이므로 중앙 데몬이 하던 별도 조회(`IsAlive` 를 먼저 물어보고 판정)가 필요 없다. tmux 백엔드가 `has-session` 을 미리 부르는 것(`internal/session/tmux.go:48`)에 대응하는 자리인데, 여기서는 **판정과 획득이 같은 한 번의 연산이라 그 사이에 낄 틈이 없다.** 기다리지 않는다(`LOCK_NB`) — 기다리면 중복 생성이 앞 세션이 끝날 때까지 멈춰 서는데, 그 자리는 곧바로 답해야 하는 자리다.

**임자를 소켓이 아니라 잠금에 둔 이유** (2026-08-31 · 1 단계 구현이 찾아낸 것, PR #36 ④(2)):

초안은 소켓 바인딩 자체를 배타성으로 삼았다. 파일이 이미 있으면 바인딩이 실패하고, 그것이 살아있는 감독 때문인지 죽은 감독이 남긴 파일 때문인지는 **붙어봐서** 갈랐다 — 연결되면 살아있고, `ECONNREFUSED` 면 죽은 소켓이라 지우고 다시 바인딩한다. 이 기전에 좁은 구멍이 있다.

```
A: bind() 성공 ─────────── listen() ──→ 서빙
B:              connect() → ECONNREFUSED → "죽은 소켓이네" → A 의 소켓을 지우고 자기 것을 바인딩
```

**bind 는 마쳤지만 아직 듣지 않는 소켓도 `ECONNREFUSED` 로 답한다.** 즉 "아직 안 뜬 감독"과 "죽은 감독"이 밖에서 보면 똑같다. 겹치기는 매우 어렵지만(그 사이가 시스템 콜 한둘이다) 없는 것은 아니고, 걸리면 같은 이름의 감독이 둘이 되어 세션 안 프로그램도 둘이 뜬다. 목록은 소켓 하나만 보므로 그 사실을 드러내지도 못한다.

**`flock` 은 그 구멍을 만들지 않는다.** 잠금은 커널이 원자적으로 주고, 감독이 그것을 수명 내내 쥔다. 그러면 "잠금이 잡히지 않았다 = 살아있는 임자가 있다"가 시점과 무관하게 참이고, **기동 도중이라는 중간 상태가 밖에서 보이지 않는다.** 죽은 소켓 판정·삭제·재바인딩 전 구간이 그 잠금 안에 들어가므로 남의 소켓을 지울 수도 없다. 잠금은 열린 파일과 함께 살고 프로세스가 죽으면 커널이 푸는 것이라, **감독을 `kill -9` 해도 이름이 곧바로 회수되는 성질이 따로 만들지 않아도 따라온다.**

대가 하나를 적어둔다: 잠금을 못 잡은 쪽은 임자의 소켓이 뜨기 **직전에** "이미 있다"로 답할 수 있다. 바인딩이 배타성이던 때에는 그 답이 소켓의 존재를 뜻했다. 그 창은 잠금과 바인딩 사이의 시스템 콜 한둘이고, 메우려면 "소켓이 받을 때까지 기다린다"는 숫자를 하나 들여야 한다 — 준비 파이프로 없앤 바로 그 숫자다(아래). 창을 남기고 숫자를 들이지 않는 쪽을 골랐다.

**소켓은 여전히 지운다.** 잠금을 쥐고 있으면 이 이름의 감독은 나뿐이므로, 그 자리에 남아 있는 소켓 파일은 죽은 감독이 남긴 것이다. 지우고 새로 바인딩한다. 다만 **잠금을 쥐지 않는 옛 판 감독**이 아직 그 이름으로 돌고 있을 수 있어(감독은 세션마다 자기를 띄운 판이 서빙한다 — 3.1) 지우기 전에 한 번 붙어보고, 답하면 물러난다. 새 판끼리는 잠금에서 이미 갈렸으니 이 확인에 걸리는 것은 옛 판뿐이다.

**이 잠금의 범위가 중앙 데몬과 다르다는 것이 핵심이다.**

| | 중앙 데몬 | 세션별 감독 |
|---|---|---|
| 잠금이 드는 때 | 데몬이 없는 모든 `create` | 모든 `create` — 다만 잡는 데 드는 것이 `flock` 한 번이다 |
| 경합 범위 | 전역 — 모든 `create` 가 한 잠금을 지난다 | 세션 이름별 — 다른 세션의 `create` 는 서로를 기다리지 않는다 |
| 잠금을 못 잡으면 | 기다린다 (남이 데몬을 띄우는 중일 수 있다) | 기다리지 않는다 — 그것이 곧 `ErrSessionExists` 다 |
| 잠금 뒤에 따라붙는 것 | 재확인 · 죽은 소켓 정리 · spawn · 소켓이 받을 때까지 폴링 | 죽은 소켓 정리 · 바인딩 |

**잠금 파일은 지우지 않는다.** 잠금을 쥔 채로 파일을 지우면 다음 사람이 같은 경로에 새로 만든 다른 inode 를 잠그게 되어, 둘이 서로 다른 잠금을 쥐고 같은 이름을 만진다. 0 바이트 파일 하나가 남는 값으로 그 어긋남을 없앤다.

**준비 신호 — 폴링하지 않는다**

부모는 감독이 떴는지를 어떻게 아는가. `setsid` 로 떼어냈으므로 종료 코드를 볼 수 없다.

`Create` 는 파이프를 하나 만들어 쓰기 끝을 감독에게 물려준다. 감독은 위 순서의 4 번에서 그 파이프에 한 줄을 쓰고 닫는다.

```
{"v":1,"ok":true}
{"v":1,"ok":false,"code":"exists"}
{"v":1,"ok":false,"code":"nocwd","msg":"작업 디렉토리가 없습니다: /abs/path"}
```

부모는 파이프를 EOF 까지 읽는다. 한 줄이 오면 그것이 답이고, 아무것도 없이 EOF 면 감독이 답하기 전에 죽은 것이라 그 세션의 로그 파일을 가리키는 오류를 낸다.

**소켓 폴링 대신 파이프를 고른 이유**

- 폴링은 **얼마나 기다릴지**를 숫자로 정해야 한다. 파이프는 답이 오면 그 자리에서 끝난다.
- 폴링은 **왜 안 떴는지**를 모른다. "소켓이 아직 안 받는다"만 알 수 있어서, 이름 충돌인지 cwd 가 없어서인지 명령을 못 찾아서인지 구별하지 못한다. `ErrSessionExists` 를 정확히 돌려주려면 이 구별이 필요하다.
- 감독이 걸려서 죽지도 답하지도 않는 경우를 위해 마감 시각은 그래도 둔다. 다만 그것은 최후의 안전장치이지 정상 경로가 아니다.

**종료**

- **자연사**: 세션 안 프로그램이 끝나면 감독은 PTY 마스터에서 EOF 를 보고, 자식을 거두고(좀비를 남기지 않는다), 붙어 있는 클라이언트에 EXIT 프레임을 보내고, **자기 소켓을 지우고 종료한다.**
- **`Kill`**: 감독이 자식의 **프로세스 그룹 전체**에 `SIGHUP` 을 보내고, 유예 뒤에도 남아 있으면 `SIGKILL` 한다. 그룹에 보내는 것이 중요하다 — 세션 안의 프로그램이 자식을 띄워두었으면 리더만 죽여서는 그 자식이 PTY 를 쥔 채 남는다. 자식이 끝나면 자연사 경로로 합류한다.
- **없는 세션 `Kill`** 은 `nil` 이다(멱등 — `internal/session/tmux.go:98` 과 같은 계약). 연결이 `ENOENT`·`ECONNREFUSED` 로 끝나는 것이 그 답이다.

정상 종료 경로가 소켓을 지우므로, **죽은 소켓은 비정상 종료(SIGKILL·크래시·재부팅)에서만 남는다.**

**세션의 환경**: 감독은 자식에게 물려줄 환경에 둘을 더한다.

| 환경변수 | 값 | 왜 |
|---|---|---|
| `KYU_SESSION` | 세션 이름 | 중첩 판정의 표식 — 아래 |
| `TERM` | 물려받은 값, 없으면 `xterm-256color` | 없으면 세션 안 프로그램이 화면 그리기를 포기한다. 감독은 떼어진 프로세스라 `TERM` 이 비어 올 수 있다(GUI 런처에서 앱을 띄운 경우) |

**중첩 판정**: `ErrNestedSession`(`internal/session/backend.go:21`) 은 감독 백엔드에서 `KYU_SESSION` 의 존재로 판정한다. tmux 가 `TMUX` 를 심는 것과 같은 기전이다(`internal/session/tmux.go:20`) — 감독이 자식을 띄울 때 심으므로 그 안에서 실행되는 모든 프로세스가 물려받고, 그 안에서 `kyu attach` 를 부르면 표식이 보인다.

tmux 의 `TMUX` 가 소켓 경로인 것과 달리 값을 **세션 이름**으로 둔다. 그러면 안내가 "이 터미널은 이미 세션 안입니다" 에서 "이 터미널은 이미 `kyu-featureX-proj-a` 세션 안입니다" 로 나아간다. 사용자가 어느 세션인지 모르는 채로 여기 도착하는 경우가 실제로 있다.

**중첩은 백엔드 안에서만 성립한다.** tmux 세션 안에서 감독 백엔드로 `kyu attach` 를 부르는 것은 중첩이 아니다 — 서로 다른 두 계층이라 겹치지 않는다. 판정을 백엔드에 두면 이것이 저절로 맞는다. 표시 계층이 판정했다면 두 표식을 다 알아야 하고 그 조합을 손으로 관리하게 된다.

**로그 — 생명주기 사건만 적는다.** 감독 기동, 세션 생성·종료(종료 코드 포함), attach·detach, 오류. **입출력 바이트는 절대 적지 않는다.** 세션의 내용은 사용자의 작업이고 도구의 관심사가 아니며(원칙 9), 적기 시작하면 로그가 무한히 자라 회전 장치가 필요해진다. 사건만 적으면 세션 하나의 일생이 수십 줄이라 그 장치가 아예 필요 없다. **세션마다 파일이 갈리므로 어느 세션의 일인지 찾는 일도 없다** — 중앙 데몬이면 한 파일에 섞였다.

### 5.4 `List` — 소켓 디렉토리 스캔

**세션 목록이라는 사실을 어느 프로세스도 소유하지 않는다. 파일 시스템이 갖는다**(원칙 10).

```
1. sessions/ 에서 *.sock 을 읽는다
2. 파일 이름에서 세션 이름을 되얻는다 (확장자를 뗀다)
3. 각각에 연결해 alive 를 묻는다
4. 답한 것만 목록에 넣는다
```

**디렉토리가 없으면 빈 목록이다. 오류가 아니다.** 세션을 한 번도 만들지 않은 상태의 정상적인 표현이다. tmux 백엔드가 이미 같은 규칙으로 동작한다(`internal/session/tmux.go:116` 의 "tmux 서버는 마지막 세션이 죽으면 함께 종료된다 — 즉 세션 0개의 정상적인 표현이 서버 없음이다").

**`List` 는 죽은 소켓을 지우지 않는다.** 읽기 연산이 파일을 지우면 안 된다. 이유가 셋이다.

1. GUI 대시보드가 3 초마다 `kyu list` 를 부른다(`desktop/.../terminal/SessionEntryPlan.kt:31` 의 주석). 조회가 파일을 지우는 것은 놀랄 일이다.
2. 막 뜨는 중인 감독의 소켓과 경쟁한다 — 바인딩은 됐는데 아직 서빙 전인 짧은 구간이 있다.
3. 치울 이유가 있는 시점이 따로 있다. 그 이름으로 다시 `Create` 할 때이고, 그때는 잠금 안에서 안전하게 할 수 있다(5.3).

죽은 소켓은 목록에서 빠질 뿐 그 자리에 남는다. **`$XDG_RUNTIME_DIR` 경로에서는 재부팅이 치우고, macOS 폴백 경로에서는 남는다** — 그 잔여를 어떻게 할지는 10절 5번.

**`--all` 접두사 필터는 그대로 성립한다.** `WorkDirSessionPrefix`(`internal/session/name.go:30`) 로 거르는 조율 계층 코드가 손대지 않고 돈다. `List` 가 돌려주는 것은 여전히 세션 이름 문자열의 목록이다.

**`IsAlive` 는 그 세션의 소켓에만 붙는다.** 디렉토리를 훑지 않는다 — 이름을 이미 알고 있으므로 경로를 곧바로 만들 수 있다. 연결이 `ENOENT`·`ECONNREFUSED` 로 끝나면 `false, nil` 이다. 감독이 살아있다는 것과 세션이 살아있다는 것이 같은 사실이므로(5.3), 연결이 되고 답이 오면 그것으로 판정이 끝난다.

### 5.5 프로토콜 — 제어 연결

**newline-delimited JSON.** 한 줄에 요청 하나, 한 줄에 응답 하나. 연결은 짧게 산다 — 요청 하나를 보내고 응답을 받고 닫는다.

**연산이 둘뿐이다.** `create` 는 프로토콜이 아니라 프로세스 기동이고(5.3), `list` 는 디렉토리 스캔이다(5.4). 감독에게 물을 것은 자기 생존과 자기 종료뿐이다.

```
→ {"v":1,"op":"alive"}
← {"v":1,"ok":true,"alive":true}

→ {"v":1,"op":"kill"}
← {"v":1,"ok":true}

← {"v":1,"ok":false,"code":"badversion","msg":"이 세션은 kyu 0.9.0 이 만들었습니다"}
```

**세션 이름이 요청에 없다.** 소켓이 곧 세션이라 어느 세션인지 말할 필요가 없다. 중앙 데몬 안에서는 모든 요청이 `name` 을 실어야 했고, 그 이름이 등록되어 있는지 매번 확인해야 했다 — 그 전부가 사라진다.

**길이 프리픽스가 아니라 개행을 고른 이유**

- 제어 메시지는 전부 작은 구조체다. 큰 페이로드도, 바이너리도 없다. 길이 프리픽스가 푸는 문제("어디까지가 한 메시지인가")를 JSON 인코더가 이미 풀어준다 — Go 의 `json.Encoder` 는 값 하나마다 정확히 한 줄을 쓰고 문자열 안의 개행은 이스케이프하므로, 값의 경계와 줄의 경계가 어긋나지 않는다.
- 손으로 들여다볼 수 있다. `socat - UNIX-CONNECT:$XDG_RUNTIME_DIR/kyu/sessions/kyu-featureX-main.sock` 에 한 줄 붙여넣으면 그 감독이 답한다. 프로토콜을 디버깅하는 도구를 따로 만들 필요가 없고, 이것은 학습자가 코드를 읽고 따라갈 수 있어야 한다는 이 프로젝트의 성향과도 맞는다.
- 길이 프리픽스는 손으로 짠 프레이밍 코드를 하나 더 만든다. attach 연결에는 그것이 꼭 필요하지만(5.6), 필요하지 않은 곳까지 같은 것을 쓸 이유는 없다.

**`code` 는 안정된 문자열이고 `msg` 는 아니다.** 클라이언트는 `code` 로 센티널 에러를 되살린다 — `"exists"` → `ErrSessionExists`(`internal/session/backend.go:14`). `msg` 는 사람이 읽을 문장이라 언제든 다듬을 수 있어야 하고, 그것으로 분기하면 문구를 고칠 때 조용히 깨진다. tmux 백엔드가 stderr 문자열 매칭 대신 `has-session` 으로 미리 판정한 것과 같은 판단이다(`internal/session/tmux.go:48` 의 주석).

**`v` 는 프로토콜 버전이다.** 새 `kyu` 가 옛 감독을 만나는 일이 세션마다 따로 벌어진다 — 10절 4번.

### 5.6 프로토콜 — attach 연결

attach 는 **전용 연결**이다. 그 세션의 소켓에 새로 연결하고, 첫 프레임이 ATTACH 이면 그 연결은 그때부터 그 세션의 스트림이 된다.

```
[type:1][len:4 big-endian][payload:len]
```

| 방향 | type | 이름 | payload |
|---|---|---|---|
| 클라이언트 → 감독 | `0x01` | ATTACH | `{"v":1,"cols":120,"rows":40}` |
| 클라이언트 → 감독 | `0x02` | INPUT | 키 입력 원시 바이트 |
| 클라이언트 → 감독 | `0x03` | RESIZE | `cols:2` + `rows:2` (big-endian) |
| 감독 → 클라이언트 | `0x81` | OUTPUT | 세션 출력 원시 바이트 |
| 감독 → 클라이언트 | `0x82` | EXIT | `{"code":0}` — 세션 안 프로그램이 끝났다 |
| 감독 → 클라이언트 | `0x83` | ERROR | `{"code":"badversion","msg":"..."}` |

**ATTACH 에 세션 이름이 없다.** 소켓이 세션을 특정한다. `TERM` 도 없다 — 그것은 자식이 태어날 때 정해지는 값이라(5.3) 이미 뜬 자식에게 전할 방법이 없다.

**왜 attach 만 프레임을 쓰는가 (제어는 JSON 인데)**

- **입력과 리사이즈는 순서가 있다.** 같은 스트림에 섞여 순서대로 처리되어야 한다. 리사이즈를 다른 연결로 보내면 "화면이 커진 뒤에 도착해야 할 키 입력"과 "키 입력 뒤에 도착해야 할 리사이즈"를 구별할 방법이 없고, 어긋나면 세션 안 프로그램이 틀린 크기로 다시 그린다.
- **끝을 구별해야 한다.** 세션 안 프로그램이 정상 종료한 것(EXIT)과 감독이 죽은 것과 소켓이 깨진 것은 사용자에게 다른 말을 해야 하는 사건이다. 연결을 그냥 닫으면 셋이 전부 "연결이 끊겼다"로 뭉개진다.
- **원시 바이트를 JSON 에 담을 수 없다.** 담으려면 base64 인데, 키를 한 번 누를 때마다 JSON 값 하나를 인코딩·디코딩하고 출력은 4/3 로 부풀린다. 이 경로는 사람이 타이핑하는 지연이 직접 보이는 자리다.

**왜 다중화가 아니라 전용 연결인가**

한 연결에 제어와 attach 를 섞으면 제어 요청이 attach 의 뒤에 줄을 서게 된다. 붙어 있는 터미널이 출력을 늦게 읽고 있으면(사용자가 스크롤을 잡고 있거나 창이 가려져 있으면) 그 역압이 `alive` 조회까지 멈춰 세우고, 3 초마다 도는 `kyu list` 가 그 세션에서 걸린다. 연결을 나누면 역압이 그 attach 하나에만 머문다 — 있어야 할 자리다.

그리고 **연결 자체가 곧 붙어 있음이다.** detach 메시지가 필요 없다 — 클라이언트가 연결을 닫으면 그것이 detach 다. 클라이언트가 죽어도(터미널 창을 강제 종료해도) 커널이 연결을 닫아주므로 같은 경로로 처리된다. 상태를 따로 관리하지 않는다.

**중복 attach**: 이미 붙은 클라이언트가 있는 세션에 새 클라이언트가 붙으면 **앞의 것을 끊고 새 것이 받는다.** tmux 와 다른 선택이다(tmux 는 여럿이 함께 보게 한다). 이유는 README 임베디드 터미널 절이 이미 "한 번에 하나만 연다"고 정해두었고, 여럿이 볼 때 화면 크기를 어느 쪽에 맞출지가 곧바로 문제가 되기 때문이다. 여럿이 보는 것이 실제로 필요해지면 그때 정한다(선행 문서 원칙: 두 번째 사용처가 보일 때 만든다).

### 5.7 스크롤백과 재접속 화면 — 이 설계에서 가장 큰 위험

**감독은 자기 세션 출력 원시 바이트의 링 버퍼를 쥔다.** PTY 마스터에서 읽은 바이트를 붙어 있는 클라이언트에게 보내는 그 자리에서 링에도 함께 적는다. attach 하면 링의 내용을 먼저 흘려보내고 그 뒤부터 실시간이다.

**여기에 정직하게 적어둘 문제가 있다.** 원시 바이트 재생은 화면 복원이 아니다.

터미널 출력은 문자와 제어 시퀀스가 섞인 스트림이고, 화면의 최종 모습은 그 스트림을 처음부터 해석한 결과다. 링은 뒤쪽 일부만 갖고 있으므로, 재생은 **중간부터 해석을 시작하는 일**이 된다. 두 가지가 어긋난다.

1. **시퀀스 중간에서 시작할 수 있다.** 링이 오래된 바이트를 버릴 때 하필 `ESC [ 3 8 ; 5 ; 2 0 4 m` 의 한가운데를 잘라내면, 재생을 받은 터미널은 남은 조각을 문자로 찍는다.
2. **화면 상태가 이어지지 않는다.** 대체 화면 버퍼에 들어가 있었는지, 커서가 어디였는지, 어떤 색이 걸려 있었는지는 링이 시작하기 전의 바이트가 정한 것이라 재생에 없다.

**대응 (1) — 링의 버림 지점을 시퀀스 경계에 맞춘다.** 출력을 링에 적으면서 최소한의 ANSI 상태 기계를 함께 돌린다. 이 기계가 알아야 하는 것은 "지금 시퀀스 안인가"뿐이다 — CSI·OSC·DCS 의 시작과 끝만 알면 되고, 각 시퀀스가 무슨 뜻인지는 몰라도 된다(원칙 9). 링은 그 기계가 ground 상태였던 마지막 지점까지만 버린다. 그러면 재생은 항상 온전한 시퀀스에서 시작한다.

**대응 (2) — 붙는 순간 다시 그리게 한다.** 재생이 끝난 뒤 감독이 새 클라이언트의 크기를 PTY 에 적용하면(`TIOCSWINSZ`) 커널이 세션의 포그라운드 프로세스 그룹에 `SIGWINCH` 를 보내고, 화면을 다 쓰는 프로그램은 대개 그 신호에 전체를 다시 그린다.

**대응 (2) 의 한계를 적어둔다**: 커널은 크기가 **실제로 바뀔 때만** `SIGWINCH` 를 보낸다. 떼었다가 같은 크기의 터미널로 다시 붙으면 신호가 나가지 않고, 화면은 재생된 바이트가 만든 모습에 머문다. 크기를 한 칸 줄였다 되돌리는 수법은 신호를 두 번 만들지만 사용자에게 화면이 한 번 출렁이는 것으로 보인다.

**그래서 남는 것**: 줄 단위로 흐르는 출력(셸, 빌드 로그)은 원시 링 재생으로 충분하다. 화면 전체를 그리는 프로그램은 **같은 크기 재접속**에서 온전할지 확실하지 않다.

**결정**: v1 은 원시 링 + 대응 (1)(2) 로 간다. 감독 안에 화면 모델(가상 터미널 파서 + 셀 격자)을 두는 것이 tmux 가 하는 일이고 확실한 해법이지만, 그것은 이 작업의 크기를 몇 배로 올린다. **다만 이것은 "나중에 보자"로 미룰 항목이 아니라 기본값 전환을 막는 관문이다** — 9절 4 단계의 선행 조건에 "빈 터미널에서 `claude` 를 띄우고, 같은 크기의 터미널로 떼었다 붙었을 때 화면이 온전한지 실측"을 넣는다. 실측이 실패하면 4 단계 전에 화면 모델을 만들거나, 만들 때까지 tmux 가 기본값으로 남는다.

#### 5.7.1 관문 실측 결과 (2026-08-31, 4 단계 착수 시점) — **불통과**

링이 감긴 뒤의 재접속을 실제로 쟀다. 진짜 감독·진짜 PTY 위에 4 MiB 링을 6 MB 출력으로 넘기고, 받은 바이트를 진짜 터미널 에뮬레이터에 그려 셀 격자를 견줬다. 기준은 "계속 붙어 있던 터미널이 받았을 것" 이다.

| 세션의 출력 모양 | 같은 크기 재접속 | 다른 크기 재접속 |
|---|---|---|
| 줄 단위로 흐르기만 함 (셸·빌드 로그) | ✅ 온전 — 격자가 한 칸도 다르지 않다 | ✅ 온전 |
| 매 프레임 화면 전체를 절대 위치로 다시 그림 | ⚠️ 격자는 같으나 **대체 화면 모드가 사라진다** | ⚠️ 같음 |
| 대체 화면 + 머리줄·상태줄을 한 번 그리고 스크롤 영역 안에서만 흐름 (pager 계열) | ❌ **깨진다** — 머리줄·상태줄이 없고 스크롤 영역 지정도 사라져 내용이 그 자리를 덮는다 | ❌ SIGWINCH 로 테두리는 돌아오지만 스크롤 영역 밖에 내용이 남는다 |

**5.7 이 예측하지 못한 것이 하나 나왔다 — 어긋나는 것은 화면만이 아니라 입력이다.**

실제 `claude`(2.1.248) 를 세션으로 띄워 잰 결과, 기동 때 다음을 **각각 한 번씩** 낸다:

| 시퀀스 | 뜻 |
|---|---|
| `ESC[?2004h` | 괄호 붙여넣기 |
| `ESC[?1004h` | 포커스 보고 |
| `ESC[?2031h` | 색 구성 변경 알림 |
| `ESC[>4;2m` · `ESC[>5u` · `ESC[<u` | 확장 키 보고 (modifyOtherKeys · kitty 키보드) |

그리고 **SIGWINCH 로 다시 그릴 때는 `ESC[?2026h/l`(동기 출력 묶음) 말고 아무 모드도 다시 세우지 않는다.** 링이 감기면 이 설정 바이트가 재생 창 밖으로 밀려나고, 다시 붙은 터미널은 그 모드가 꺼진 채로 세션을 받는다. 세션 안 프로그램은 켜져 있다고 믿는다.

**그래서 대응 (2) 가 구제하지 못한다.** 대응 (2) 는 "붙는 순간 다시 그리게 한다" 이고, 다시 그리기는 화면에 찍는 일이지 모드를 세우는 일이 아니다. 5.7 은 크기가 다른 재접속이 SIGWINCH 로 구제된다고 봤지만, 그것은 **그리기 쪽만** 맞다. 확장 키 보고가 꺼진 터미널은 프로그램이 기다리는 키 조합을 다른 바이트로 보낸다 — 사용자에게는 "다시 붙었더니 어떤 키가 안 먹는다" 로 보인다.

**링을 키우는 것은 해법이 아니다.** 링 크기는 감기는 시점을 미룰 뿐이고, 오래 쓰는 세션에서 감기는 것은 시간 문제다. 이 관문은 용량의 문제가 아니라 "중간부터 해석을 시작한다" 는 성질의 문제다(위 두 문단).

**남는 결론**: 원시 바이트 링만으로는 관문을 넘지 못한다. **감독 안에 화면 모델을 두는 작업이 4 단계 앞에 하나 더 들어간다.** 그때까지 tmux 가 기본값으로 남는다 — 이것이 이 절의 "결정" 이 미리 정해둔 갈림길의 결과다.

**화면 모델이 갖춰야 하는 것** (다음 작업의 범위):

| 무엇 | 왜 |
|---|---|
| 셀 격자 (문자 · 색 · 속성) | 재생이 아니라 **현재 화면을 그려 보내기** 위해. 중간부터 해석하는 문제가 아예 사라진다 |
| 터미널 모드 상태 (대체 화면 · 괄호 붙여넣기 · 키 보고 · 커서 보이기) | 붙는 순간 되세워 보내야 입력이 어긋나지 않는다 — 이번 실측이 찾아낸 것 |
| 커서 위치 · 스크롤 영역 | 재생 뒤 이어지는 실시간 출력이 맞는 자리에 찍히도록 |
| 스크롤백은 링 그대로 | 위로 올려 보는 것은 지금 링이 하는 일로 충분하다. 화면 모델이 대신할 것은 **보이는 화면** 이다 |

실측을 시험으로 남긴 자리: `internal/session/supervise_reattach_gate_test.go`.

**메모리는 세션 수에 비례한다.** 링이 감독마다 하나이므로 세션 넷이면 링 넷이다. 중앙 데몬이었어도 세션당 링이라는 점은 같았으니 총량은 다르지 않지만, **한 프로세스의 상주 메모리로 보이던 것이 여러 프로세스로 흩어져 보인다.** 링 크기 기본값을 정할 때(10절 1번) `ps` 에서 감독 하나가 어떻게 보이는지가 사용자 인상에 들어간다.

### 5.8 SessionBackend 계약 — 다섯은 그대로, 하나가 는다

다섯 함수(`internal/session/backend.go:25`)의 시그니처와 의미는 **바뀌지 않는다.** `SupervisorBackend` 가 같은 계약을 채우고 같은 센티널을 반환한다.

**하나가 는다.**

```go
// DetachKey 는 이 백엔드의 세션에서 빠져나오는 키다.
DetachKey() string
```

`internal/cli/attach.go:17` 의 주석이 이 순간을 미리 적어두었다 — *"백엔드가 아직 하나뿐이어서다. 두 번째 백엔드가 생기면 그때 세션 계층이 답하도록 옮긴다."* 상수 `detachKeyBinding`(`internal/cli/attach.go:22`) 은 사라지고, 그것을 쓰던 두 자리(`attach.go:53` 의 안내, `attach.go:58` 의 중첩 오류)가 백엔드에게 묻는다.

**키를 돌려주고 문장은 돌려주지 않는다.** 무엇을 어떻게 말할지는 표시 계층의 몫이다 — 백엔드가 완성된 문장을 주면 CLI 와 GUI 가 같은 문장을 쓰게 되는데, 두 곳은 사용자가 있는 자리가 달라 할 말도 다르다.

**감독 백엔드의 키는 `Ctrl-\` 다.**

| 후보 | 판단 |
|---|---|
| `Ctrl-\` (0x1c) | **선택.** 대화형 프로그램이 거의 쓰지 않는다. `dtach`·`abduco` 의 관례이기도 하다 — 배치가 같으니 관례도 따른다(3.2). raw 모드에서는 `ISIG` 를 끄므로 SIGQUIT 이 되지 않는다 |
| tmux 처럼 접두 키 + `d` | 기각. tmux 의 `Ctrl-b` 는 readline·emacs 계열에서 "한 글자 뒤로" 라 세션 안 프로그램에게서 그 키를 뺏는다. 다른 접두 키를 골라도 상태를 가진 키 파싱이 하나 생긴다 |
| `Ctrl-a d` | 기각. `Ctrl-a` 는 "줄 처음으로" 다. 위와 같은 이유로 더 나쁘다 |

단일 키의 대가: 그 키를 세션 안으로 **보낼 수 없다.** 두 번 누르면 통과시키는 탈출 규칙을 둘 수 있지만 v1 은 하지 않는다 — `Ctrl-\` 를 세션 안에서 써야 하는 상황이 실제로 나타나면 그때 만든다.

### 5.9 의미론 대응표

원칙 8("의미론을 새로 정하지 않는다")이 요구하는 것을 표로 적어둔다. 3 단계의 동등성 테스트가 확인할 목록이기도 하다.

| 성질 | tmux 백엔드 | 감독 백엔드 | 같은가 |
|---|---|---|---|
| 세션 생존의 근거 | tmux 서버 프로세스 | 그 세션의 감독 프로세스 | ✅ 같은 성질, 반경만 좁다 |
| 그 프로세스가 죽으면 | 세션 **전부** 죽음 | **그 세션 하나** 죽음 | ⚠️ 감독 쪽이 좁다 — 나은 방향의 차이 |
| 세션 0 개의 표현 | 서버 없음 | 소켓 없음 (또는 디렉토리 없음) | ✅ |
| detach | 클라이언트 종료 | 클라이언트 연결 종료 | ✅ |
| detach 키 | `Ctrl-b d` | `Ctrl-\` | ⚠️ 다르다 — 5.8 이 인터페이스로 흡수 |
| 떼었다 다시 붙었을 때의 화면 | 화면 모델이 그대로 복원한다 | 원시 링 재생 — 링이 감기면 어긋난다 | ❌ **다르다** — 관문이 여기서 막혔다(5.7.1). 3.5 단계가 메운다 |
| 재접속 뒤의 터미널 모드 (대체 화면 · 키 보고 · 괄호 붙여넣기) | 화면 모델이 함께 세운다 | 링이 감기면 잃는다 | ❌ **다르다** — 화면이 아니라 입력이 어긋나는 자리다(5.7.1) |
| 중첩 표식 | `TMUX` 환경변수 | `KYU_SESSION` 환경변수 | ✅ 같은 기전 |
| 중복 이름 생성 | `has-session` 선조회 후 `ErrSessionExists` | 이름 잠금 획득 실패가 곧 `ErrSessionExists` | ✅ 같은 결과, 감독 쪽이 원자적 |
| 없는 세션 kill | `nil` (멱등) | `nil` (멱등) | ✅ |
| 세션 명명 | `kyu-<workdir>-<repo>` | 동일 | ✅ `internal/session/name.go` 그대로 |
| `--all` 접두사 필터 | `WorkDirSessionPrefix` | 동일 | ✅ `internal/session/name.go:30` 그대로 |
| 이름의 `.`·`:` 치환 | tmux 타깃 문법 때문에 필요 | 파일 이름으로 쓰이므로 여전히 유용 | ✅ 아래 |
| 세션 안 명령 변경 | 불가 (생성 시 확정) | 불가 (생성 시 확정) | ✅ `internal/cli/start.go` 의 경고가 그대로 맞다 |

**이름 치환을 유지하는 이유**: `tmuxTargetSyntaxReplacer`(`internal/session/name.go:19`) 는 tmux 의 타깃 문법을 피하려고 만든 것이다. 감독 백엔드에서는 이유가 달라지지만 사라지지는 않는다 — 이름이 파일 이름이 되므로 특수문자를 줄여두는 것이 여전히 이롭다(5.2). 그리고 백엔드를 바꿨을 때 **같은 워크디렉토리가 다른 세션 이름을 갖게 되면** 사용자가 tmux 로 되돌렸을 때 자기 세션을 잃어버린 것처럼 보인다. 치환 규칙은 이름을 만드는 순수 함수이고 백엔드에 딸린 것이 아니므로, 한쪽에 이유가 남아 있는 한 양쪽이 같은 규칙을 쓴다.

---

## 6. 백엔드 선택과 전환

**`KYU_SESSION_BACKEND=tmux|supervisor`.** 읽는 곳은 `cmd/kyu/main.go:151` 의 `withSessionBackend` 한 군데다. 그 함수의 주석이 이미 그 역할을 적어두었다 — *"어느 플랫폼 구현을 쓸지는 진입점의 결정이다. 그래야 internal/cli 가 tmux 를 모른 채로 남고, 백엔드가 늘어날 때 바뀌는 파일이 이 하나로 끝난다."*

| 시기 | 기본값 | 없는 백엔드를 지정하면 |
|---|---|---|
| 1~3.5 단계 | `tmux` | 감독은 환경변수로만 |
| 4 단계 이후 | `supervisor` | tmux 미설치 안내는 `KYU_SESSION_BACKEND=tmux` 일 때만 나온다 |

`KYU_SESSION_BACKEND`(선택)와 `KYU_SESSION`(중첩 표식, 5.3)은 이름이 앞부분을 공유한다. 둘 다 정확히 일치하는 이름으로만 조회하므로(`os.LookupEnv`) 섞이지 않지만, 읽는 사람이 한 번 멈칫할 자리라 여기 적어둔다.

**tmux 를 지우지 않는 이유**(원칙 7): 검증된 폴백이 있는 것과 없는 것은 사용자가 감수하는 위험이 다르다. 감독이 어떤 터미널·어떤 셸에서 무너지면, 사용자는 자기 작업을 멈추지 않고 환경변수 하나로 돌아갈 수 있어야 한다. 코드 비용도 거의 없다 — `TmuxBackend` 는 이미 완성되어 테스트까지 있고, 3 단계 이후로는 동등성 매트릭스가 두 백엔드를 함께 돌리므로 방치되어 썩지도 않는다.

지우는 판단은 두 백엔드가 오래 같이 산 뒤에 따로 한다. 이 문서는 그것을 정하지 않는다.

---

## 7. GUI 에 닿는 것

**앱은 PTY 를 열어 그 안에서 `kyu attach` 를 실행한다**(`desktop/.../terminal/pty/PtyKyuSessionTerminalAttacher.kt:81`). `kyu attach` 가 tmux 클라이언트에서 감독 클라이언트로 바뀌어도 **그 구조는 그대로다** — 앱이 부르는 것은 여전히 `kyu` 이고, `kyu` 가 여전히 TTY 를 요구하며, PTY 가 여전히 그것을 준다.

그래도 손볼 곳이 셋 있다. **"GUI 무변경"이 문자 그대로 참이 아니므로 적어둔다.**

| 자리 | 무엇 | 왜 |
|---|---|---|
| `desktop/.../terminal/SessionEntryPlan.kt:62`·`71` | `TMUX` 만 지우던 것을 `KYU_SESSION` 도 지우도록 | 앱을 세션 안에서 띄웠을 때 물려받은 표식은 앱 안 PTY 의 사실이 아니다. 표식이 바뀌었으니 지울 것도 바뀐다 |
| `desktop/.../terminal/pty/KyuAttachTtyConnector.kt:31` | 주석만 — "이 한 줄이 tmux 까지 닿는다" | 실제로는 그 세션의 감독까지 닿는다. 경로가 한 홉 길어진다(아래) |
| `desktop/.../terminal/EmbeddedTerminalStateHolder.kt:17` | 주석만 — tmux 가 보장한다는 서술 | 보장하는 주체가 바뀐다 |

**리사이즈 경로가 한 홉 길어진다.** 지금은 PTY 의 winsize 변경 → `SIGWINCH` → tmux 클라이언트가 세션 창을 다시 그린다. 감독에서는 PTY winsize 변경 → `SIGWINCH` → **`kyu attach` 클라이언트가 그 신호를 받아 RESIZE 프레임을 보낸다** → 감독이 세션 PTY 에 적용 → 세션 안 프로그램에 `SIGWINCH`. 가운데 굵은 부분이 새로 만들어야 하는 것이다. **`kyu attach` 클라이언트에 `SIGWINCH` 핸들러를 두는 일이 2 단계에 들어간다** — tmux 클라이언트가 대신 해주던 일이라 지금은 없다.

**앱의 검증 항목**(2 단계 완료 확인에 넣는다):

1. 카드를 눌러 터미널이 열리고 키 입력·색·스크롤백이 터미널 창에서 `kyu attach` 한 것과 같다
2. 창 크기를 바꾸면 세션 화면이 따라 바뀐다 (위의 새 경로)
3. 터미널을 닫고 다른 카드로 갔다가 돌아오면 하던 자리가 나온다 (detach → 재접속 → 스크롤백 재생, 5.7)
4. 앱을 껐다 켜도 세션이 살아 있다 (원칙 5)
5. 앱을 tmux 세션 안에서 띄워도 카드가 열린다 (표식 제거의 첫 줄)
6. **세션 넷을 띄우고 그중 하나의 감독을 `kill -9` 했을 때, 대시보드에서 그 카드만 `RUNNING` 에서 빠지고 나머지 셋은 그대로다** — 이 설계를 고른 이유를 직접 확인하는 항목이다

앱이 `kyu` 에게서 받는 `--json` 문서(README 기계용 표면)는 하나도 바뀌지 않는다. 세션이 어디서 사는지는 그 문서에 없는 사실이다.

---

## 8. 윈도우 전망

**이 작업이 로드맵 v4(윈도우 네이티브)의 전제다.** 3층 구조에서 플랫폼에 의존하는 곳은 세션 계층뿐이고(선행 문서 5.1), 지금 그 자리를 tmux 가 채우고 있어서 윈도우에는 채울 것이 없었다. 감독을 우리가 갖고 나면 남는 것은 **같은 감독의 부품 몇을 윈도우 것으로 바꾸는 일**이다.

| 부품 | 유닉스 | 윈도우 |
|---|---|---|
| 의사 터미널 | PTY (`/dev/ptmx` 계열) | ConPTY (`CreatePseudoConsole`) |
| 클라이언트 통로 | 세션당 유닉스 도메인 소켓 | 세션당 명명 파이프 `\\.\pipe\kyu-<user>-<세션 이름>` |
| 배타성 (= `ErrSessionExists`) | 세션 이름별 `flock` (5.3) | 같은 이름의 뮤텍스·파이프 인스턴스 생성 실패 |
| 세션 목록 | 소켓 디렉토리 스캔 | 파이프 이름 공간 열거 |
| 접근 제어 | 파일 권한 0600 | 파이프의 DACL 을 현재 사용자로 |
| 크기 변경 | `TIOCSWINSZ` | `ResizePseudoConsole` |
| 종료 | 프로세스 그룹에 `SIGHUP` | Job Object 로 트리째 |
| 떼어내기 | `setsid` | `DETACHED_PROCESS` |
| 준비 신호 | 물려준 파이프 fd | 상속 가능한 핸들로 같은 것 |

**바뀌지 않는 것**: 프로토콜(5.5·5.6), 스크롤백(5.7), 세션 명명, `SessionBackend` 계약, 조율 계층 전부, 표시 계층 전부. 프레임 정의와 JSON 은 바이트라서 통로가 무엇이든 같다.

**세션별 감독이 윈도우에서 오히려 잘 맞는다.** 명명 파이프는 이름 공간에 사는 것이라 "이름 하나에 서버 하나"가 자연스럽고, 그 이름을 이미 세션 이름이 정해준다. 중앙 데몬이었으면 파이프 하나 위에 세션 다중화를 다시 얹어야 했다.

윈도우도 AF_UNIX 소켓을 지원하므로 통로를 그대로 쓸 수 있을 가능성이 있다. 다만 접근 제어가 파일 권한으로 이뤄지는 부분이 윈도우에서 같은 뜻인지 확인되지 않았고, 소켓에 붙을 수 있다는 것이 그 세션에 키를 보낼 수 있다는 뜻인 이상(5.2) 확인 없이 고를 항목이 아니다. **명명 파이프를 기본으로 두고 AF_UNIX 는 확인 뒤에 판단한다**(10절 7번).

이 절은 전망이지 계획이 아니다. 윈도우는 별도 작업이고 9절의 5 단계로 떼어둔다.

---

## 9. 구현 단계

각 단계가 **PR 하나**이고, 각 단계 끝에 손으로 확인 가능한 상태가 된다(선행 문서 9.2 와 같은 자세).

| 단계 | 내용 | 완료 확인 |
|---|---|---|
| **1** ✅ (PR #36) | 런타임 경로(5.2) · `kyu __supervise` 골격 · 소켓 바인딩으로 배타성 확보 · 준비 파이프 · 제어 프로토콜(5.5) · `SupervisorBackend` 의 create/list/kill/alive · `List` 디렉토리 스캔(5.4) · 죽은 소켓 정리(5.3) | `KYU_SESSION_BACKEND=supervisor kyu start proj-a` 로 세션이 뜨고, `kyu list` 에 `RUNNING` 으로 보이고, `kyu kill` 로 죽고 소켓이 사라진다. 같은 이름으로 두 번 `start` 하면 `ErrSessionExists` 다. `ps` 에 감독이 세션 수만큼 보인다. **attach 는 아직 안 된다** |
| **2** ✅ (PR #37) | attach 프레임(5.6) · 스크롤백 링과 경계 처리(5.7) · raw 모드 클라이언트 · `SIGWINCH` 핸들러(7절) | 터미널에서 붙어 작업하고 `Ctrl-\` 로 빠져나왔다가 다시 붙으면 하던 자리가 나온다. 창 크기를 바꾸면 세션 화면이 따라 바뀐다. **앱의 검증 6 항목(7절)이 전부 통과한다** |
| **3** ✅ (PR #38) | `DetachKey` 인터페이스 확장(5.8) · 동등성 테스트 매트릭스 · CI 가 두 백엔드를 돈다 | 같은 테스트 본문이 두 백엔드에서 통과한다. 5.9 표의 각 줄에 대응하는 테스트가 있다 |
| **4** ⛔ 관문 불통과로 멈춤 (5.7.1) — 3.5 단계가 먼저다 | 기본값을 `supervisor` 로 · README 요구사항 표에서 tmux 를 선택으로 강등 · `install.sh` 안내 정리 | 환경변수 없이, **tmux 가 설치되지 않은 머신에서** 워크디렉토리를 만들고 세션을 띄우고 붙어 작업하고 나오는 전 과정이 돈다 |
| **5** (별도) | 윈도우 ConPTY + 명명 파이프(8절) | 윈도우 네이티브에서 세션이 뜨고 붙는다. 릴리스에 msi 가 붙는다 |

**1 단계에 넣을 격리 시험 하나**: 세션 셋을 띄우고 가운데 것의 감독을 `kill -9` 한 뒤 `kyu list` 를 부른다. 죽은 것만 목록에서 빠지고 나머지 둘은 `RUNNING` 이어야 한다. **이 설계를 고른 이유가 실제로 성립하는지를 가장 먼저 확인한다** — 나중에 확인하면 그때는 이미 이 배치 위에 많은 것이 얹혀 있다.

**4 단계의 선행 조건 — 이것이 통과하지 않으면 기본값을 바꾸지 않는다**

빈 터미널에서 `claude` 를 세션으로 띄우고, 대화를 몇 번 주고받아 화면을 채운 뒤

1. 떼었다가 **크기가 다른** 터미널로 다시 붙는다 → 화면이 온전한가
2. 떼었다가 **크기가 같은** 터미널로 다시 붙는다 → 화면이 온전한가

2 번이 5.7 이 지목한 위험이다. 실패하면 감독 안에 화면 모델을 만드는 작업이 4 단계 앞에 하나 더 들어가고, 그때까지 tmux 가 기본값으로 남는다. **이 실측은 2 단계 PR 의 본문에 결과를 적는다** — 4 단계에 가서 처음 알게 되면 이미 늦다.

**조건 하나가 빠져 있었다**: 위 두 줄은 링이 감겼는지를 말하지 않는다. 2 단계 PR 의 실측은 세션이 뱉은 전부(13 KB)가 4 MiB 링에 통째로 들어 있던 조건이라 재생이 곧 처음부터 다시 보내기였고, 그래서 둘 다 통과했다. **관문이 재려던 위험은 링이 감긴 뒤에야 나타난다.** 조건을 "링이 한 바퀴 이상 감긴 뒤" 로 고쳐 적는다.

**실측 결과 (2026-08-31) — 불통과.** 세 가지 출력 모양과 실제 `claude` 로 쟀고, 결과와 근거는 5.7.1 에 있다. 요지는 둘이다:

- 줄 단위로 흐르는 출력은 링이 감긴 뒤에도 온전하다
- 화면을 다 쓰는 프로그램은 온전하지 않고, **크기가 다른 재접속도 구제되지 않는다** — 잃는 것이 화면만이 아니라 터미널 모드(입력)라서, 다시 그리기가 되돌리지 못한다

**그래서 4 단계 앞에 단계가 하나 들어간다.** 아래 표의 3.5 단계다.

| 단계 | 내용 | 완료 확인 |
|---|---|---|
| **3.5** (새로 들어감) | 감독 안의 화면 모델 — 셀 격자 · 터미널 모드 상태 · 커서 · 스크롤 영역. 붙는 순간 재생 대신 **현재 화면을 그려 보낸다** (범위는 5.7.1 의 표) | `supervise_reattach_gate_test.go` 의 관문 시험이 "모드가 재생에 없다" 에서 "붙는 순간 모드가 되세워진다" 로 뒤집힌다. 링이 감긴 세션에 같은 크기로 다시 붙어도 화면과 키가 온전하다 |

**1 단계가 먼저인 이유**: 선행 문서 9.2 의 이유가 여기서도 같다. 이 작업에서 예상 못 한 문제가 있는 곳은 프로세스를 떼어내고 소켓을 붙잡는 부분이지 프레이밍이 아니다. 다만 순서가 뒤집히지는 않는다 — attach 없이 뜨는 세션은 반쪽이지만 `list`·`kill` 로 확인은 되고, 프레임 설계를 세션 생명주기 위에서 하는 편이 그 반대보다 낫다.

**의존 하나**: PTY 할당에 `github.com/creack/pty` 를 쓴다. 직접 짜면 리눅스는 `/dev/ptmx` + `TIOCGPTN`, macOS 는 `TIOCPTYGRANT` 계열로 OS 마다 다른 ioctl 상수를 손으로 맞춰야 하는데, 그것을 검증할 하드웨어가 두 종류 다 필요하다. cgo 없이 도는 작은 라이브러리이고, 앱 쪽이 이미 같은 이유로 pty4j 에 기대고 있다(`PtyKyuSessionTerminalAttacher.kt`). raw 모드는 새 의존이 필요 없다 — `golang.org/x/term` 이 이미 직접 의존이다(`go.mod`).

---

## 10. 열린 질문

구현을 막지는 않지만 해당 단계에서 정해야 하는 것들이다.

**확정으로 해소된 것 넷**을 먼저 적는다. 초안(중앙 데몬)에 있던 질문이고, 세션별 감독을 고르면서 사라졌다.

| 초안의 질문 | 어떻게 사라졌나 |
|---|---|
| 데몬 크래시 복구 | **반경이 줄어 문제의 크기가 바뀌었다.** 감독이 죽으면 그 세션 하나가 죽는다. 나머지는 자기 감독 위에서 계속 돈다. **되살리는 기능은 여전히 만들지 않는다** — 마스터를 쥔 프로세스가 사라지면 세션이 끝나는 것은 2절의 결론이고, 뒤에 남은 자식을 새 PTY 에 다시 붙일 방법은 없다 |
| 데몬 자동 종료 정책 | **해소.** 감독은 자기 세션이 끝나면 죽는다. 유예 시간이라는 숫자를 정할 일이 없다(5.3) |
| 기동 경쟁 (전역 잠금 + 폴링) | **해소.** 전역 직렬화 지점이 사라졌다. 배타성은 세션 이름별 `flock` 이 주고, 감독은 그것을 수명 내내 쥔다 — 다른 세션의 `create` 는 서로를 기다리지 않는다. 폴링은 준비 파이프로 대체되었다(5.3) |
| 프로토콜 버전 불일치가 전부-아니면-전무 | **부분 해소.** 세션마다 자기를 띄운 판이 서빙하므로 열화가 점진적이다. 남은 부분은 아래 4번 |

**남은 질문**

1. **스크롤백 링 크기 기본값 — 잠정 4 MiB (2 단계에서 실측)** — tmux 는 창당 2000 줄(`history-limit`)이다. 원시 바이트 링이라 줄이 아니라 바이트로 재야 하는데, 색과 커서 이동이 섞인 출력은 한 줄이 수백 바이트가 되기도 한다.

   2 단계에서 잰 값(리눅스, `claude` 2.1.248, 빈 대화):

   | 잰 것 | 값 |
   |---|---|
   | 화면 전체 다시 그리기 한 번 (120x40 → 119x40) | **2.7 KB** |
   | 세션 하나가 뜬 뒤 재접속까지의 출력 총량 | 13 KB |
   | 감독 프로세스의 상주 메모리 (링이 거의 빈 상태) | 15 MB |
   | 그 세션 안 `claude` 의 상주 메모리 | 326 MB |

   **4 MiB 를 잠정값으로 둔다.** 다시 그리기 한 번의 1500 배라 5.7 의 대응이 성립하고, 다 차도 감독 하나가 세션 안 `claude` 의 1/60 이라 `ps` 에서 눈에 띄지 않는다. 링은 고정 크기로 잡지만 상주 메모리는 실제로 쓴 만큼만 늘어난다.

   **잠정인 이유**: 잰 것이 빈 대화의 한 화면이다. 대화가 길고 창이 큰(200 열 넘는) 화면의 다시 그리기는 이보다 크고, 그 값은 실제로 오래 쓴 세션에서만 나온다. 설정으로 뺄지는 4 단계에서 정한다 — 지금 빼면 어디에 두고 어떤 형식으로 읽을지를 근거 없이 정하게 된다.

   **이 숫자는 관문의 지렛대가 아니다 (2026-08-31 실측, 5.7.1)**. 링을 키우면 감기는 시점이 미뤄질 뿐이고, 오래 쓰는 세션에서 감기는 것은 시간 문제다. 관문이 막고 있는 것은 용량이 아니라 "중간부터 해석을 시작한다" 는 성질이라, 화면 모델(3.5 단계)이 들어오면 이 숫자의 역할도 바뀐다 — 그때 링은 화면이 아니라 **위로 올려 보는 스크롤백** 만 맡는다. 그 뒤에 정하는 편이 근거가 있다.

2. **소켓 권한 0600 은 확정, 검사 여부가 열려 있다** — 만드는 것은 0600 이다(5.2). 문제는 **연결받을 때 소켓 파일의 권한을 다시 확인할 것인가** 다. `umask` 나 사용자의 손에 의해 파일 모드가 열려 있을 수 있다. 확인하고 열려 있으면 거절하는 편이 안전하지만, 정상적인 사용자를 막을 실패 모드가 하나 생긴다. **감독 기동 때 자기 소켓을 한 번 확인하고 열려 있으면 서빙을 거부하는 안**을 추천한다 — 매 연결마다 검사하는 것보다 싸고, 실패가 기동 시점에 몰려 진단하기 쉽다.

3. **감독이 죽었는데 자식이 남는 경우** — 감독을 `kill -9` 하면 PTY 마스터가 닫히고 자식에게 `SIGHUP` 이 가지만, `SIGHUP` 을 무시하는 프로그램은 살아남는다. 그러면 소켓은 죽었는데 프로세스가 남는다. 이것을 감지하고 치울 것인지, 아니면 tmux 와 같은 정도로 두고 사용자에게 맡길지. **tmux 도 같은 상태를 만들 수 있으므로 원칙 8("의미론을 새로 정하지 않는다")에 따르면 두는 것이 맞다.** 1 단계에서 실제로 어떤 상태가 되는지 확인하고 정한다.

4. **새 `kyu` 가 옛 감독을 만났을 때의 처방** — `v` 로 감지하는 것은 정해졌다(5.5). 세션별로 갈리므로 전면적이지는 않지만, 그 세션 하나에 대해서는 여전히 답이 필요하다. 후보: (가) 그 세션만 거절하고 "이 세션은 옛 판 `kyu` 가 만들었습니다 — `kyu kill <repo>` 뒤 다시 띄우세요"라고 알린다 (나) 옛 프로토콜을 함께 말할 수 있게 유지한다. **(가)를 추천한다** — (나)는 프로토콜이 늘어날수록 조합이 늘고, 세션 하나를 다시 띄우는 비용은 낮다. `kyu list` 가 그런 세션을 어떻게 표시할지는 함께 정한다.

5. **버려진 세션 이름의 죽은 소켓** — `List` 는 죽은 소켓을 지우지 않고(5.4), 지우는 것은 그 이름으로 다시 `Create` 할 때다. 워크디렉토리를 버리면 그 이름은 다시 오지 않으므로 0 바이트 파일이 남는다. `$XDG_RUNTIME_DIR` 경로에서는 재부팅이 치우지만 macOS 폴백 경로에서는 남는다. 후보: 그대로 둔다(해가 없다) / 감독이 뜰 때 자기 디렉토리의 오래된 죽은 소켓을 한 번 치운다 / `kyu kill --all` 이 함께 치운다. **세 번째가 사용자의 의도와 가장 가깝다** — 정리를 명시적으로 요청한 자리다. 실제로 얼마나 쌓이는지 보고 정한다.

6. **소켓 경로 길이** — 유닉스 도메인 소켓의 경로는 `sun_path` 에 들어가고 그 크기가 macOS 104 · 리눅스 108 바이트다. **중앙 데몬의 고정 경로와 달리 이제 세션 이름이 경로에 들어가므로 한도에 더 가깝다.** `$HOME` 이 깊고 워크디렉토리·레포 이름이 길면 닿을 수 있다. 닿으면 소켓 디렉토리로 `chdir` 한 뒤 상대 경로로 바인딩하는 길이 있다. **1 단계에서 한도를 넘는 경로를 만나면 알아볼 수 있는 오류로 끊는다** — 조용히 실패하면 원인을 찾는 데 오래 걸리는 종류의 문제다.

7. **윈도우 통로: 명명 파이프인가 AF_UNIX 인가** — 8절. 윈도우에서 AF_UNIX 소켓의 접근 제어가 파일 권한과 같은 뜻인지 확인되지 않았다. 5 단계에서 확인한다.

8. **`creack/pty` 의 윈도우 지원 범위** — 유닉스 계열이 본령인 라이브러리다. ConPTY 를 어디까지 다루는지 확인되지 않았고, 다루지 않으면 윈도우 쪽 의사 터미널은 따로 쓰게 된다. 5 단계 전에 확인한다. 1~4 단계에는 영향이 없다.

---

## 부록 A. 확인된 사실

구현 중 다시 조사하지 않도록 근거를 모아둔다.

| 사실 | 근거 |
|---|---|
| PTY 마스터를 쥔 마지막 프로세스가 죽으면 슬레이브 쪽에 EOF·SIGHUP 이 간다 — 그래서 세션 생존은 마스터를 쥔 프로세스를 반드시 요구한다 | 2절 |
| `flock` 은 열린 파일과 함께 살고 프로세스가 죽으면 커널이 푼다 — 이 배타성이 `ErrSessionExists` 의 근거이고, `kill -9` 당한 감독의 이름 회수도 여기서 나온다 | 5.3 |
| bind 는 마쳤지만 아직 listen 하지 않은 유닉스 소켓도 `connect` 에 `ECONNREFUSED` 로 답한다 — 붙어보는 것으로는 "아직 안 뜬 감독"과 "죽은 감독"을 가를 수 없다 | 5.3 · 1 단계 구현이 찾아낸 것 |
| 유닉스 도메인 소켓은 파일이 이미 있으면 바인딩에 실패한다 — 그래서 죽은 소켓은 지우고 다시 바인딩해야 한다 | 5.3 |
| tmux 서버는 마지막 세션이 죽으면 함께 종료되고, 그때 tmux 명령은 종료 코드 1 로 끝난다. 즉 "세션 0 개"의 정상적 표현이 "서버 없음"이다 | `internal/session/tmux.go:116`~`127` 의 실측 주석 |
| tmux 는 `TMUX` 가 설정돼 있으면 `TMUX_TMPDIR` 을 무시하고 그 소켓에 붙는다 | `internal/session/tmux_test.go:31` 의 실측 주석 |
| tmux 의 `-t <name>` 은 기본이 접두사 매칭이라 정확 일치를 위해 `=` 를 붙여야 한다 | `internal/session/tmux.go:154` |
| 세션이 실행할 명령은 만드는 순간 확정되고 이후 바꿀 수 없다 | `internal/cli/start.go` 의 `warnRunningSessionKeepsTheCommandItWasCreatedWith` |
| `SessionBackend.Create` 에 env 인자가 없어, 환경변수가 필요한 호출부는 명령을 `env` 로 감싼다 | `internal/cli/start.go` 의 `envCommandName` |
| `kyu list` 의 실제 비용은 세션 조회가 아니라 레포마다 도는 git 상태 판정이다 | 선행 문서 5.3 의 상태 추론 표 · 3.1 의 상쇄 근거 |
| GUI 는 PTY 안에서 `kyu attach` 를 실행하고, PTY 가 아니면 TTY 요구를 채우지 못한다 | `desktop/.../terminal/pty/PtyKyuSessionTerminalAttacher.kt` 클래스 주석 |
| GUI 는 자식 환경에서 `TMUX` 를 덜어낸다 — 물려받은 표식이 앱 안 PTY 의 사실이 아니기 때문 | `desktop/.../terminal/SessionEntryPlan.kt:62` |
| GUI 대시보드가 아는 상태는 최대 3 초 묵은 것이다 | `desktop/.../terminal/SessionEntryPlan.kt:31` 의 주석 |
| detach 키를 세션 계층으로 옮기는 것은 두 번째 백엔드가 생길 때로 예정되어 있었다 | `internal/cli/attach.go:17`~`22` |
| 백엔드 선택은 진입점 한 군데의 결정이다 | `cmd/kyu/main.go:141`~`152` |
| CI 는 tmux 를 설치하고 `tmux -V` 로 설치를 확인한다 — 세션 테스트가 tmux 없으면 `t.Skip` 이라 조용히 통과하는 것을 막기 위해서다 | `.github/workflows/ci.yml:24`~`32` |
| 커널은 `TIOCSWINSZ` 로 크기가 **실제로 바뀔 때만** `SIGWINCH` 를 보낸다 — 같은 크기 재접속은 다시 그리기를 유발하지 못한다 | 5.7 · 2 단계에서 실측 |
| 링이 한 바퀴 감기면 세션이 기동 때 한 번만 세운 터미널 모드가 재생 창 밖으로 밀려나고, **SIGWINCH 다시 그리기는 그것을 되세우지 않는다** — 그래서 크기가 다른 재접속도 구제되지 않는다 | 5.7.1 · 4 단계 착수 시점의 실측 |
| `claude`(2.1.248) 는 기동 때 `?2004h`·`?1004h`·`?2031h`·`[>4;2m`·`[>5u`·`[<u` 를 각각 한 번씩만 내고, 크기 변경에는 `?2026h/l` 말고 아무 모드도 다시 세우지 않는다 | 5.7.1 |
| 줄 단위로 흐르기만 하는 출력은 링이 감긴 뒤에도 재접속 화면이 한 칸도 다르지 않다 — 관문이 막는 것은 화면을 다 쓰는 프로그램 쪽이다 | 5.7.1 · `supervise_reattach_gate_test.go` |
| 유닉스 도메인 소켓 경로 한도는 macOS 104 · 리눅스 108 바이트다 | `sun_path` 크기 · 10절 6번 |
| `golang.org/x/term` 은 이미 직접 의존이라 raw 모드에 새 의존이 필요 없다 | `go.mod` |
| 윈도우 네이티브가 막혀 있는 이유는 바이너리가 아니라 tmux 다 | `desktop/.../engine/githubrelease/EngineReleaseTarget.kt:7` · `.github/workflows/release.yml:132` |
| `dtach`·`abduco` 가 세션마다 프로세스 하나에 소켓 하나라는 배치를 오래 써왔다 | 3.2 |

## 부록 B. 용어

| 용어 | 의미 |
|---|---|
| **감독** (supervisor) | 세션 하나를 떠받치는 `kyu` 프로세스. 그 세션의 PTY 마스터와 스크롤백을 쥐고, 자기 소켓 하나를 듣고, 자기 세션이 끝나면 함께 끝난다 |
| **PTY 마스터 / 슬레이브** | 커널이 만드는 의사 터미널 쌍. 슬레이브는 세션 안 프로그램의 터미널이고, 마스터는 그 반대편이다 |
| **준비 신호** | 감독이 부모에게 "떴다/못 떴다"를 알리는 한 줄. 물려받은 파이프에 쓰고 닫는다 (5.3) |
| **제어 연결** | `alive`·`kill` 을 주고받는 짧은 연결. NDJSON (5.5) |
| **attach 연결** | 한 세션에 붙어 있는 동안 유지되는 연결. 프레임 (5.6). 연결 자체가 곧 붙어 있음이다 |
| **스크롤백 링** | 세션 출력 원시 바이트의 고정 크기 링 버퍼. 붙을 때 재생된다 (5.7) |
| **중첩 표식** | 세션 안에서 도는 프로세스가 물려받는 환경변수. tmux 는 `TMUX`, 감독은 `KYU_SESSION` (5.3) |
| **죽은 소켓** | 감독이 비정상 종료하면서 남긴 소켓 파일. 연결이 `ECONNREFUSED` 로 거절되는 것으로 구별한다 (5.3) |
| **동등성 테스트** | 같은 테스트 본문을 두 백엔드에서 돌려 5.9 표의 각 줄을 확인하는 매트릭스 (9절 3 단계) |
