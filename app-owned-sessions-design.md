# 앱이 세션을 보유한다 — 설계 문서

> 데스크톱 앱이 `kyu attach` 를 거치지 않고 자기 PTY 에서 `claude` 를 직접 띄운다. 화면 모델을 만드는 대신 이미 갖고 있는 것을 쓴다 — **JediTerm 위젯이 곧 살아있는 화면 모델이다.** 내구성은 프로세스 생존이 아니라 `claude --resume` 이 떠받친다.

- 작성일: 2026-08-31
- 상태: **1~4 단계 머지 완료 (PR #41 · #42 · #43 · #44).** 4 단계에서 코드 한 자리가 남았다 — 7.0
- 선행 문서: [workdir-orchestrator-design.md](workdir-orchestrator-design.md) — 특히 원칙 5, 5.1(3층 구조), 5.4(메인 세션의 `--add-dir`)
- 선행 문서: [session-supervisor-design.md](session-supervisor-design.md) — 특히 3.3(이 안을 기각했던 자리), 5.7.1(관문 실측), 9절 3.5 단계
- 대상 플랫폼: macOS, Linux(WSL 포함) — 윈도우 네이티브가 이 전환으로 가까워진다(5.7)
- 사용자 결정 (2026-08-31): **GUI 의 세션 계층을 앱이 보유한다.** 감독 3.5 단계(화면 모델)는 착수 전에 멈춘다
- 사용자 결정 (2026-08-31): **감독은 지우지 않고 CLI 옵션으로 존치한다.** 감독 설계 원칙 7("지우지 않는다, 강등한다")을 감독 자신에게 적용한다

**낱말 하나를 먼저 정한다.** 사용자는 이 전환을 "Conductor 모델" 이라고 불렀다. 그런데 Conductor 는 선행 문서 3.3 이 기각한 기존 오케스트레이터의 이름이기도 하다. 같은 낱말이 "그 도구" 와 "그 도구들이 공유하는 배치" 를 동시에 가리키면 다음 세션이 "그럼 Conductor 를 쓰기로 한 건가" 를 다시 묻게 된다. **이 문서는 배치를 가리킬 때 "앱이 세션을 보유한다" 로 쓰고, Conductor 는 도구 이름으로만 쓴다.** 기존 오케스트레이터를 채택한다는 결정은 없다 — 그들은 여전히 단일 레포·git worktree 를 전제하고(선행 문서 3.3), 이번에 빌려오는 것은 "앱이 PTY 를 쥔다" 는 배치 하나뿐이다.

---

## 1. 배경 — 왜 방향을 바꾸는가

### 1.1 관문이 막은 것

감독 설계는 4 단계(기본값 전환) 앞에 관문을 하나 걸어두었다. *"링이 한 바퀴 이상 감긴 뒤, 떼었다가 같은 크기의 터미널로 다시 붙었을 때 화면이 온전한가."*

2026-08-31 에 쟀고, **불통과했다**([session-supervisor-design.md 5.7.1](session-supervisor-design.md#571-관문-실측-결과-2026-08-31-4-단계-착수-시점--불통과)). 요지는 둘이다.

- 줄 단위로 흐르기만 하는 출력은 링이 감긴 뒤에도 한 칸도 다르지 않다
- 화면을 다 쓰는 프로그램은 온전하지 않고, **크기가 다른 재접속도 구제되지 않는다** — 잃는 것이 화면만이 아니라 터미널 모드(입력)라서, `SIGWINCH` 다시 그리기가 되돌리지 못한다

실측이 설계가 예측하지 못한 것을 하나 찾아냈다. `claude`(2.1.248) 는 기동 때 괄호 붙여넣기·포커스 보고·확장 키 보고를 **각각 한 번씩만** 내고 그 뒤로 다시 세우지 않는다. 링이 감기면 그 바이트가 재생 창 밖으로 밀려나고, 다시 붙은 터미널은 그 모드가 꺼진 채로 세션을 받는다. 세션 안 프로그램은 켜져 있다고 믿는다. 사용자에게는 "다시 붙었더니 어떤 키가 안 먹는다" 로 보인다.

### 1.2 남은 조각의 무게

관문이 정해둔 갈림길의 결과는 "4 단계 앞에 3.5 단계(화면 모델)를 하나 더 넣는다" 였다. 그 3.5 단계가 담아야 하는 것을 감독 설계가 표로 적어두었다 — 셀 격자, 터미널 모드 상태, 커서 위치, 스크롤 영역.

**이것은 가상 터미널 에뮬레이터를 쓰는 일이다.** 감독 설계 원칙 9("감독은 세션 안을 모른다")가 정확히 금지하던 것이기도 하다 — 링이 화면 모델이 아니라 바이트 링인 것이 그 원칙의 직접적 결과라고 그 문서가 적어두었다. 3.5 단계는 그 원칙을 감독에서 걷어내는 작업이다.

크기를 가늠해보면 이렇다.

| 만들어야 하는 것 | 왜 무거운가 |
|---|---|
| CSI · OSC · DCS · SS2/SS3 파서 | 지금 링이 가진 것은 "지금 시퀀스 안인가" 만 아는 상태 기계다. 각 시퀀스가 무슨 뜻인지는 몰라도 됐는데, 이제 전부 알아야 한다 |
| 셀 격자와 속성 | 문자·전경색·배경색·굵기·밑줄·역상, 그리고 폭이 2 인 문자와 결합 문자 |
| 모드 상태 | 대체 화면, 괄호 붙여넣기, 포커스 보고, 커서 보이기, modifyOtherKeys, kitty 키보드 프로토콜 — 재접속에 되세워 보내야 하는 것 전부 |
| 스크롤 영역·커서·탭 정지 | 재생 뒤 이어지는 실시간 출력이 맞는 자리에 찍히도록 |
| 그리고 이 전부의 시험 | 실측을 시험으로 남긴 자리가 이미 있다(`internal/session/supervise_reattach_gate_test.go`). 늘어나는 것은 그 시험의 범위다 |

**그리고 이 조각을 만들어도 얻는 것은 "tmux 만큼" 이다.** 감독 설계 1.3 이 스스로 적어두었다 — *"이 작업의 성패는 '되는가' 가 아니라 '**tmux 만큼 되는가**' 에 달려 있다."* 즉 가장 무거운 조각의 보상이 새 기능이 아니라 **이미 되던 것을 남의 프로그램 없이 되게 하는 것**이다. 그 보상은 진짜지만(설치 요구·윈도우·detach 키), 조각의 무게에 비하면 얇다.

### 1.3 "CLI 를 버리면?" 이라는 물음이 연 것

사용자가 물은 것은 CLI 를 버리는 것이었고, 논의가 닿은 곳은 그보다 좁다 — **GUI 에서만 세션 계층을 바꾼다.**

그 물음이 드러낸 사실이 하나 있다. **앱은 이미 완성된 화면 모델을 갖고 있다.**

앱 안의 터미널은 JediTerm 위젯이다(`desktop/src/main/kotlin/com/kyuchestration/desktop/terminal/pty/KyuAttachTtyConnector.kt:18`). JediTerm 은 셀 격자와 모드 상태를 쥔 진짜 터미널 에뮬레이터이고, 3.5 단계가 만들려던 것을 이미 전부 갖고 있다. 지금 그 위젯은 PTY 안에서 `kyu attach` 를 실행하고, `kyu attach` 는 감독에게 붙어 **재생된 바이트**를 받는다.

즉 지금 구조는 이렇다.

```
[JediTerm — 화면 모델 있음]  ←PTY←  [kyu attach]  ←소켓←  [감독 — 화면 모델 없음]  ←PTY←  [claude]
                                                              ↑
                                                 3.5 단계가 채우려던 자리
```

**화면 모델이 두 번 필요한 것이 아니라, 없는 쪽을 거치고 있는 것이다.** 가운데 두 홉을 걷어내면 이렇게 된다.

```
[JediTerm — 화면 모델 있음]  ←PTY←  [claude]
```

재접속·재생이라는 문제가 사라진다. 문제가 풀리는 것이 아니라 **성립하지 않는다** — 붙었다 떼는 일이 없으므로 재생할 것이 없다. 위젯이 살아있는 동안 화면 모델도 살아있고, 그 위젯은 앱이 사는 동안 산다.

### 1.4 무엇을 잃는가 — 원칙 5 개정

**여기가 이 문서에서 가장 중요한 절이다.** 이 전환은 선행 문서의 설계 원칙 하나를 정면으로 건드린다.

> 5. **도구가 죽어도 작업이 안 끊긴다** — 세션의 생명주기는 도구와 독립적이다.

감독 설계 3.3 이 "GUI 가 세션을 보유" 하는 안을 기각한 첫 사유가 이 원칙이었다. *"앱을 끄면 세션이 죽는다. README 임베디드 터미널 절이 '터미널 닫기·다른 카드로 이동·앱 종료 — 전부 kill 이 아니라 detach 다' 라고 약속한 것이 그 자리에서 거짓이 된다."*

**그 기각을 뒤집는다.** 뒤집는 근거는 "원칙이 틀렸다" 가 아니라 **원칙이 지키려던 것이 프로세스의 생존이 아니었다** 는 것이다. 원칙의 이름은 "세션이 안 죽는다" 가 아니라 "**작업**이 안 끊긴다" 다. 세션 프로세스의 생존은 그것을 이루는 한 가지 수단이었고, `claude --resume` 이 다른 수단을 준다 — 프로세스가 아니라 **대화 기록**을 복구한다.

**개정된 원칙 5**

> 5. **도구가 죽어도 대화가 안 끊긴다** — CLI 세션의 생명주기는 도구와 독립적이다(tmux · 감독). GUI 세션은 앱과 생사를 같이하고, 대신 앱이 다시 뜰 때 `claude --resume` 으로 같은 대화를 이어간다.

**그래서 정확히 무엇을 잃는지 적어둔다.** 이 표를 흐리면 이 결정이 나중에 근거 없이 되뇌어진다.

| 잃는 것 | 크기 |
|---|---|
| **진행 중인 턴** | 앱을 끄는 순간 `claude` 가 쓰던 답이 사라진다. 도구 호출이 돌던 중이면 그 결과도. 다시 열면 그 턴이 시작되기 전 자리다 |
| **세션 안에서 돌던 오래 걸리는 명령** | 세션 안에서 빌드나 테스트를 돌리던 중 앱을 끄면 함께 죽는다 |
| **앱 크래시 = 세션 죽음** | tmux 였으면 살아남았다. 반경이 창 하나에서 앱 하나로 넓어진다 |
| **원격에서 이어 붙기** | 다른 터미널·다른 머신에서 그 세션에 붙는 길이 GUI 세션에는 없다. (CLI 세션에는 그대로 있다) |

**잃지 않는 것도 함께 적는다.** 잃는 것을 과장해도 결정이 흐려진다.

| 남는 것 | 왜 |
|---|---|
| **이미 한 일** | 파일 수정은 디스크에 있다. 커밋도 마찬가지다. 잃는 것은 "지금 하던 말" 이지 "이미 한 일" 이 아니다 |
| **대화 전체** | `claude` 가 전사를 디스크에 남기고 `--resume` 이 그것을 읽는다(부록 A) |
| **CLI 의 원칙 5** | `kyu start` · `kyu attach` 는 그대로 tmux(또는 감독) 위에 있다. 터미널 창을 닫아도 세션은 산다 |

**감독 설계 3.3 의 나머지 두 기각 사유는 이번 안에 걸리지 않는다.**

| 그때의 기각 사유 | 이번 안에서 |
|---|---|
| "CLI 만 쓰는 사람은 GUI 를 띄워야 세션을 가지게 된다" | **CLI 를 건드리지 않는다.** `kyu start`·`attach`·`kill` 은 그대로다. 대가는 두 세계가 갈리는 것이고, 그 대가를 5.4 가 정직하게 표시한다 |
| "표시 계층이 핵심 자산을 소유하게 된다 — 3층 구조가 뒤집힌다" | **앱이 소유하는 것은 세션(PTY)이지 조율 계층이 아니다.** 조립 지식은 엔진에 남는다(5.2). 뒤집히는 것은 3층 구조가 아니라 "세션 계층은 표시 계층과 무관하다" 는 전제 하나다(5.1) |

---

## 2. 핵심 문제 — 화면 모델은 누가 쥐는가

감독 설계 2절이 *"누군가는 PTY 마스터를 쥐고 있어야 한다"* 였다. 이번 문서의 같은 자리는 이렇다.

**떼었다 다시 붙는 구조는 화면 모델을 반드시 요구한다.** 터미널 출력은 문자와 제어 시퀀스가 섞인 스트림이고, 화면의 최종 모습은 그 스트림을 **처음부터** 해석한 결과다. 뒤쪽 일부만 다시 보내는 것은 "중간부터 해석을 시작하는 일" 이고, 그것은 화면 복원이 아니다. 이 결론은 우회 가능한 종류가 아니다 — 링을 키우는 것은 감기는 시점을 미룰 뿐이다(감독 설계 5.7.1).

그러므로 답은 둘 중 하나다.

1. **떼었다 붙는 구조를 유지하고, 화면 모델을 만든다** — 감독 3.5 단계
2. **떼었다 붙지 않는 구조로 바꾸고, 이미 있는 화면 모델을 쓴다** — 이 문서

2 번을 고르면 화면 모델이 필요 없어지는 것이 아니라 **이미 있는 것으로 충분해진다.** JediTerm 위젯이 그것이다.

그리고 2 번은 세션 프로세스의 생존을 내놓는다. 그 자리를 무엇으로 채우느냐가 이 설계의 두 번째 질문이고, 답은 **`claude --resume`** 이다(5.5).

---

## 3. 검토한 대안과 기각 사유

이 절은 **다른 세션에서 "이거 그냥 X 하면 되는 거 아냐?" 를 다시 묻지 않기 위한 기록**이다(선행 문서 3절 · 감독 설계 3절과 같은 목적).

### 3.1 감독 안에 화면 모델을 만든다 (3.5 단계 완수)

관문이 정해둔 원래의 길. 감독이 셀 격자와 모드 상태를 쥐고, 붙는 순간 재생 대신 현재 화면을 그려 보낸다.

**중단 사유 (1) — 이 작업에서 제일 무거운 조각이고, 보상이 "tmux 만큼" 이다.** 1.2 의 표가 그 크기이고, 1.2 의 마지막 문단이 그 보상이다.

**중단 사유 (2) — GUI 에는 이미 화면 모델이 있다.** 만들면 앱 하나에 화면 모델이 둘이 되고, 그 둘 사이를 바이트가 두 번 오간다(1.3 의 그림). 같은 것을 두 번 갖는 구조에 가장 무거운 조각을 투자하는 것이 이 시점의 판단으로 맞지 않다.

**중단 사유 (3) — 사용자 결정 (2026-08-31).** 방향 자체가 바뀌었다.

**지우지 않는다.** 3.5 단계는 **착수 전 중단**이고 기각이 아니다. 감독을 CLI 기본값으로 올리려면 여전히 이 조각이 필요하고, 그 필요는 사라지지 않았다 — 다만 GUI 가 그것을 기다리지 않게 됐으므로 우선순위가 내려갔다(6절).

### 3.2 GUI 만 전환하고 CLI 감독은 계속 발전시킨다 (하이브리드)

GUI 는 이 문서대로 가고, CLI 쪽은 3.5 단계를 마저 만들어 감독을 기본값으로 올리는 안.

**부분 채택.** 앞쪽 절반(GUI 전환)이 곧 이 문서다. 갈리는 것은 뒤쪽 절반이다.

**뒤쪽 절반을 지금 하지 않는 이유**: 3.5 단계를 지금 하면 이 문서의 네 단계(7절)와 경쟁한다. 그리고 그 순서로 하면 GUI 가 전환된 뒤에 CLI 화면 모델을 만들게 되는데, **그때는 그 조각이 정말 필요한지를 실제 사용이 답해준다** — 앱을 쓰는 사람이 늘면 CLI 세션의 비중이 줄고, 줄지 않으면 그 자체가 근거다. 근거를 얻고 나서 가장 무거운 조각에 착수하는 편이 낫다.

**그래서 감독은 동결이지 폐기가 아니다.** `KYU_SESSION_BACKEND=supervisor` 는 계속 동작하고, 동등성 매트릭스가 CI 에서 계속 돈다(6절).

### 3.3 앱이 감독 소켓에 직접 붙는다

`kyu attach` 프로세스를 거치지 않고 앱이 감독의 유닉스 소켓에 직접 연결해 attach 프레임을 주고받는 안. `kyu attach` 라는 한 홉이 사라진다.

**기각 사유**: **문제의 자리를 건드리지 않는다.** 홉이 하나 줄어도 여전히 감독이 화면 모델 없이 바이트를 재생하고, 관문이 막은 그 자리가 그대로다(1.3 의 그림에서 가운데 상자는 그대로 남는다).

그리고 대가가 크다. 감독 프로토콜(감독 설계 5.5·5.6)의 클라이언트가 Go 와 코틀린 두 벌이 되고, 프로토콜 판이 바뀔 때마다 두 곳이 함께 바뀌어야 한다. 감독 설계가 *"클라이언트와 감독의 프로토콜 코드가 같은 패키지에 있어 어긋날 자리가 없다"* 를 이점으로 세었는데, 이 안이 그것을 정확히 무너뜨린다.

### 3.4 앱이 `claude` 를 기계 프로토콜로 몰고 화면을 직접 그린다

터미널 에뮬레이터를 아예 버리고, `claude --print --output-format=stream-json` 으로 몰면서 앱이 대화 UI 를 Compose 로 그리는 안.

**기각 사유 (1)**: **선행 문서 원칙 3("세션 내부에 개입하지 않는다") 정면 위반이다.** 도구가 세션의 입출력을 해석하기 시작하면 세션이 무엇을 하는지가 도구의 관심사가 된다.

**기각 사유 (2)**: `claude` 의 대화형 UI 를 앱이 다시 만들게 된다 — 권한 확인 프롬프트, 계획 모드, 슬래시 명령, 파일 선택기, 진행 표시. 그리고 `claude` 가 그것을 고칠 때마다 앱이 따라가야 한다. **이 도구가 `claude` 를 따라잡는 경주에 들어간다.**

**기각 사유 (3)**: 이 도구의 가치는 조율이지 대화 UI 가 아니다. README 가 *"각 레포에서 세션을 따로 띄워야 그 레포의 설정이 살아난다"* 를 이 도구의 존재 이유로 적어둔 그 자리에 대화 UI 는 없다.

### 3.5 앱을 백그라운드에 남긴다 (창을 닫아도 프로세스가 산다)

앱이 세션을 보유하되, 창을 닫아도 앱 프로세스는 트레이에 남아 세션을 계속 붙잡는 안. 원칙 5 를 원래 뜻대로 지킬 수 있다.

**기각 사유 (1)**: **감독 설계가 기각한 중앙 데몬(3.1)을 GUI 로 되살린다.** 앱 하나가 모든 세션의 PTY 를 쥐므로 크래시 반경이 다시 전부가 된다. 감독을 세션별로 쪼갠 판단을 정면으로 되돌린다.

**기각 사유 (2)**: **사용자가 "껐다" 고 믿은 것이 안 꺼진다.** 창을 닫는 것과 프로그램을 끝내는 것이 갈리고, 그 차이를 플랫폼마다 다르게(맥의 독, 리눅스의 트레이) 설명해야 한다.

**기각 사유 (3)**: 자동 종료 정책이라는 숫자가 돌아온다 — 마지막 세션이 끝나면 앱은 언제 죽는가. 감독 설계가 세션별 감독을 고르며 없앤 질문이 그대로 되돌아온다(감독 설계 10절의 "확정으로 해소된 것 넷").

**남는 것**: 앱을 끄기 전에 "돌고 있는 세션 N 개가 끊깁니다" 를 확인받는 것은 이 기각과 무관하게 필요하다. 8절 열린 질문 4 번.

---

## 4. 설계 원칙 — 개정 하나, 추가 둘

선행 문서 4절의 여섯 원칙 중 **다섯 번째를 개정**한다(1.4). 나머지 다섯은 그대로다. 감독 설계가 더한 원칙 7~10 은 감독의 것이라 이 문서가 상속하지 않는다.

**개정**

> 5. **도구가 죽어도 대화가 안 끊긴다** — CLI 세션의 생명주기는 도구와 독립적이다. GUI 세션은 앱과 생사를 같이하고, 앱이 다시 뜰 때 `claude --resume` 으로 같은 대화를 이어간다. 잃는 것은 진행 중인 턴이고, 잃지 않는 것은 이미 한 일과 대화 전체다.

**추가**

11. **조립 지식은 엔진에 하나만 둔다** — 세션이 무슨 명령을 어느 디렉토리에서 어떤 환경으로 실행하는가는 엔진이 답한다. 앱은 그 답을 실행할 뿐 스스로 조립하지 않는다. README 가 이미 같은 자세를 조율 계층에 대해 적어두었다 — *"워크디렉토리 스캔·상태 추론·계획 파싱을 코틀린으로 한 번 더 옮기면 두 구현이 조용히 갈라진다."* 그 문장이 이제 세션 조립에도 적용된다.

12. **세션 계층이 표시 계층마다 갈리는 것을 감춘다면 그것이 결함이다** — CLI 세션과 GUI 세션은 서로를 보지 못한다. 이 사실을 화면에서 숨기면 사용자는 `RUNNING` 카드를 눌렀는데 새 대화가 열리는 것으로 그것을 알게 된다. **갈린다는 것을 화면이 먼저 말한다**(5.4).

---

## 5. 아키텍처

### 5.1 배치

선행 문서 5.1 의 3층 구조는 그대로다. **바뀌는 것은 "세션 계층은 표시 계층과 무관하다" 는 전제 하나다.**

```
┌──────────────────────────┬──────────────────────────────────────┐
│ [표시 계층] CLI           │ [표시 계층] 데스크톱 앱               │
├──────────────────────────┴──────────────────────────────────────┤
│ [조율 계층]  WorkDir 스캔 · plan 파싱 · 상태 추론 · 세션 조립     │  ← 안 바뀐다. 앱은 kyu 로 부른다
├──────────────────────────┬──────────────────────────────────────┤
│ [세션 계층] SessionBackend│ [세션 계층] 앱이 보유                 │
│   ├ TmuxBackend (기본)    │   pty4j PTY + JediTerm 위젯          │
│   └ SupervisorBackend     │   └ claude (직접 실행)               │
│       └ claude            │                                      │
└──────────────────────────┴──────────────────────────────────────┘
```

**조율 계층은 여전히 한 벌이고 여전히 엔진 안에 있다.** 이 도구의 고유 가치가 사는 곳이 그대로 남는 것이 이 배치를 받아들일 수 있는 이유다(선행 문서 5.1 — *"이 배치의 목적은 조율 계층을 두 번 쓰지 않는 것이다"*).

**갈라지는 것은 세션 계층뿐이고, 그것이 두 번째 갈래다.** 첫 번째 갈래는 플랫폼이었다(tmux / 감독 / 윈도우). 이제 표시 계층이 두 번째 축이 된다. 선행 문서 5.1 이 세션 계층을 "유일한 플랫폼 의존부" 라고 불렀는데, 이제 **"플랫폼과 표시 계층 양쪽에 의존하는 유일한 자리"** 다. 축이 하나 늘어난 것을 적어둔다 — 늘어난 축을 감추면 나중에 "왜 GUI 만 다르지" 를 코드에서 알아내야 한다.

**`SessionBackend` 인터페이스는 바뀌지 않는다.** 앱은 그 인터페이스를 구현하지 않는다 — 앱의 세션은 Go 프로세스 밖에 있고, 엔진이 그것을 만들거나 죽이거나 셀 수단이 없다. **인터페이스를 코틀린으로 한 번 더 구현하지 않는 것이 이 설계의 결정이다.** 구현하려면 앱이 `Create`·`Kill`·`List`·`IsAlive` 를 엔진에게 답해줄 통로가 필요하고, 그 통로는 앱이 죽는 순간 거짓말을 시작한다.

### 5.2 조립 지식은 엔진에 남는다 — `kyu session-command`

**문제**: 메인 세션의 명령은 `claude --add-dir <레포 절대경로>...` 이고, 그 레포 목록은 스캔 결과에서 온다(`internal/cli/start.go:147`). 레포 세션의 명령은 `claude` 이고 cwd 가 레포 디렉토리다(`internal/cli/start.go:171`). `--bypass-permissions` 는 `--dangerously-skip-permissions` 로 옮겨지고(`internal/cli/start.go:34`), `--repo-claude-md` 는 환경변수 하나로 옮겨진다(`internal/cli/start.go:40`).

**이 지식이 전부 `internal/cli/start.go` 안에 있고, 앱이 세션을 직접 띄우면 앱도 이것을 알아야 한다.** 두 곳에 두면 갈라진다.

**결정: 엔진이 답하고 앱은 실행한다.**

```
kyu session-command [repo] [--bypass-permissions] [--repo-claude-md] --json
```

```json
{
  "schemaVersion": 1,
  "command": ["claude", "--session-id", "d1b9d634-a927-4abf-ad80-5941671b08a8",
              "--add-dir", "/home/me/work/WorkDir-featureX/proj-a",
              "--add-dir", "/home/me/work/WorkDir-featureX/proj-b"],
  "cwd": "/home/me/work/WorkDir-featureX",
  "env": { "CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD": "1" }
}
```

**`env` 는 더할 것만 담는다.** 전체 환경을 답하지 않는다 — 엔진은 앱이 자식에게 물려줄 바탕 환경을 모르고(`desktop/.../platform/ChildProcessEnvironment.kt` 가 정한다), 엔진의 환경을 그대로 답하면 그것이 앱의 환경을 덮어쓴다. 앱은 자기 바탕 환경에 이 항목을 얹는다.

**`env` 가 짝이 아니라 객체인 것이 CLI 쪽의 제약을 물려받지 않는다는 뜻이다.** CLI 는 `SessionBackend.Create` 에 환경 인자가 없어서 명령을 `env KEY=VALUE ...` 로 감싼다(`internal/cli/start.go:47`). 그 수법은 인터페이스를 좁게 유지하려고 고른 것이고, 앱은 프로세스를 직접 띄우므로 그 제약 아래 있지 않다. **같은 사실을 두 표현으로 답하지 않는다** — 이 표면은 환경을 환경으로 답한다.

**답에 담지 않는 것**

| 담지 않는 것 | 왜 |
|---|---|
| 세션 이름 (`kyu-<workdir>-<repo>`) | 앱은 세션 이름으로 아무것도 하지 않는다. 앱 세션은 엔진이 아는 세션이 아니다(5.4) |
| 라벨 (`main` · 레포 이름) | 앱이 물을 때 이미 알고 있던 것이다 |
| 세션 생존 여부 | `kyu list --json` 이 답하는 사실이고, 여기서 또 답하면 두 문서가 어긋날 자리가 생긴다 |

**`--json` 없이 부르면 사용법 안내와 함께 종료한다.** 다른 기계용 명령들과 달리 이 명령에는 사람용 흐름이 없다 — 답이 "사람이 읽고 무엇을 할 것" 이 아니라 "기계가 실행할 것" 이기 때문이다. 사람이 볼 필요(앱이 무엇을 띄우는지 확인)가 실제로 생기면 그때 사람용 출력을 더한다.

**이름을 `session-command` 로 두는 이유**: 답이 명령만이 아니라 cwd 와 환경까지라서 `session-plan` 이 더 정확해 보인다. 쓰지 않는다 — **`plan` 은 이 도구에서 이미 `.coord/plan.md` 의 것이다.** 조율 계획과 세션 진입 계획이 같은 낱말을 쓰면 `kyu list` 의 `planWarnings` 와 이 명령이 한 화면에서 만날 때 사람이 한 번 멈칫한다. 명령이 답하는 것 중 가장 큰 것이 명령이므로 그것을 이름으로 쓴다.

#### 5.2.1 검토한 다른 안

**(가) 앱이 코틀린으로 조립 규칙을 다시 구현한다**

**기각.** 원칙 11 이 정확히 이것을 막는다. 그리고 `--repo-claude-md` 가 환경변수 하나로 옮겨지는 것 같은 규칙은 이 도구의 판단이라 조용히 갈라진다 — 갈라진 것을 발견하는 자리가 "메인 세션이 레포 지침을 안 읽는다" 이고, 그때 어느 쪽이 틀렸는지 알아내는 데 오래 걸린다.

**(나) `kyu start --exec` — kyu 가 자기 프로세스를 `claude` 로 갈아치운다**

앱이 PTY 에서 `kyu start --exec main` 을 실행하면 `kyu` 가 조립한 뒤 `syscall.Exec` 으로 자기를 `claude` 로 갈아치운다. 조립 지식이 엔진에 남고, 앱이 아는 명령은 하나뿐이고, 홉도 남지 않는다(exec 는 프로세스를 대체하므로 껍데기가 안 남는다).

**기각 사유 — 윈도우에 `exec` 가 없다.** Go 의 `syscall.Exec` 은 유닉스 계열에서만 동작하고, 윈도우에는 프로세스를 제자리에서 갈아치우는 호출이 없다. **이 전환의 큰 보상 중 하나가 윈도우 네이티브인데(5.7), 그 자리에서 정확히 무너지는 안이다.** 윈도우에서만 다른 길을 두면 "플랫폼 의존을 한 겹에 가둔다"(선행 문서 원칙 4)가 앱 쪽에서 깨진다.

부차적으로, exec 이면 답을 앱이 볼 수 없다. 앱이 무엇을 띄웠는지 기록하거나 화면에 보일 길이 없고, 실패했을 때 그 이유가 터미널 화면에만 남는다.

**(다) `kyu attach` 를 그대로 두고 백엔드만 "앱 보유" 로 하나 더 만든다**

`SessionBackend` 구현을 하나 더 만들어 앱이 그것을 쓴다. **기각** — 5.1 이 적은 대로, 앱의 세션은 Go 프로세스 밖에 있어서 엔진이 `IsAlive` 를 답할 수단이 없다. 답할 수 없는 계약을 채우는 구현은 거짓말을 하게 된다.

### 5.3 앱이 세션을 여는 길

**지금**

```
카드 클릭
  → ProcessBuilder(kyu start [repo])           비 PTY, 끝날 때까지 기다림
  → PtyProcessBuilder(kyu attach <label>)      PTY, 살아 있음
  → KyuAttachTtyConnector → JediTerm
```

`desktop/.../terminal/pty/PtyKyuSessionTerminalAttacher.kt:49`·`50` 의 두 줄이 그것이다.

**바뀐 뒤**

```
카드 클릭
  → ProcessBuilder(kyu session-command [repo] --json)   비 PTY, 끝날 때까지 기다림 → 문서 하나
  → PtyProcessBuilder(문서의 command, cwd, 바탕환경 + 문서의 env)   PTY, 살아 있음
  → KyuSessionTtyConnector → JediTerm
```

**모양이 같다.** 앞의 비 PTY 호출 하나와 뒤의 PTY 호출 하나라는 배치가 그대로이고, 앞 호출이 "세션을 띄운다" 에서 "무엇을 띄울지 묻는다" 로 바뀔 뿐이다. 그래서 `PtyKyuSessionTerminalAttacher` 의 뼈대와 실패 타입(`TerminalSessionFailure`)이 거의 그대로 남는다.

**`SessionEntryPlan` 은 타입 모양 그대로 살고 채우는 출처만 바뀐다.** 지금은 `planSessionEntry`(`desktop/.../terminal/SessionEntryPlan.kt:32`)가 코틀린 안에서 조립하고, 바뀐 뒤에는 `kyu session-command` 의 답을 옮긴다. 담는 것이 이미 명령·cwd·환경 셋이라(`SessionEntryPlan.kt:12`) 문서와 모양이 맞는다.

**`startCommand` 필드는 사라진다.** "붙기 전에 먼저 띄운다" 가 없어지므로 계획에 남길 것이 없다.

**두 가지 손질은 남는다.**

| 지금 | 바뀐 뒤 | 왜 |
|---|---|---|
| `TERM=xterm-256color` 로 덮어쓴다 (`SessionEntryPlan.kt:67`) | **그대로 유지** | JediTerm 이 흉내 내는 것이 xterm 계열이라는 사실은 백엔드와 무관하다. 오히려 이제 `claude` 가 이 값을 직접 읽으므로 더 중요해진다 |
| `TMUX` 를 지운다 (`SessionEntryPlan.kt:63`) | **그대로 유지** | 이유가 바뀐다. 지금은 `kyu attach` 가 중첩으로 거절하기 때문이고, 바뀐 뒤에는 `claude` 가 그 값을 보고 다르게 굴 수 있기 때문이다(`claude --help` 에 `--tmux` 가 있다). **앱 안 PTY 는 tmux 안이 아니라는 사실은 어느 쪽이든 참이다** |

`KYU_SESSION`(감독의 중첩 표식)도 함께 지운다 — 같은 이유이고, 앱을 감독 세션 안에서 띄웠을 때 물려받는다.

**초기 크기 상수(`PtyKyuSessionTerminalAttacher.kt:101`·`102`)는 그대로다.** JediTerm 이 자리를 잡으면 진짜 크기로 다시 부르는 것도 그대로이고, 이제 그 `resize` 한 줄이 tmux 나 감독을 거치지 않고 `claude` 에게 바로 닿는다 — **홉이 둘 줄어든다**(`KyuAttachTtyConnector.kt:28` 의 주석이 그만큼 짧아진다).

### 5.4 세션 상태 — 두 곳에서 오는 사실을 합친다

**문제**: `kyu list --json` 의 `state` 는 세션 백엔드에게 물어 정해진다(선행 문서 5.3 의 상태 추론 표). 앱이 보유한 세션은 tmux 세션도 감독 세션도 아니므로 **엔진 눈에 보이지 않는다.** 그대로 두면 앱에서 세션을 열어 작업하는 동안 그 카드가 `IDLE` 이나 `DIRTY` 로 보인다.

**결정: 앱이 자기 사실을 엔진의 사실 위에 얹는다.**

| 카드가 보여줄 것 | 어디서 오는가 |
|---|---|
| **앱이 이 세션을 보유 중** | 앱 자신 — `EmbeddedTerminalStateHolder` 의 상태 |
| **CLI 세션이 떠 있음** | `kyu list --json` 의 `state == "RUNNING"` |
| **git 상태** (`DIRTY` · `AHEAD` · `IDLE`) | `kyu list --json` 의 `state` — CLI 세션이 없을 때만 실려 온다 |

**`kyu list --json` 의 계약(schemaVersion 1)은 바뀌지 않는다.** 필드도 뜻도 그대로다. 앱은 이제 그 문서를 "레포 상태 그 자체" 가 아니라 **"엔진이 본 것"** 으로 읽고, 자기가 본 것과 합친다.

**엔진이 앱 세션을 보게 만들지 않는 이유**: 선행 문서 원칙 6 이 *"상태는 관찰해서 얻는다 — 세션이 상태를 보고하도록 요구하지 않는다"* 다. 앱이 자기 세션 목록을 파일에 적고 엔진이 그것을 읽게 하면, 그 파일은 **선언된 상태**가 되어 원칙 6 을 어긴다. 그리고 앱이 크래시하면 그 파일은 그 순간부터 거짓말을 한다 — 죽은 세션을 `RUNNING` 이라고 말하고, 그것을 고칠 사람은 아무도 없다.

#### 5.4.1 얻는 것 하나 — 두 사실을 동시에 알게 된다

지금은 `state` 가 값 하나라 세션이 떠 있으면 git 상태가 가려진다. `RUNNING` 인 레포가 더러운지는 알 수 없다.

**앱 세션에서는 그 가림이 없다.** 엔진 눈에 세션이 없으므로 엔진은 git 상태를 답하고, 앱은 그 위에 자기 `RUNNING` 을 얹는다. 즉 **`RUNNING · DIRTY` 를 한 카드에 함께 보여줄 수 있다.** 지금보다 정보가 는다.

계약을 바꾸지 않고 얻는 것이라 이번 범위에 넣는다.

#### 5.4.2 정직하게 적어야 하는 표시 차이

**두 세계가 갈린다.** 원칙 12 가 요구하는 것이 이 표다.

| 사실 | 사용자가 알아야 하는 이유 |
|---|---|
| **앱에서 연 세션은 `kyu list` 에 안 보인다** | 터미널에서 `kyu list` 를 쳤을 때 앱에서 작업 중인 레포가 `DIRTY` 로 나온다. 틀린 것이 아니라 다른 것을 보고 있는 것이다 |
| **앱에서 연 세션은 `kyu kill` 로 못 죽인다** | 죽이는 길은 앱의 터미널을 닫거나 앱을 끄는 것뿐이다 |
| **CLI 로 띄운 세션에 앱이 붙지 못한다** | 앱은 더 이상 `kyu attach` 를 부르지 않는다. 그 카드를 누르면 **새 세션이 열린다** — 같은 레포에 `claude` 가 둘 돌게 된다 |
| **그래서 카드는 두 종류의 `RUNNING` 을 갈라 보여준다** | 갈라 보이지 않으면 위 줄을 사용자가 세션 두 개를 만들고 나서 알게 된다 |

**막지 않고 알린다.** CLI 세션이 떠 있는 레포의 카드를 눌러도 앱은 세션을 연다 — 막으면 사용자가 앱에서 할 수 있는 일이 없어지고(앱은 그 세션에 붙을 수도 죽일 수도 없다), 같은 레포에 `claude` 를 둘 띄우는 것 자체는 사용자가 실제로 원할 수 있는 일이다. **알리고 맡긴다.**

**대화 ID 는 다투지 않는다.** CLI 세션은 `kyu start` 가 띄우고, `kyu start` 는 `--session-id` 를 붙이지 않는다(5.5 의 마지막 문단). 두 세션은 서로 다른 대화를 갖는다.

### 5.5 대화 이어가기 — `--session-id` 와 `--resume`

앱이 세션을 잃는 자리(앱 종료·크래시)를 메우는 것이 이것이다. 개정된 원칙 5 의 뒷부분이 여기서 성립하거나 성립하지 않는다.

#### 5.5.1 확인된 사실 (2026-08-31 실측, `claude` 2.1.251)

| 사실 | 근거 |
|---|---|
| `claude --session-id <uuid>` — 대화의 ID 를 **부르는 쪽이 정할 수 있다** | `claude --help`: *"Use a specific session ID for the conversation (must be a valid UUID)"* |
| `claude --resume <uuid>` — 그 ID 의 대화를 이어간다 | `claude --help`: *"Resume a conversation by session ID"* |
| `claude --resume <없는 uuid>` 는 **즉시** 종료 코드 1 로 끝나고 `No conversation found with session ID: <uuid>` 를 stderr 에 낸다. 네트워크 왕복이 없다 | 실측 |
| 전사는 cwd 별로 갈린 디렉토리에 대화 ID 이름의 파일로 남는다 (`~/.claude/projects/<경로에서 유도한 이름>/<uuid>.jsonl`) | 실측 |
| `--fork-session` — 이어가면서 새 ID 를 받는 길이 따로 있다 | `claude --help` |

**첫 줄이 이 설계에서 가장 중요하다.** 대화 ID 를 **알아내는** 것이 아니라 **정해주는** 것이라, `claude` 가 자기 ID 를 어디에 어떻게 내놓는지 알아낼 필요가 없다. 알아내는 길이었다면 `claude` 의 내부 저장 형식에 기대게 되고, 그것은 계약이 아니라 구현 세부라 판이 바뀔 때 조용히 어긋난다.

**네 번째 줄(전사 경로)에는 기대지 않는다.** 그 경로는 `claude` 의 살림이지 약속이 아니다. 이 설계가 쓰는 것은 `--session-id` 와 `--resume` 두 플래그뿐이고, 전사가 어디 있는지는 몰라도 된다. 다만 그 경로가 cwd 별로 갈려 있다는 사실 하나는 설계에 영향을 준다 — **`--resume` 은 같은 cwd 에서 불러야 한다.** 이 도구의 세션은 cwd 가 세션마다 고정이므로 그 전제가 저절로 지켜진다.

#### 5.5.2 기록 — `.coord/conversations.json`

**대화 ID 를 어딘가 적어두어야 다음 실행이 그것을 이어갈 수 있다.**

```
WorkDir-featureX/
    .coord/
        plan.md                 # 사람이 쓰고 도구가 읽는다
        conversations.json      # 도구가 쓰고 도구가 읽는다  ← 새로 생기는 것
```

```json
{
  "schemaVersion": 1,
  "conversations": {
    "main": "d1b9d634-a927-4abf-ad80-5941671b08a8",
    "proj-a": "211f6974-88a8-4453-9248-a02b0d6febae"
  }
}
```

**키는 세션 라벨이다** — 사용자가 부르는 이름(`main` 또는 레포 이름)이고, `kyu attach`·`kyu kill` 이 받는 것과 같다. 세션 이름(`kyu-<workdir>-<repo>`)을 쓰지 않는 이유는 워크디렉토리 이름이 키 안에 또 들어가기 때문이다 — 파일이 이미 그 워크디렉토리 안에 있으므로 중복이다.

**왜 `.coord/` 인가**

| 근거 | 설명 |
|---|---|
| 워크디렉토리를 버리면 함께 사라진다 | 워크디렉토리는 작업이 끝나면 버리는 것이다(선행 문서 부록 B). 밖에 두면 버려진 워크디렉토리의 기록이 남아 쌓인다 — 감독 설계 10절 5번이 죽은 소켓에서 만난 것과 같은 종류의 문제다 |
| 워크디렉토리를 옮기면 함께 간다 | 다만 옮기면 대화는 어차피 끊긴다 — `claude` 의 전사가 cwd 별로 갈려 있어서다(5.5.1). 파일이 따라가는 것이 손해는 아니다 |
| CLI 도 같은 파일을 읽을 수 있다 | 나중에 CLI 세션이 같은 대화를 이어가는 길을 열 때 자리를 다시 정하지 않아도 된다 |
| 사람이 열어볼 수 있다 | 선행 문서 6.2 — *"디버깅이 곧 파일 열기다"* |

**`plan.md` 와 방향이 반대라는 것을 적어둔다.** `plan.md` 는 사람이 쓰고 도구가 읽는다. 이 파일은 도구가 쓰고 도구가 읽는다. 같은 디렉토리에 방향이 다른 두 파일이 사는 것이라, 사용자가 이 파일을 손으로 고칠 것으로 오해하지 않도록 `kyu init` 이 만드는 템플릿에는 넣지 않는다 — **필요할 때 생긴다.**

**레포 안에는 아무것도 넣지 않는다**(선행 문서 원칙 2). 이 파일은 워크디렉토리 것이지 레포 것이 아니다.

#### 5.5.3 흐름

```
kyu session-command proj-a --json
    │
    ├─ .coord/conversations.json 에 "proj-a" 기록이 있는가?
    │
    ├─ 없다 → 새 UUID 를 만들어 기록하고,  command 에 --session-id <새 UUID> 를 넣어 답한다
    │
    └─ 있다 → command 에 --resume <기록된 UUID> 를 넣어 답한다
```

**묻는 것이 곧 기록하는 것이다.** 조회처럼 보이는 명령에 부작용이 있으므로 짚어둔다. **UUID 를 만드는 것과 기록하는 것은 갈라둘 수 없다** — 만들어 놓고 기록하지 않으면 다음 물음이 다른 UUID 를 만들고, 그러면 이어갈 대화가 매번 새로 생겨 이 기능 전체가 성립하지 않는다.

**기록했는데 앱이 실행하지 않은 경우**(PTY 열기 실패 등)에는 그 ID 의 전사가 없는 채로 기록만 남는다. 다음 실행이 `--resume` 을 시도했다 실패하고, 그것을 아래의 폴백이 받는다. 해가 없다.

#### 5.5.4 resume 실패와 폴백

기록은 있는데 이어갈 수 없는 경우가 있다.

| 언제 | 왜 |
|---|---|
| 사용자가 `~/.claude` 를 정리했다 | 전사가 사라졌다 |
| 워크디렉토리를 다른 머신에서 열었다 | 전사는 그 머신에 있다 |
| `claude` 가 오래된 전사를 정리했다 | 보존 정책은 `claude` 의 것이다 |

**이때 `claude --resume` 은 즉시 종료 코드 1 로 끝난다**(5.5.1 실측). 앱 안에서는 PTY 가 곧바로 닫히고, 사용자가 보는 것은 한 줄 남기고 끝난 터미널이다.

**폴백**: 앱은 그 사실을 알아채고 기록을 버린 뒤 새 대화로 다시 연다.

**어떻게 알아채는가가 열린 질문이다**(8절 2번). 후보를 적어둔다.

| 후보 | 판단 |
|---|---|
| 답 문서에 `"resumedConversationId": "<uuid>"` 를 실어, 앱이 "resume 으로 연 세션이 끝났다" 를 가려낸다 | **추천.** 시간 숫자가 필요 없다. 다만 사용자가 정상으로 `/exit` 한 것과 구별되지 않는다 — 그 경우 새 대화가 한 번 열리는 것이 대가다 |
| PTY 가 아주 짧게 살고 끝났으면 실패로 본다 | 기각 쪽. "짧게" 라는 숫자가 하나 생기고, 느린 머신에서 그 숫자가 틀린다 |
| 엔진이 미리 판정한다 (전사가 있는지 본다) | 기각. 전사 경로는 `claude` 의 구현 세부라 계약이 아니다(5.5.1) |

**필드를 지금 넣지 않는다.** 계약 규칙상 필드를 더하는 변경은 `schemaVersion` 을 올리지 않으므로(`internal/cli/list_json.go:46` 의 판 규약), 3 단계에서 더해도 이미 도는 독자가 깨지지 않는다. **1 단계에 쓰지 않을 필드를 미리 넣지 않는다**(선행 문서의 YAGNI 자세).

기록을 버리는 통로도 3 단계에서 정한다 — 같은 표면의 옵션 하나(`--forget-conversation` 류)가 가장 가깝다.

#### 5.5.5 검토한 다른 안

**(가) UUID 를 유도한다 — 기록 파일이 없는 안**

워크디렉토리 절대경로와 세션 라벨에서 UUIDv5 로 결정적으로 만든다. 같은 자리는 항상 같은 ID 이므로 기록할 것이 없고, 잃을 것도 어긋날 것도 없다. 선행 문서 원칙 6("상태는 관찰해서 얻는다")에 가장 가까운 안이다.

**기각 사유 — "새 대화로 시작" 이 불가능해진다.** 대화가 길어져 새로 시작하고 싶은 것은 아주 흔한 요구인데, ID 가 결정적이면 새 ID 를 만들 수가 없다. 카운터를 붙이면 그 카운터를 어딘가 적어야 하고, 그러면 기록 파일로 되돌아온다 — **기록을 없애려고 고른 안이 기록을 다시 요구한다.**

부차적으로, 워크디렉토리를 지우고 같은 이름으로 다시 만들면 옛 대화가 되살아난다.

**(나) `claude --continue` 로 이어간다**

`-c` 는 "이 디렉토리의 가장 최근 대화" 를 이어간다. cwd 가 세션마다 고정이므로 이것도 되고, 기록 파일이 아예 필요 없다.

**기각 사유 (1)**: **"가장 최근" 이 우리 것이라는 보장이 없다.** 사용자가 같은 레포 디렉토리에서 터미널로 `claude` 를 따로 띄우면 그쪽이 가장 최근이 되고, 앱이 그 대화를 이어간다. 레포 디렉토리에서 `claude` 를 직접 띄우는 것은 이 도구를 쓰는 사람에게 아주 흔한 일이다.

**기각 사유 (2)**: 이어갈 대화가 하나도 없는 첫 실행에서 실패한다. 첫 실행인지를 앱이 갈라야 하고, 그 판정이 곧 기록이다 — (가)와 같은 결말이다.

**(다) `kyu start` 도 같은 표면을 쓰게 통일한다**

`kyu start` 가 `kyu session-command` 의 답을 그대로 써서 CLI 세션에도 `--session-id` 가 붙게 하는 안.

**이번 범위에서 제외.** CLI 세션은 detach 로 살아남으므로 이어갈 필요가 없다 — **resume 은 앱이 세션을 잃기 때문에 생긴 요구다.** 통일하면 CLI 세션과 앱 세션이 같은 대화 ID 를 다투게 되고(5.4.2 의 마지막 줄이 성립하지 않게 된다), 얻는 것은 없다.

**조립 지식은 그래도 갈라지지 않는다.** `kyu start` 와 `kyu session-command` 는 같은 패키지 안에서 같은 함수(`mainSessionCommand`·`claudeCommand`, `internal/cli/start.go:147`·`163`)를 부른다. 갈라질 자리는 애초에 프로세스 경계 너머였고, 이 표면이 그 경계를 메운다.

### 5.6 낱말 — "세션" 이 둘이다

`claude` 는 대화 하나를 "session" 이라 부르고(`--session-id`, `--resume`), 이 도구도 PTY 하나를 "세션" 이라 부른다. **같은 낱말이 다른 것을 가리킨다.**

| 이 문서의 낱말 | 가리키는 것 | `claude` 의 낱말 |
|---|---|---|
| **세션** | PTY 하나와 그 안의 프로그램. `kyu-<workdir>-<repo>` 로 이름 붙는다 | (해당 없음) |
| **대화** | 이어갈 수 있는 기록 하나. UUID 로 식별한다 | session |

**코드와 문서에서 `claude` 쪽은 "대화"(conversation) 로 쓴다.** 플래그 이름(`--session-id`)만 원어로 인용한다. 파일 이름을 `.coord/conversations.json` 으로 둔 것도 이 규칙을 따른 것이다.

### 5.7 tmux 의존이 GUI 에서 사라진다 — 윈도우가 가까워진다

**GUI 의 요구사항에서 tmux 가 즉시 빠진다.** 앱은 더 이상 `kyu attach` 를 부르지 않고, `kyu session-command`·`kyu list`·`kyu init`·`kyu clone` 중 세션 백엔드를 만지는 것은 `kyu list` 하나뿐이다.

**윈도우 네이티브의 벽이 낮아진다.** 지금 막고 있는 것을 `desktop/.../engine/githubrelease/EngineReleaseTarget.kt:7` 과 릴리스 워크플로가 적고 있다 — 바이너리가 아니라 tmux 다. 전환 뒤에 남는 것은 이렇다.

| 조각 | 윈도우에서 |
|---|---|
| 앱 (Compose Desktop) | JVM 이라 그대로 돈다 |
| PTY | **pty4j 가 ConPTY 를 쓴다** — 감독 설계 8절이 직접 만들어야 한다고 적은 자리를 라이브러리가 이미 채우고 있다 |
| JediTerm | 그대로 |
| `kyu session-command` | 스캔과 경로 조립뿐이라 **세션 백엔드를 만지지 않는다.** Go 크로스 컴파일 한 줄 |
| `kyu list` | ⚠️ `IsAlive` 를 부르므로 세션 백엔드가 필요하다 — **아래** |
| `kyu clone` · `kyu auth` · `kyu repos` | git 과 HTTP 다. 세션 계층과 무관 |

**남는 구멍 하나**: `kyu list` 는 레포마다 `IsAlive` 를 물어 `RUNNING` 을 판정한다. 윈도우에는 tmux 도 감독도 없으므로 답할 백엔드가 없다. **앱이 자기 세션 상태를 스스로 알므로(5.4) 그 답이 "세션 없음" 이어도 GUI 는 성립한다.** 그것을 "백엔드가 없으니 실패" 가 아니라 "이 머신에 CLI 세션이 없다" 로 답하는 자리가 필요하다. 4 단계에 넣고, 판단은 8절 5 번. **— PR #45 에서 (가) 로 메워졌다(7.0.1). 위 표의 ⚠️ 는 그 전까지의 사실이다.**

**이 절은 전망이지 계획이 아니다.** 윈도우는 별도 작업이고, 이 문서의 네 단계에 들어 있지 않다. 적어두는 이유는 이 전환의 보상 중 하나가 그것이기 때문이다.

---

## 6. 기존 자산의 처지

**"강등하되 지우지 않는다."** 감독 설계 원칙 7 을 감독 자신에게 적용한다.

| 자산 | 처지 | 근거 |
|---|---|---|
| **tmux 백엔드** (`internal/session/tmux.go`) | **CLI 기본값 그대로** | 검증되어 있고 CLI 의 원칙 5 를 떠받친다. 바꿀 이유가 이 전환에는 없다 |
| **감독 1~3 단계** (`internal/session/supervise*.go` · `supervisor*.go`) | **CLI 옵션으로 존치** — `KYU_SESSION_BACKEND=supervisor` | 아래 |
| **감독 3.5 단계** (화면 모델) | **착수 전 중단** | 3.1. 기각이 아니라 중단이고, 재개 조건은 3.2 |
| **감독 4 단계** (기본값 전환) | **3.5 단계에 걸린 채로** | 관문이 그대로 막고 있다 |
| **동등성 매트릭스** (`internal/session/backend_parity_test.go`) | **그대로 CI 에서 돈다** | 감독이 방치되어 썩지 않게 하는 것이 이 시험의 역할이다 |
| **`kyu attach`** (`internal/cli/attach.go`) | **CLI 전용으로 남는다** | 앱이 부르지 않게 될 뿐 CLI 에서는 그대로 필요하다 |
| **`DetachKey` 인터페이스**(`internal/session/backend.go:66`) | 그대로 | CLI 안내가 쓴다. 앱은 이제 detach 라는 개념 자체가 없다 |

**감독을 지우지 않는 근거**

1. **CLI 에는 여전히 그 요구가 있다.** 터미널 창은 앱과 달리 세션을 보유하지 않는다 — 창을 닫으면 그 안의 프로세스가 죽는다. tmux 없이 그것을 주는 길은 감독뿐이고, 그 요구는 이 전환으로 사라지지 않았다.
2. **1~3 단계는 완성되어 있고 시험이 있다.** 지우는 비용이 남기는 비용보다 크다.
3. **관문은 감독의 결함이 아니라 미완이다.** 3.5 단계가 들어가면 통과한다. 지우면 그 판단을 다시 할 수 없다.
4. **감독 설계 원칙 7 이 tmux 에 대해 적은 문장이 감독에도 그대로 적용된다** — *"검증된 폴백이 있는 것과 없는 것은 사용자가 감수하는 위험이 다르다."*

### 6.1 GUI 코드 — 남는 것과 버리는 것

| 자리 | 처지 |
|---|---|
| `desktop/.../terminal/pty/PtyKyuSessionTerminalAttacher.kt` | **뼈대 유지.** 두 번의 프로세스 실행이라는 배치가 같고(5.3), 실행하는 것이 바뀐다 |
| pty4j 의존 | **유지.** PTY 를 여는 일이 남는다 — 오히려 이제 이것이 세션 계층 자체다 |
| JediTerm 의존 | **유지.** 화면 모델이 여기 있다는 것이 이 전환의 근거다 |
| `desktop/.../terminal/SessionEntryPlan.kt` 의 타입 | **유지.** 담는 셋(명령·cwd·환경)이 문서와 맞는다 |
| `SessionEntryPlan.startCommand` | **삭제.** "붙기 전에 띄운다" 가 없어진다 |
| `planSessionEntry` 의 조립 로직 (`SessionEntryPlan.kt:40`~`54`) | **삭제.** 엔진의 답을 옮기는 코드로 대체된다 |
| `PtyKyuSessionTerminalAttacher.startSessionBeforeAttaching` (`:59`) | **대체.** `kyu start` 를 부르던 자리가 `kyu session-command --json` 을 부른다 |
| `KyuAttachTtyConnector` | **유지, 이름이 바뀐다.** 더 이상 `kyu attach` 를 감싸지 않는다. `resize` 는 그대로이고 주석(`:28`)이 짧아진다 |
| `EmbeddedTerminalStateHolder` | **유지.** 다만 클래스 주석(`:17`)의 *"전환이 상태를 잃지 않는 것은 tmux 가 보장하므로"* 가 거짓이 된다 — **한 번에 하나만 붙는다** 는 결정의 근거가 통째로 사라진다(8절 3번) |
| `EmbeddedTerminalStateHolder.closeTerminal` (`:73`) | **뜻이 바뀐다.** 지금은 detach 이고, 바뀐 뒤에는 **세션을 끝내는 일**이다. 그 자리에 kill 이 없는 것이 "빠뜨린 것이 아니" 라던 주석이 반대가 된다 |
| `WorkDirSnapshot.aliveSessionCount` (`:25`) | **뜻이 바뀐다.** 엔진이 본 세션만 센다. 앱 세션을 더하는 것은 화면 쪽에서 합친다(5.4) |
| `SessionTarget` | **유지.** `kyu` 가 메인과 레포를 가리키는 방식은 그대로다 |
| `TerminalSessionFailure` | **항목이 바뀐다.** `SessionStartRejected` 가 "세션 시작 거절" 에서 "무엇을 띄울지 묻는 데 실패" 로 |

---

## 7. 구현 단계

각 단계가 **PR 하나**이고, 각 단계 끝에 손으로 확인 가능한 상태가 된다(선행 문서 9.2 · 감독 설계 9절과 같은 자세).

| 단계 | 내용 | 완료 확인 |
|---|---|---|
| **1** | `kyu session-command [repo] --json` (5.2) — 조립을 `kyu start` 와 같은 함수에서 가져오고, `.coord/conversations.json` 을 읽고 쓰고(5.5.2), 새 대화면 `--session-id <새 UUID>` 를 넣어 답한다 | 워크디렉토리에서 `kyu session-command --json` 과 `kyu session-command proj-a --json` 이 각각 맞는 명령·cwd·환경을 답한다. `--repo-claude-md` 가 `env` 에 실려 나오고 `--bypass-permissions` 가 `command` 에 실려 나온다. 두 번 불러도 같은 UUID 를 답한다. `.coord/conversations.json` 이 생기고 사람이 읽을 수 있다 |
| **2** | 앱이 세션을 직접 보유한다 — `kyu attach` 경유 제거(5.3), 상태 병합(5.4), 카드가 두 종류의 `RUNNING` 을 가른다(5.4.2) | 아래 검증 항목 전부 |
| **3** | 대화 이어가기 — 기록이 있으면 `--resume` 으로 답하고(5.5.3), 실패 폴백을 앱이 받는다(5.5.4). 판정 기준을 이 단계에서 정한다 | 세션에서 대화를 몇 번 주고받고 앱을 껐다 켜서 카드를 누르면 **그 대화가 이어진다.** `.coord/conversations.json` 의 UUID 를 손으로 망가뜨리면 새 대화가 한 번 열리고 기록이 새 값으로 바뀐다 |
| **4** | 문서·요구사항 정리 — README 요구사항 표에서 tmux 를 GUI 요구에서 빼고, 임베디드 터미널 절의 "전부 detach 다" 를 고치고, 두 세계가 갈린다는 사실을 적는다. 윈도우에서 `kyu list` 가 백엔드 없이 답하는 자리(5.7) | tmux 가 설치되지 않은 머신에서 앱을 설치해 워크디렉토리를 열고, 카드를 눌러 작업하고, 앱을 껐다 켜서 이어간다. README 를 처음 읽는 사람이 "앱 세션은 `kyu list` 에 안 보인다" 를 문서에서 먼저 안다 |

**1 단계가 먼저인 이유**: 이 작업에서 가장 많은 판단이 모인 곳이 조립 표면이고, 그것은 CLI 만으로 눈으로 확인할 수 있다. 앱을 고치기 전에 답이 맞는지 확정하면, 2 단계에서 틀린 것을 찾을 때 "앱이 잘못 읽었나 엔진이 잘못 답했나" 를 가를 필요가 없다.

**1 단계가 `--session-id` 까지 포함하는 이유**: 3 단계를 기다리면 2 단계가 머지된 뒤 3 단계 전까지 **대화가 기록조차 되지 않는 창**이 생긴다. 그 사이에 앱을 껐다 켜면 이어갈 것이 아예 없다. 1 단계가 처음부터 ID 를 붙여두면 그 창에서도 전사가 남고, 3 단계는 "이어가기" 만 더한다.

### 7.0 어디까지 왔나 (2026-08-31 추가)

**네 단계가 전부 머지됐다.**

| 단계 | PR | 처지 |
|---|---|---|
| **1** — `kyu session-command` | #41 | 완료 |
| **2** — 앱이 세션을 보유 | #42 | 완료. 7.1 의 검증 항목을 그 PR 이 지났다 |
| **3** — 대화 이어가기 | #43 | 완료. `.coord/conversations.json` 과 `--resume` 폴백까지 |
| **4** — 문서·요구사항 정리 | #44 · #45 | 완료. 문서가 #44, *"`kyu list` 가 백엔드 없이 답하는 자리"* 가 #45 |

**4 단계는 PR 두 개로 나뉘었다.** 4 단계 칸에는 문서 정리와 함께 *"윈도우에서 `kyu list` 가 백엔드 없이 답하는 자리(5.7)"* 가 적혀 있는데, 그것은 코드 변경이라 문서 PR 에 넣지 않았다. #44 가 머지된 뒤 한동안 이 자리가 비어 있었고, 그동안 4 단계의 완료 확인 첫 줄 — *"tmux 가 설치되지 않은 머신에서 앱을 설치해 워크디렉토리를 열고"* — 은 통과하지 못했다. `PATH` 에서 tmux 를 빼고 `kyu list <워크디렉토리> --json` 을 부르면 `tmux 를 찾을 수 없습니다` 와 종료 코드 1 로 끝났고, 대시보드는 그것을 3 초마다 부르므로 tmux 없는 머신에서는 목록이 서 있었다.

**#45 가 8절 5 번의 (가) 를 그대로 채택해 그 자리를 메웠다.** 세션 백엔드를 세울 수 없을 때 "세션 없음" 만 답하는 백엔드(`internal/session/no_sessions.go`)를 두고, **묻기만 하는 명령**(`kyu list` · `kyu clone` · 인자 없는 `kyu` 의 진입 판정)에만 그것을 넘긴다. 세션을 만지는 셋(`start` · `attach` · `kill`)은 그대로 설치 안내로 끝난다 — 그쪽에 "세션 없음" 을 주면 `attach` 가 *"kyu start 로 시작하세요"* 라는 못 지킬 안내로 끝나고 `kill` 이 죽인 것 없이 성공한다.

#### 7.0.1 4 단계 완료 확인 — 통과 (2026-08-31 실측, PR #45)

`git` 심볼릭 링크 하나만 든 디렉토리를 `PATH` 로 주어 tmux 만 없는 환경을 만들고 쟀다.

| 잰 것 | 결과 |
|---|---|
| `kyu list` · `kyu list --json` | 종료 코드 0. 모든 레포가 세션 없음 기준으로 판정된다(`IDLE` · `DIRTY`), `mainSession.alive` 는 `false`. stderr 에 tmux 가 나오지 않는다 |
| 인자 없는 `kyu` | 초기화까지 가고(`.coord/plan.md` 생성) 세션을 만드는 걸음에서 설치 안내로 끝난다 |
| `kyu start` · `kyu attach main` · `kyu kill --all` · `kyu kill main` | 예전 그대로 tmux 설치 안내 + 종료 코드 1 |
| tmux 가 있는 환경 | 불변. 세션 하나를 띄우면 `● proj-a RUNNING`, `kyu kill proj-a` 로 종료된다 |
| 데스크톱 | `KYU_BINARY_PATH` 로 이 엔진을 겨눠 `./gradlew build` 성공 |

**앱이 부르는 엔진 명령에 세션 백엔드를 요구하는 것이 하나도 남지 않았다** — `version` · `list` · `session-command` · `init` · `clone` · `repos` · `auth` 전부다(호출 자리는 `desktop/.../KyuCliWorkDirObserver.kt` 등 `kyucli` 어댑터들). 5.7 이 "남는 구멍 하나" 로 적은 자리는 이것으로 닫혔다.

**잰 범위를 정확히 적어 둔다.** 위는 *엔진이 tmux 없이 답하는가* 를 끝까지 잰 것이고, tmux 가 아예 설치되지 않은 별도 머신에 앱을 설치해 카드를 눌러 보는 일은 하지 않았다. 앱 쪽 요구는 2·3 단계에서 이미 지나간 것(앱은 자기 PTY 에서 세션을 연다)이라 남아 있던 미지수가 엔진 하나였고, 그것이 닫혔다.

### 7.1 2 단계의 검증 항목

감독 설계 7절이 같은 자리에 둔 목록을 이 전환에 맞게 다시 쓴다.

1. 카드를 눌러 터미널이 열리고, 그 안이 터미널 창에서 그 레포에 들어가 `claude` 를 띄운 것과 같다 — 키 입력·색·스크롤백
2. 창 크기를 바꾸면 화면이 따라 바뀐다 (`resize` 가 `claude` 에게 바로 닿는다 — 5.3)
3. 메인 세션 카드를 눌렀을 때 `claude` 가 `--add-dir` 로 하위 레포를 전부 보고 있다 (`/context` 등으로 확인)
4. 레포 세션에서 그 레포의 `.mcp.json`·`CLAUDE.md` 가 살아 있다 — **이 도구의 존재 이유가 성립하는지를 직접 재는 항목이다**(선행 문서 1.3)
5. 앱을 tmux 세션 안에서 띄워도 카드가 열린다 (`TMUX` 제거 — 5.3)
6. 앱이 보유한 세션의 카드가 `RUNNING` 으로 보이고, **같은 순간 터미널에서 `kyu list` 를 치면 그 레포가 `RUNNING` 이 아니다** — 두 세계가 갈린다는 사실을 직접 확인하는 항목
7. `kyu start proj-a` 로 CLI 세션을 띄운 뒤 앱의 그 카드가 **CLI 세션이 있음을 갈라 보여준다**(5.4.2)
8. 터미널을 닫으면 그 세션의 `claude` 가 끝난다 — **detach 가 아니라 종료라는 것을 눈으로 본다**(6.1 의 `closeTerminal` 줄)
9. 앱을 끄면 그 `claude` 프로세스가 `ps` 에 남지 않는다 (8절 1번의 반대 확인)

---

## 8. 열린 질문

구현을 막지는 않지만 해당 단계에서 정해야 하는 것들이다.

**실측으로 해소된 것 하나를 먼저 적는다.** 이 문서의 골격에는 "`claude` 가 대화 ID 를 어떻게 노출하는가 — 실측 필요" 가 있었다. **해소됐고, 답은 "노출받지 않고 정해준다" 다**(5.5.1). `--session-id <uuid>` 가 있으므로 `claude` 의 저장 형식에 기대는 코드가 한 줄도 필요 없다.

**남은 질문**

1. **앱이 크래시했을 때 자식 `claude` 의 처지** — 앱이 죽으면 pty4j 가 쥔 PTY 마스터가 닫히고 자식에게 `SIGHUP` 이 간다. 하지만 `SIGHUP` 을 무시하는 프로그램은 살아남는다. **그러면 `kyu list` 에도 안 보이고 앱에도 안 보이는 프로세스가 남는다** — 감독 설계 10절 3번과 같은 질문이 여기서 반복된다.
   찾을 수단이 없다는 것이 감독 때보다 나쁘다. 감독은 소켓 파일이 남아 흔적이 있었는데, 앱의 자식은 `ps` 에만 보인다. 후보: (가) 자식 환경에 표식 하나(`KYU_APP_SESSION=<워크디렉토리>-<라벨>`)를 넣어 `ps` 로 찾을 수 있게 한다 (나) 앱이 뜰 때 그 표식을 가진 고아를 찾아 알린다 (다) 두지 않고 사용자에게 맡긴다.
   **(가)를 먼저 한다** — 값이 싸고, (나)를 나중에 하려면 표식이 이미 있어야 한다. 2 단계에서 실제로 어떤 상태가 되는지 확인하고 정한다.

2. **resume 실패를 앱이 무엇으로 가려낼 것인가** — 5.5.4 의 표. 추천은 답 문서에 `resumedConversationId` 를 싣는 것이고, 그러면 정상 `/exit` 과 구별되지 않는 대가가 남는다. **3 단계에서 정한다.**

3. **한 번에 여러 세션을 열 것인가** — 지금 앱은 한 번에 하나만 붙는다. 그 결정의 근거가 *"전환이 상태를 잃지 않는 것은 tmux 가 보장하므로"* 였는데(`EmbeddedTerminalStateHolder.kt:17`), **그 보장이 사라진다.** 카드를 옮기는 것이 곧 앞 세션을 끝내는 일이 되면 사용자가 하던 작업을 잃는다.
   후보: (가) 탭을 두어 여러 세션을 동시에 보유한다 (나) 하나만 두되 카드를 옮길 때 확인을 받는다 (다) 하나만 두되 옮겨도 앞 세션의 PTY 는 살려두고 화면만 감춘다.
   **(다)를 추천한다** — 사용자가 지금 겪는 것(옮겨도 안 잃는다)과 가장 가깝고, 탭이라는 화면 작업 없이 된다. 대가는 `claude` 프로세스가 세션 수만큼 사는 것이고, 감독 설계 10절 1번의 실측에 따르면 `claude` 하나가 326 MB 다. **2 단계에서 정해야 한다** — 이 결정이 `EmbeddedTerminalStateHolder` 의 모양을 정한다.

4. **앱을 끌 때 확인을 받을 것인가** — 3.5 의 기각과 별개로, 세션이 도는 채로 앱을 끄면 진행 중인 턴을 잃는다(1.4). "돌고 있는 세션 N 개가 끊깁니다" 를 확인받는 것이 맞아 보이지만, 매번 물으면 성가시다. 2 단계에서 정한다.

5. **윈도우에서 `kyu list` 가 세션 백엔드 없이 답하는 자리** — 5.7 의 구멍. 후보: (가) "세션 없음" 만 답하는 백엔드를 하나 둔다 (나) `IsAlive` 실패를 "없음" 으로 읽는다 (다) 윈도우는 감독 5 단계까지 기다린다. **(가)를 추천한다** — 사실을 사실대로 답하고, `kyu list` 가 실패하지 않는다. 4 단계.

6. **다중 워크디렉토리 창** — 지금 앱은 창 하나에 워크디렉토리 하나다. 둘을 열면 대화 기록 파일이 서로 다른 워크디렉토리 것이라 다투지 않지만, **같은 워크디렉토리를 두 창에서 열면 같은 대화 ID 로 `claude` 를 둘 띄우게 된다.** `--session-id` 를 이미 쓰인 ID 로 두 번 부르면 어떻게 되는지가 확인되지 않았다 — 거절인지, 이어가기인지, 전사가 섞이는지. **1 단계에서 실측한다.**

7. **`--bypass-permissions` 를 앱의 어디에 두는가** — 엔진 쪽 전달 경로는 정해져 있다(`kyu session-command <repo> --bypass-permissions --json`). 정해지지 않은 것은 앱의 화면이다. 카드마다? 워크디렉토리마다? 설정에 한 번? **위험 수준이 다른 모드이므로 켜져 있다는 것이 화면에 계속 보여야 한다.** 2 단계 이후.

8. **`kyu list --json` 의 `state` 를 세션과 git 으로 쪼갤 것인가** — 5.4.1 이 앱 세션에서는 두 사실을 함께 얻는다고 적었지만, CLI 세션이 떠 있는 레포는 여전히 git 상태가 가려진다. 쪼개면 `schemaVersion` 을 올리는 변경이다(필드의 뜻이 바뀐다). **이번 범위 밖.** 실제로 불편한지 보고 정한다.

9. **세션 안에서 `/exit` 했을 때 앱의 화면** — PTY 가 닫히고 `TtyConnector.isConnected` 가 거짓이 된다. 지금은 그 카드를 다시 누르면 다시 붙는다(`EmbeddedTerminalStateHolder.kt:88`). 바뀐 뒤에는 다시 누르면 **새 세션이 열리고 대화는 이어진다** — 사용자가 기대하는 것과 맞는지, 끝났다는 표시를 화면에 남길지. 2 단계.

---

## 부록 A. 확인된 사실

구현 중 다시 조사하지 않도록 근거를 모아둔다. 감독 설계 부록 A 의 사실들은 그대로 유효하고 여기서 되풀이하지 않는다.

| 사실 | 근거 |
|---|---|
| `claude --session-id <uuid>` 로 **대화 ID 를 부르는 쪽이 정할 수 있다** — 알아낼 필요가 없다 | `claude --help` (2.1.251) |
| `claude --resume <uuid>` 는 그 ID 의 대화를 이어간다 | `claude --help` |
| `claude --resume <없는 uuid>` 는 네트워크 왕복 없이 즉시 종료 코드 1 로 끝나고 `No conversation found with session ID: <uuid>` 를 stderr 에 낸다 | 2026-08-31 실측 |
| `claude --fork-session` 은 이어가면서 새 ID 를 받는 길이다 — "새 대화로 시작" 의 재료 | `claude --help` |
| 전사는 cwd 별로 갈린 디렉토리에 대화 ID 이름의 파일로 남는다. **그러므로 `--resume` 은 같은 cwd 에서 불러야 한다** | 2026-08-31 실측 (`~/.claude/projects/`) |
| 링이 감긴 뒤의 재접속은 화면만이 아니라 터미널 모드(입력)를 잃는다 — 원시 바이트 재생으로는 넘을 수 없는 관문 | 감독 설계 5.7.1 |
| 앱은 이미 pty4j 로 진짜 PTY 를 연다 | `desktop/.../terminal/pty/PtyKyuSessionTerminalAttacher.kt:85` |
| 앱의 터미널은 JediTerm 위젯이고, 그것이 곧 셀 격자와 모드 상태를 쥔 화면 모델이다 | `desktop/.../terminal/pty/KyuAttachTtyConnector.kt:18` (`ProcessTtyConnector`) |
| 앱은 `kyu attach` 앞에 `kyu start` 를 조건 없이 먼저 부른다 — 대시보드가 아는 상태가 최대 3 초 묵은 것이라 | `desktop/.../terminal/SessionEntryPlan.kt:24`~`27` |
| 세션이 실행할 명령은 만드는 순간 확정되고 이후 바꿀 수 없다 | `internal/cli/start.go` 의 `warnRunningSessionKeepsTheCommandItWasCreatedWith` |
| `SessionBackend.Create` 에 환경 인자가 없어 CLI 는 명령을 `env` 로 감싼다 — 새 표면은 그 제약 아래 있지 않다 | `internal/cli/start.go:47` |
| `kyu list --json` 의 `schemaVersion` 은 **필드를 더할 때는 오르지 않는다** — 나중에 필드를 더해도 도는 독자가 안 깨진다 | `internal/cli/list_json.go:46` · `desktop/.../workdir/kyucli/KyuListOutput.kt:19` |
| 앱은 모르는 상태 낱말을 만나도 멈추지 않고 그대로 들고 있는다 | `desktop/.../workdir/WorkDirSnapshot.kt:82` (`RepoState.Unrecognized`) |
| GUI 대시보드가 아는 상태는 최대 3 초 묵은 것이다 — 앱이 자기 세션 상태를 스스로 아는 것은 이 지연을 타지 않는다 | `desktop/.../dashboard/WorkDirDashboardStateHolder.kt:121` |
| 윈도우 네이티브가 막혀 있는 이유는 바이너리가 아니라 tmux 다 | `desktop/.../engine/githubrelease/EngineReleaseTarget.kt:7` |
| pty4j 는 윈도우에서 ConPTY 를 쓴다 — 감독 설계 8절이 직접 만들어야 한다고 적은 자리를 라이브러리가 채운다 | pty4j 문서. **5 단계 전에 실제로 확인할 것** |
| 감독 설계 3.3 은 이 배치를 원칙 5 위반으로 기각했다 — 이 문서는 원칙을 개정해 그 기각을 뒤집는다 | 감독 설계 3.3 · 이 문서 1.4 |

## 부록 B. 용어

감독 설계 부록 B 에 더한다.

| 용어 | 의미 |
|---|---|
| **앱 세션** | 앱이 자기 PTY 에서 직접 띄운 `claude`. 앱과 생사를 같이하고 `kyu list` 에 보이지 않는다 |
| **CLI 세션** | `kyu start` 가 tmux(또는 감독) 위에 띄운 세션. 지금까지의 "세션" 이고, 이 전환으로 바뀌지 않는다 |
| **대화** (conversation) | 이어갈 수 있는 `claude` 의 기록 하나. UUID 로 식별한다. `claude` 자신은 이것을 "session" 이라 부른다 (5.6) |
| **조립 지식** | 세션이 무슨 명령을 어느 디렉토리에서 어떤 환경으로 실행하는가. 엔진에 하나만 둔다 (원칙 11) |
| **기계 표면** | `--json` 으로 답하는 명령. GUI 와 스크립트가 읽는 계약 (README 의 같은 낱말) |
