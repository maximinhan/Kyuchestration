# WorkDir Orchestrator — 설계 문서

> 여러 레포가 하나의 목표로 묶여 움직일 때, 각 레포의 독립성을 유지한 채 조율하는 CLI 도구.

- 작성일: 2026-08-27
- 상태: 설계 확정, 구현 미착수
- 대상 플랫폼: macOS, Windows(WSL) — 이후 Windows 네이티브
- 구현 확정 사항: 명령 이름은 `kyu` (문서 내 `wd` 표기는 설계 당시 기록)
- 구현 확정 사항: 기본 명령은 `list` 가 아닌 원스텝 진입 — 인자 없는 `kyu` 는 초기화·메인 세션 생성·진입까지 한 번에 한다 (사용자 결정, PR 10)
- 구현 확정 사항: 레포 클론 명령 `kyu clone` 추가 — 진입 플로우와 분리된 독립 명령이고, 인증은 사용자가 등록한 토큰 프로필만 쓴다 (머신의 `GITHUB_TOKEN`·`gh` 로그인을 읽지 않는다) (사용자 요구, PR 13)

---

## 1. 배경 — 왜 이 도구가 필요한가

### 1.1 실제 작업 패턴

작업 단위마다 **워크디렉토리**를 하나 만들고, 그 아래에 필요한 레포를 클론해서 쓴다.

```
~/WorkDir-featureX/
    proj-a/          # git clone
    proj-b/          # git clone
    proj-c/          # 작업 도중 얽혀서 추가로 clone
```

MSA 환경에서는 하나의 기능이 여러 레포에 걸친다. 예를 들어 이벤트 하나를 추가하려면 공통 라이브러리 레포에 메시지 클래스를 넣고, 발행하는 서비스와 소비하는 서비스를 각각 고쳐야 한다. 이때 세 레포는 **하나의 목표를 공유하지만 서로 다른 코드베이스**다.

이 패턴은 반복적으로 발생한다. 작업이 끝나면 워크디렉토리를 버리고, 다음 작업에서 새로 만든다.

### 1.2 지금 겪는 구체적 불편

**(1) 레포마다 터미널 탭을 오간다**

각 레포에서 별도 세션을 띄워야 하는데(이유는 1.3), 레포가 3~4개면 탭을 계속 오가게 된다. 어느 세션이 무슨 작업 중인지, 끝났는지 한눈에 안 보인다.

**(2) 워크디렉토리에서 한 세션으로 총괄하려 하면 각 레포의 설정이 죽는다**

워크디렉토리 최상위에서 세션 하나를 띄우고 `--add-dir` 로 하위 레포를 붙이면, 파일은 보이지만 **각 레포의 MCP 설정과 프로젝트 지침이 로드되지 않는다.** 공식 문서의 표현이 정확하다.

> "Additional directories grant file access, not configuration"

즉 `proj-a/.mcp.json` 에 코드 분석용 MCP 서버를 세팅해두어도 워크디렉토리 세션은 그것을 읽지 않는다. 프로젝트별 `CLAUDE.md` 도 마찬가지로 기본값에서는 로드되지 않는다(환경변수로만 예외 허용 — 5.4 참조).

**(3) 그래서 각 레포에서 세션을 띄우면, 이번엔 조율이 안 된다**

각 레포 디렉토리에서 세션을 띄우면 설정 문제는 해결된다. 대신 레포 간에 합의해야 할 것들 — 인터페이스 시그니처, 작업 순서, 어디까지 진행됐는지 — 을 공유할 방법이 없다. 사람이 매번 복붙해서 중개하게 된다.

### 1.3 왜 각 레포에서 세션을 띄워야 하는가

레포마다 다음이 다를 수 있다.

| 항목 | 위치 | 워크디렉토리 세션에서 |
|---|---|---|
| MCP 서버 설정 | `<repo>/.mcp.json` | ❌ 로드 안 됨 |
| 프로젝트 지침 | `<repo>/CLAUDE.md` | ❌ 기본값에서 로드 안 됨 |
| 커스텀 에이전트 | `<repo>/.claude/agents/` | ❌ 로드 안 됨 |
| 프로젝트 설정·훅 | `<repo>/.claude/settings.json` | ❌ 로드 안 됨 |

**설정은 오직 세션을 시작한 디렉토리에서만 온다.** 이것이 이 도구가 존재하는 근본 이유다.

이 제약은 버그가 아니라 보안 설계다. `.mcp.json` 은 임의 명령을 실행하는 파일이므로(`command`, `args` 필드), 추가 디렉토리의 것을 자동 로드하면 레포를 클론하는 것만으로 코드가 실행된다. 따라서 **이 제약이 완화되기를 기대하고 설계하면 안 된다.**

---

## 2. 핵심 문제 — 독립과 결합의 긴장

각 레포는 **독립적이면서 동시에 비독립적**이어야 한다. 이 긴장이 문제의 본질이다.

| 측면 | 요구사항 | 실현 방법 |
|---|---|---|
| **실행은 격리** | 각 레포 세션은 자기 cwd·MCP·지침·에이전트로 돌고, 서로의 상태를 오염시키지 않는다 | 각 레포 디렉토리에서 별도 프로세스로 세션을 띄운다 |
| **지식은 공유** | 하나의 목표를 향하므로 인터페이스 합의·작업 순서·진행 상황은 공유된다 | 워크디렉토리의 공유 파일을 통해 전달한다 |

**설계 원칙: 프로세스는 격리, 지식은 공유.**

이 원칙이 이후의 모든 결정을 지배한다. 세션 간 직접 통신을 만들지 않는 것, 조율 상태를 파일에 두는 것, 도구가 세션 내부에 개입하지 않는 것이 전부 여기서 나온다.

---

## 3. 검토한 대안과 기각 사유

새로 만들기 전에 기존 수단을 전부 검토했다. 이 절은 **다른 세션에서 "이거 그냥 X 쓰면 되는 거 아냐?" 를 다시 묻지 않기 위한 기록**이다.

### 3.1 워크디렉토리 단일 세션 + 서브에이전트

메인 세션 하나에서 서브에이전트가 각 레포를 병렬로 수정하는 방식.

**확인된 사실**: 서브에이전트의 MCP 연결은 정의 방식에 따라 갈린다.

- **이름 문자열 참조** → 메인 세션의 연결을 공유한다
- **inline 정의** → 서브에이전트가 시작할 때 자기 인스턴스를 연결하고 종료 시 해제한다

즉 서브에이전트 정의 파일의 `mcpServers` frontmatter 에 inline 으로 정의하면 레포별 전용 MCP 인스턴스를 띄울 수 있다.

```yaml
---
name: coder-proj-a
description: proj-a 전용
mcpServers:
  - analyzer-a:
      type: stdio
      command: <server-command>
      args: ["--project", "/abs/path/to/WorkDir/proj-a"]
---
```

**기각 사유**:

1. **레포가 늘어날 때마다 에이전트 정의 파일을 만들어야 한다.** 워크디렉토리를 자주 새로 만드는 패턴에서는 이 비용이 반복된다.
2. **레포의 나머지 설정은 여전히 안 따라온다.** MCP 는 inline 으로 해결해도 그 레포의 `CLAUDE.md`·커스텀 에이전트·훅은 로드되지 않는다.
3. **서브에이전트가 뜰 때마다 MCP 서버가 부팅된다.** 코드 분석 MCP 는 언어 서버를 띄우므로 부팅이 무겁다. 위임을 잘게 쪼개는 방식과 상성이 나쁘다.

부분적으로는 유효한 수단이므로 폐기하지는 않되, **이 도구의 접근과는 다른 층위**다.

### 3.2 Agent Teams

팀 리드 세션이 별도 인스턴스로 뜨는 팀메이트들을 공유 태스크 리스트로 조율하는 내장 기능.

**기각 사유**: 팀메이트가 **어느 working directory 에서 뜨는지, 리드와 다른 cwd 를 지정할 수 있는지, 자기 `.mcp.json`·`CLAUDE.md` 를 새로 로드하는지가 공식 문서에 명시되어 있지 않다.** 이 도구의 핵심 요구사항(레포별 설정 격리)이 성립하는지 확인할 수 없다.

추가로 팀메이트 하나하나가 완전한 인스턴스라 토큰 소비가 크고, 팀메이트끼리 협상하는 모델이라 "메인이 판단을 확정하고 각 레포는 적용만 한다"는 운용 방침과 어긋난다.

### 3.3 기존 오케스트레이터 (Conductor, Vibe Kanban, Claude Squad 등)

**기각 사유**: 이들은 전부 **"하나의 레포, 여러 브랜치"** 를 전제로 한다. 격리 수단이 git worktree이고, worktree 는 "레포 하나에 체크아웃은 하나"라는 제약을 푸는 도구다.

레포가 여러 개면 애초에 디렉토리가 별개라 worktree 가 필요 없다. **이들이 푸는 문제와 우리 문제는 다르다.** 멀티레포에서 레포별 설정을 격리한 채 조율하는 기능은 어느 도구에도 없다.

(Claude Squad 의 tmux 백엔드 방식은 참고할 만하다 — 8.2 참조.)

### 3.4 셸 함수

워크디렉토리 하위 git 레포를 스캔해 `--add-dir` 플래그를 자동 생성하는 함수.

**기각 사유**: 3.1의 (2)와 같다. 파일 접근만 열릴 뿐 레포별 설정은 여전히 죽는다. 그리고 조율 기능이 전혀 없다.

다만 **이 도구가 없을 때의 최소 대안**으로는 유효하다.

---

## 4. 설계 원칙

1. **프로세스는 격리, 지식은 공유** — 2절의 핵심 원칙.
2. **레포 안에는 아무것도 넣지 않는다** — 레포는 팀 공용이다. 이 도구를 쓰기 위해 레포에 파일을 추가하거나 지침을 넣도록 요구하지 않는다. 조율에 필요한 모든 것은 워크디렉토리에 둔다.
3. **세션 내부에 개입하지 않는다** — 도구는 세션을 띄우고, 보여주고, 진입시킬 뿐 세션의 입출력을 가로채지 않는다. 세션이 무엇을 하는지는 도구의 관심사가 아니다.
4. **플랫폼 의존을 한 겹에 가둔다** — 세션 실행 계층만 플랫폼에 의존하고, 나머지는 순수하게 유지한다.
5. **도구가 죽어도 작업이 안 끊긴다** — 세션의 생명주기는 도구와 독립적이다.
6. **상태는 관찰해서 얻는다** — 세션이 상태를 보고하도록 요구하지 않는다. 관찰 가능한 것(프로세스 생존, git 상태)에서 추론한다. 원칙 2의 따름정리다.

---

## 5. 아키텍처

### 5.1 3층 구조

```
┌─────────────────────────────────────────────┐
│ [표시 계층]                                  │
│   목록 출력 · 상태 표시 · 명령 파싱          │  ← 교체 가능
├─────────────────────────────────────────────┤
│ [조율 계층]                                  │
│   WorkDir 스캔 · plan 파싱 · 상태 추론       │  ← 플랫폼 무관 (핵심 자산)
├─────────────────────────────────────────────┤
│ [세션 계층]  SessionBackend 인터페이스       │
│   create / attach / kill / list / isAlive    │  ← 유일한 플랫폼 의존부
│         └── tmux 구현 (v1)                   │
└─────────────────────────────────────────────┘
```

**이 배치의 목적은 조율 계층을 두 번 쓰지 않는 것이다.** 이 도구의 고유 가치가 사는 곳이고, 표시 계층과 세션 계층은 교체 가능한 껍데기다.

### 5.2 SessionBackend 인터페이스

플랫폼 의존부는 다음 5개 함수가 전부다.

```go
type SessionBackend interface {
    // Create 는 cwd 에서 cmd 를 실행하는 백그라운드 세션을 만든다.
    // 이미 같은 이름의 세션이 있으면 ErrSessionExists 를 반환한다.
    Create(name, cwd string, cmd []string) error

    // Attach 는 호출한 터미널을 해당 세션에 연결한다.
    // 사용자가 빠져나올 때까지 블로킹된다.
    Attach(name string) error

    // Kill 은 세션을 종료한다. 없으면 nil 을 반환한다(멱등).
    Kill(name string) error

    // List 는 이 백엔드가 관리하는 세션 이름 전체를 반환한다.
    List() ([]string, error)

    // IsAlive 는 세션의 생존 여부를 반환한다.
    IsAlive(name string) (bool, error)
}
```

tmux 구현은 각각 명령 한 줄에 대응한다.

| 인터페이스 | tmux 명령 |
|---|---|
| `Create` | `tmux new-session -d -s <name> -c <cwd> <cmd...>` |
| `Attach` | `tmux attach-session -t <name>` |
| `Kill` | `tmux kill-session -t <name>` |
| `List` | `tmux list-sessions -F '#{session_name}'` |
| `IsAlive` | `tmux has-session -t <name>` (종료 코드로 판정) |

**세션 명명 규칙**: `wd-<workdir>-<repo>` (메인 세션은 `wd-<workdir>-main`). 워크디렉토리가 여러 개 열려 있어도 충돌하지 않고, `List()` 결과를 접두사로 필터링해 이 도구가 만든 세션만 골라낼 수 있다.

이름에 쓸 수 없는 문자(`.`, `:` 등)는 치환한다. tmux 세션 이름에서 `.` 과 `:` 는 타깃 지정 문법과 충돌한다.

### 5.3 조율 계층

플랫폼과 무관한 순수 로직. 세 가지 일을 한다.

**(1) WorkDir 스캔** — 최상위 하위 디렉토리 중 `.git` 을 가진 것을 레포로 인식한다. 설정 파일에 레포 목록을 유지하지 않는다. **클론하면 곧바로 인식되는 것이 요구사항이다.**

**(2) 상태 추론** — 원칙 6에 따라 관찰로만 얻는다.

| 상태 | 판정 근거 |
|---|---|
| `RUNNING` | 세션이 살아있음 (`IsAlive`) |
| `DIRTY` | 세션 없음 + 커밋되지 않은 변경 있음 (`git status --porcelain` 비어있지 않음) |
| `AHEAD` | 세션 없음 + 워킹 트리 깨끗 + 업스트림보다 앞선 커밋 있음 |
| `IDLE` | 세션 없음 + 변경 없음 |

`AHEAD` 판정은 `git rev-list --count @{u}..HEAD` 로 한다. **업스트림이 설정되지 않은 브랜치는 판정하지 않고 `IDLE` 로 둔다** — 작업 브랜치에 업스트림을 두지 않는 운용도 있으므로, 없는 것을 이상 상태로 취급하지 않는다.

**두 종류의 상태를 구분한다.** 이 표의 상태는 도구가 **관찰**해서 얻는 것이고, 6.1의 `status` 필드는 사람 또는 세션이 **선언**하는 것이다. 둘은 독립적이며 서로를 덮어쓰지 않는다.

| | 출처 | 값 | 갱신 주체 |
|---|---|---|---|
| 레포 상태 | 관찰 (tmux + git) | `RUNNING` / `DIRTY` / `AHEAD` / `IDLE` | 도구 (매 조회 시 재계산) |
| 작업 상태 | 선언 (`.coord/plan.md`) | `blocked` / `ready` / `doing` / `done` | 사람 또는 세션 |

**(3) plan 파싱** — `.coord/plan.md` 에서 작업 목록과 의존 관계를 읽는다(6절).

### 5.4 워크디렉토리 세션 (메인)

조율을 담당하는 세션. 워크디렉토리 최상위에서 뜨고, 하위 레포를 `--add-dir` 로 붙인다.

```
claude --add-dir proj-a --add-dir proj-b --add-dir proj-c
```

**메인은 코드를 직접 수정하지 않는다.** 각 레포를 읽어 현황을 파악하고, 인터페이스를 확정하고, 작업 순서를 `.coord/plan.md` 에 기록하는 역할이다. 따라서 레포별 MCP 설정이 필요 없고, `--add-dir` 의 "파일 접근만 준다"는 성질이 이 용도에 정확히 맞는다.

**선택 사항**: 메인이 각 레포의 지침 파일까지 읽게 하려면 환경변수 `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` 을 설정하면 추가 디렉토리의 `CLAUDE.md` 와 `.claude/rules/` 가 로드된다. 다만 레포 수만큼 컨텍스트가 늘어나므로 기본값은 끄고, 계획이 레포 컨벤션과 어긋나는 문제가 실제로 생기면 켜도록 한다. 도구는 이 값을 설정으로 노출한다.

### 5.5 데이터 흐름

```
        ┌──────────────────────────────────────┐
        │  WorkDir/.coord/plan.md              │
        │  (진실의 근원 — 인터페이스·순서)      │
        └──────────────────────────────────────┘
              ▲ write              │ read
              │                    ▼
    ┌─────────────────┐   ┌──────────────────────┐
    │  main 세션       │   │  proj-a 세션          │
    │  (WorkDir)      │   │  (WorkDir/proj-a)     │
    └─────────────────┘   └──────────────────────┘
                          ┌──────────────────────┐
                          │  proj-b 세션          │
                          └──────────────────────┘
                                    │
              ┌─────────────────────┘
              ▼ 관찰 (tmux + git)
        ┌──────────────────────────────────────┐
        │  도구 — 상태 추론 후 표시              │
        └──────────────────────────────────────┘
```

**세션 간 직접 통신은 없다.** 메인이 파일에 쓰고, 각 레포 세션이 필요할 때 읽는다. 도구는 세션 바깥에서 관찰만 한다.

이 구조의 이점: 세션이 언제 뜨고 죽어도 무방하다. 도구를 껐다 켜도 상태가 유실되지 않는다. 조율 상태가 파일이므로 사람이 직접 읽고 고칠 수 있다.

---

## 6. 파일 규약

워크디렉토리 최상위의 `.coord/` 디렉토리에 둔다. **레포 안에는 아무것도 넣지 않는다**(원칙 2).

```
WorkDir/
    .coord/
        plan.md          # 작업 계획 — 메인이 쓰고 각 레포 세션이 읽는다
    proj-a/
    proj-b/
```

### 6.1 plan.md

YAML frontmatter 와 Markdown 본문을 함께 쓴다. **frontmatter 는 도구가 파싱하고, 본문은 사람과 세션이 읽는다.**

```markdown
---
tasks:
  - id: commons-event
    repo: proj-a
    title: 이벤트 클래스 추가
    needs: []
    status: done

  - id: publish
    repo: proj-b
    title: 발행 로직 구현
    needs: [commons-event]
    status: ready

  - id: consume
    repo: proj-c
    title: 컨슈머 구현
    needs: [commons-event]
    status: blocked
---

## 확정 인터페이스

다른 레포가 의존하는 시그니처를 그대로 적는다. 요약하지 않는다.

    SomeEvent(id: Long, ownerId: Long, occurredAt: Instant)
    채널: <채널 식별자>

## 배경

이 작업을 하는 이유와 결정 근거.

## 작업 순서 근거

commons-event 가 머지되어야 나머지 두 레포가 컴파일된다.
publish 와 consume 은 서로 의존하지 않으므로 병렬 가능.
```

**필드 정의**

| 필드 | 필수 | 의미 |
|---|---|---|
| `id` | ✓ | 작업 식별자. `needs` 에서 참조된다 |
| `repo` | ✓ | 수행할 레포 디렉토리명 |
| `title` | ✓ | 한 줄 설명 |
| `needs` | | 선행 작업 id 목록. 빈 배열이면 즉시 실행 가능 |
| `status` | ✓ | `blocked` / `ready` / `doing` / `done` |

**`status` 는 사람 또는 세션이 갱신한다.** v1 에서 도구는 이 값을 읽어 표시만 하고, 자동으로 바꾸지 않는다. 자동 전이(선행 작업이 `done` 이면 `blocked` → `ready`)는 v2 후보다(10절).

**파싱 실패 시 동작**: `.coord/plan.md` 가 없거나 frontmatter 가 없으면 계획 없이 동작한다(레포 목록과 상태만 표시). frontmatter 가 있으나 형식이 깨졌으면 경고를 출력하고 계획 없이 동작한다. **계획 파싱 실패가 도구 전체를 막지 않는다.**

### 6.2 왜 파일인가

- 세션 간 통신 채널을 만들 필요가 없다. 구현량이 크게 준다.
- 세션이 언제 뜨고 죽어도 상태가 유실되지 않는다.
- 사람이 직접 읽고 고칠 수 있다. 디버깅이 곧 파일 열기다.
- 이 도구 없이도 동작한다. 도구는 편의 계층이지 필수 인프라가 아니다.

---

## 7. v1 범위

**포함**

| 기능 | 설명 |
|---|---|
| WorkDir 초기화 | `.coord/plan.md` 템플릿 생성 |
| 레포 자동 발견 | 하위 `.git` 보유 디렉토리 스캔. 설정 불필요 |
| 상태 목록 | 레포별 세션 생존 + git 상태 + plan 상 작업 |
| 메인 세션 실행 | `--add-dir` 자동 조립 |
| 레포 세션 실행 | 해당 레포 디렉토리에서 생성 (설정 격리 자동 성립) |
| 세션 진입 | 선택한 세션으로 attach |
| 세션 종료 | 개별 / 전체 |

**명시적 제외 (YAGNI)**

| 제외 항목 | 이유 |
|---|---|
| 자체 TUI 화면 | v1 은 목록 출력 + 명령으로 충분. 프론트는 나중에 교체 |
| 세션에 작업 자동 주입 | 헤드리스 실행의 권한·승인 동작 미검증 (11절) |
| plan status 자동 전이 | 수동으로 충분히 굴려보고 규칙을 확정한 뒤에 |
| 통합 diff 뷰 | 별도 기능 덩어리. v1 가치와 독립 |
| 코드 분석 MCP 자동 등록 | 도구마다 등록 방식이 다름. 레포 세션에서 1회 하면 유지됨 |
| git worktree 지원 | 우리 문제는 멀티레포이지 멀티브랜치가 아님 |
| 팀 배포·설치 관리 | 우선 자기 환경에서 검증 |

---

## 8. 기술 선택과 근거

### 8.1 언어: Go

| 이유 | 설명 |
|---|---|
| 단일 바이너리 | 런타임 의존이 0. 새 머신에서 바이너리 하나 복사로 끝 |
| 크로스 컴파일 | `GOOS=windows GOARCH=amd64 go build` 한 줄 |
| 경로 처리 | `path/filepath` 가 구분자 차이를 흡수 |
| 설정 위치 | `os.UserConfigDir()` 이 플랫폼별 관례를 처리 |
| TUI 확장 | bubbletea 등 성숙한 생태계. v2에서 같은 코드베이스로 이어짐 |

**셸 스크립트를 기각한 이유**: macOS·WSL 은 커버하지만 Windows 네이티브 확장 시 전부 재작성이다. 조율 계층(plan 파싱, 의존성 판정)이 커지면 셸로는 금방 한계에 부딪힌다. 경로·설정 위치 분기를 전부 손으로 써야 한다.

### 8.2 세션 백엔드: tmux

세션 실행을 직접 구현하지 않고 tmux 에 위임한다.

| 위임으로 얻는 것 | 직접 구현했을 때의 비용 |
|---|---|
| PTY 관리 | pty 할당, 입출력 펌프 |
| 리사이즈 대응 | SIGWINCH 처리 |
| 스크롤백 | 버퍼 관리 |
| detach / 재접속 | 데몬 프로세스 설계 |
| 도구와 독립적인 세션 생존 | 프로세스 분리 |

**자체 TUI 구현량의 대부분이 이 표의 오른쪽 열이다.** tmux 백엔드를 쓰면 통째로 사라지고, 원칙 5("도구가 죽어도 작업이 안 끊긴다")가 공짜로 성립한다.

**사용자는 tmux 를 몰라도 된다.** 도구가 감춘다. 진입은 도구 명령으로 하고, 세션에서 빠져나오는 방법만 안내하면 된다. Claude Squad 도 같은 구조로 검증되어 있다.

**tmux 미설치 시**: 명확한 안내와 함께 종료한다(`brew install tmux` / `apt install tmux`). 자동 설치는 하지 않는다.

---

## 9. 구현 시작점

### 9.1 디렉토리 구조

```
wd/
    cmd/wd/main.go              # 진입점, 명령 라우팅
    internal/
        session/
            backend.go          # SessionBackend 인터페이스 정의
            tmux.go             # tmux 구현
            name.go             # 세션 명명 규칙 + 문자 치환
        workdir/
            scan.go             # 하위 git 레포 발견
            state.go            # 상태 추론 (세션 + git)
            plan.go             # .coord/plan.md 파싱
        cli/
            list.go  start.go  attach.go  kill.go  init.go
    go.mod
```

**`internal/session` 밖에서는 tmux 를 직접 호출하지 않는다.** 이 규칙이 지켜지는 한 플랫폼 확장은 파일 하나 추가로 끝난다.

### 9.2 구현 순서

의존 순서대로 쌓되, **각 단계가 끝날 때마다 손으로 확인 가능한 상태**가 되게 한다.

| 단계 | 내용 | 완료 확인 |
|---|---|---|
| 1 | `SessionBackend` 인터페이스 + tmux 구현 | 세션을 만들고 `tmux ls` 로 보이고 죽일 수 있다 |
| 2 | WorkDir 스캔 | 하위 레포 목록이 정확히 나온다 |
| 3 | 상태 추론 (세션 + git) | 각 레포의 RUNNING/DIRTY/AHEAD/IDLE 이 맞다 |
| 4 | `wd list` | 목록과 상태가 한 화면에 나온다 |
| 5 | `wd start` / `wd attach` | 메인·레포 세션을 띄우고 진입할 수 있다 |
| 6 | `wd init` + plan 파싱 | 계획의 작업 목록이 목록에 함께 표시된다 |
| 7 | `wd kill` | 개별·전체 종료 |

**1번이 먼저인 이유**: 이 도구에서 유일하게 "외부와 씨름하는" 부분이라 예상 못 한 문제가 여기 있다. 나머지는 순수 로직이라 예측 가능하다.

### 9.3 명령 인터페이스 (초안)

```
wd init [name]        # 워크디렉토리 초기화 (.coord/plan.md 생성)
wd list               # 레포 목록 + 상태 (기본 명령, 인자 없이 실행 시)
wd start [repo]       # 세션 시작. 인자 없으면 main
wd attach <repo>      # 세션 진입. main 도 가능
wd kill [repo|--all]  # 세션 종료
```

`wd list` 출력 예:

```
WorkDir: <name>   (3 repos, 2 sessions)

  ●  proj-a    RUNNING   2m    [commons-event] done
  ○  proj-b    DIRTY           [publish] ready
  ○  proj-c    IDLE            [consume] blocked ← needs commons-event
  ●  main      RUNNING   12m

wd attach <repo> 로 진입
```

### 9.4 첫 마일스톤

**"레포 3개짜리 워크디렉토리에서 `wd list` 로 상태를 보고, `wd attach proj-a` 로 들어가 작업하고, 나와서 다시 목록을 볼 수 있다."**

이것이 되면 1.2의 (1)과 (3)이 해결된다. plan 관련 기능은 그 뒤에 붙여도 된다.

---

## 10. 확장 로드맵

| 단계 | 추가 | 안 바뀌는 것 |
|---|---|---|
| **v1** | 조율 계층 + tmux 어댑터 + 목록 출력 | — |
| v2 | 자체 TUI (bubbletea) — 목록/진입/상태를 한 화면에 | 조율 계층, 세션 계층 |
| v3 | plan status 자동 전이, 통합 diff 뷰 | 세션 계층, 표시 계층 |
| v4 | Windows 네이티브 백엔드 | 조율 계층, 표시 계층 |

**각 단계에서 앞 단계의 코드를 버리지 않는 것이 이 순서의 목적이다.**

---

## 11. 열린 질문 — 구현 전 검증 필요

다음 항목은 이 설계에서 **미확인**이다. v1 범위에는 들어있지 않으므로 구현을 막지는 않지만, 나중에 해당 기능으로 갈 때 먼저 확인해야 한다.

1. **헤드리스 세션 실행** — 세션을 비대화형으로 실행해 작업을 자동 주입할 때, `.mcp.json` 승인이 어떻게 처리되는지, 파일 수정 권한을 어떻게 부여하는지 확인되지 않았다. v3의 자동 실행 기능에 걸린다. **먼저 레포 디렉토리에서 손으로 한 번 돌려보고 MCP 가 붙는지 확인할 것.**

2. **Agent Teams 의 cwd 동작** — 3.2 참조. 만약 팀메이트가 다른 cwd 를 지정할 수 있고 거기서 설정을 새로 로드한다면, 이 도구의 일부 기능이 내장 기능으로 대체될 수 있다.

3. **코드 분석 MCP 의 등록 자동화** — 도구마다 프로젝트 등록 방식이 다르고, 이미 등록된 경로에 재실행할 때의 동작이 확인되지 않았다. v1에서 제외한 이유다.

4. **동일 이름 레포의 충돌** — 서로 다른 워크디렉토리에 같은 이름의 레포가 클론되어 있을 수 있다(`WorkDir-a/proj-x` 와 `WorkDir-b/proj-x`). 세션 이름은 워크디렉토리를 포함하므로 충돌하지 않지만, **외부 도구(코드 분석 MCP 등)가 레포를 이름으로 식별하면 엉뚱한 경로를 잡을 수 있다.** 도구가 다루는 모든 경로는 절대경로로 유지한다.

---

## 부록 A. 확인된 사실 정리

구현 중 재조사하지 않도록 근거를 모아둔다.

| 사실 | 출처 |
|---|---|
| 추가 디렉토리는 파일 접근만 주고 설정은 주지 않는다 | 공식 문서: "Additional directories grant file access, not configuration" |
| 서브에이전트의 MCP 서버를 이름으로 참조하면 부모 세션 연결을 공유한다 | 공식 문서 (sub-agents) |
| 서브에이전트에 inline 정의된 MCP 서버는 시작 시 연결되고 종료 시 해제된다 | 공식 문서 (sub-agents) |
| 서브에이전트 frontmatter 필드명은 `mcpServers` 이며 리스트를 받는다 | 공식 문서 예시 |
| 하위 디렉토리의 지침 파일은 해당 디렉토리 파일을 읽을 때 on-demand 로드된다 | 공식 문서 (memory) |
| 추가 디렉토리의 지침 파일은 `CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD=1` 일 때만 로드된다 | 공식 문서 |
| Agent Teams 팀메이트의 cwd·설정 로딩은 문서에 명시되어 있지 않다 | 조사 결과 (미확인) |
| 기존 오케스트레이터는 git worktree 기반이라 단일 레포를 전제한다 | 도구별 문서 |

## 부록 B. 용어

| 용어 | 의미 |
|---|---|
| **WorkDir** | 하나의 작업 목표를 위해 여러 레포를 모아둔 디렉토리. 작업이 끝나면 버린다 |
| **메인 세션** | WorkDir 최상위에서 뜨는 조율용 세션. 코드를 직접 수정하지 않는다 |
| **레포 세션** | 각 레포 디렉토리에서 뜨는 작업용 세션. 해당 레포의 설정을 그대로 따른다 |
| **조율 계층** | 스캔·상태 추론·계획 파싱을 담당하는 플랫폼 무관 로직 |
| **세션 백엔드** | 세션 생성·진입·종료를 담당하는 플랫폼 의존 어댑터 |
