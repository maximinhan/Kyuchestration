# 세션 데몬 — 설계 문서

> 세션이 tmux 없이 살게 한다. 도구를 꺼도 작업이 안 끊긴다는 성질은 그대로 두고, 그 성질을 남의 프로그램이 아니라 `kyu` 자신이 떠받친다.

- 작성일: 2026-08-31
- 상태: **설계 검토 대기 — 구현 미착수**
- 선행 문서: [workdir-orchestrator-design.md](workdir-orchestrator-design.md) — 특히 5.2(SessionBackend), 8.2(tmux 위임으로 얻는 것), 원칙 5
- 대상 플랫폼: macOS, Linux(WSL 포함) — 윈도우 네이티브는 8절
- 사용자 결정: **tmux 의존을 제거한다.** 이 문서는 그 구현의 근거다
- **구현 착수 전 확정 필요**: 3.1 — 중앙 데몬 하나(5절의 설계)로 갈지, 세션별 감독 프로세스로 갈지. 1 단계의 모양이 갈린다

---

## 1. 배경 — 왜 tmux 를 걷어내는가

### 1.1 지금 구조

세션 계층은 tmux 에 위임되어 있다. `internal/session/tmux.go` 의 다섯 함수가 tmux 명령 다섯 줄에 그대로 대응하고, 그 바깥에서는 tmux 를 부르지 않는다(`internal/session/tmux.go:48`·`71`·`98`·`116`·`138`).

이 위임은 선행 문서 8.2 의 판단이었고, 그 판단은 지금도 맞다. tmux 는 PTY 관리·리사이즈·스크롤백·detach 재접속·도구와 독립적인 세션 생존을 전부 대신 해준다. 그 다섯 줄이 없었다면 v1 은 훨씬 늦게 나왔을 것이다.

문제는 위임 자체가 아니라 **위임한 상대가 사용자의 머신에 따로 설치되어야 하는 외부 프로그램**이라는 데서 나온다.

### 1.2 tmux 가 물리는 지점

**(1) 설치 요구가 도구의 첫인상을 정한다**

README 요구사항 표의 첫 줄이 tmux 다 — "없으면 `kyu init` 외의 명령이 안내와 함께 종료". 릴리스 바이너리는 한 줄로 받을 수 있게 만들어 두었는데(`install.sh`), 받은 사람은 곧바로 패키지 관리자로 돌아가 tmux 를 설치해야 한다. 데스크톱 앱은 엔진까지 동봉해 "앱만 설치하면 끝"을 표방하지만, 그 앱도 tmux 앞에서는 같은 자리에 선다(README 의 설치 안내가 `sudo apt install tmux` 를 시스템 패키지·관리자 권한과 함께 요구하는 이유).

**(2) 윈도우 네이티브가 이 한 줄에 막혀 있다**

로드맵 v4(윈도우 네이티브 백엔드)가 미착수인 이유가 tmux 다. `desktop/src/main/kotlin/com/kyuchestration/desktop/engine/githubrelease/EngineReleaseTarget.kt:7` 은 그 사실을 사용자에게 "WSL 안에서 앱을 띄우세요"로 옮기고 있고, 릴리스 워크플로는 윈도우 msi 를 아예 만들지 않는다. **tmux 를 걷어내는 것은 윈도우 지원의 전제다** — 이 작업이 끝나야 v4 가 "포팅"이 되고, 끝나기 전에는 "다시 만들기"다.

**(3) 세션 계층의 지식이 표시 계층으로 새어 있다**

`internal/cli/attach.go:22` 의 `detachKeyBinding = "Ctrl-b d"` 는 tmux 의 키다. 그 자리의 주석이 스스로 밝히고 있다 — *"백엔드가 아직 하나뿐이어서다. 두 번째 백엔드가 생기면 그때 세션 계층이 답하도록 옮긴다."* 두 번째 백엔드가 생기는 것이 이 문서다.

**(4) 화면의 주인이 우리가 아니다**

`kyu attach` 는 자기 표준 입출력을 tmux 에게 넘긴다(`internal/session/tmux.go:83`). 즉 사용자가 세션에서 보는 화면과 키 입력은 전부 tmux 의 것이고, 이 도구는 그 사이에 아무것도 놓을 수 없다. 지금은 그것으로 충분하지만, 세션 화면 위에 이 도구의 것을 하나라도 얹으려면(진입 배너, 상태 줄, 세션 간 이동) tmux 설정을 프로그램으로 만지는 길밖에 없고, 그 길은 사용자의 `~/.tmux.conf` 와 충돌한다.

**(5) 사용자는 tmux 를 몰라도 된다고 했지만, 키 하나는 알아야 한다**

선행 문서 8.2 의 "도구가 감춘다"는 완전하지 않다. `Ctrl-b d` 는 감춰지지 않고, 사용자가 `~/.tmux.conf` 에서 prefix 를 바꿔 두었다면 이 도구의 안내가 틀린 키를 알려준다. 남의 프로그램을 감싸는 한 그 프로그램의 설정까지 감쌀 수는 없다.

### 1.3 무엇을 잃게 되는가

선행 문서 8.2 의 표를 그대로 되읽는다. 오른쪽 열이 전부 돌아온다.

| tmux 가 대신 해주던 것 | 직접 구현했을 때의 비용 | 이 문서에서 |
|---|---|---|
| PTY 관리 | pty 할당, 입출력 펌프 | 5.7 · 데몬이 쥔다 |
| 리사이즈 대응 | SIGWINCH 처리 | 5.4 · RESIZE 프레임 + 클라이언트 SIGWINCH 핸들러 |
| 스크롤백 | 버퍼 관리 | 5.5 · **가장 큰 위험** |
| detach / 재접속 | 데몬 프로세스 설계 | 5.6 |
| 도구와 독립적인 세션 생존 | 프로세스 분리 | 5.6 · setsid |

**이 표를 지우려고 tmux 를 썼는데 이제 이 표를 채우겠다는 것이다.** 그러므로 이 작업의 성패는 "되는가"가 아니라 **"tmux 만큼 되는가"** 에 달려 있다. 3 단계를 동등성 테스트로 두고, 4 단계(기본값 전환)에 실측 선행 조건을 거는 것이 그 때문이다(9절).

---

## 2. 핵심 문제 — 누군가는 PTY 마스터를 쥐고 있어야 한다

세션이 살아있다는 것은 그 안의 프로그램이 계속 돈다는 뜻이다. 대화형 프로그램이 계속 돌려면 자기 표준 입출력이 터미널이어야 하고(그래야 화면을 그리고 키를 읽는다), 그 터미널은 PTY 쌍의 슬레이브다. 슬레이브가 살아있으려면 **마스터를 연 채로 쥐고 있는 프로세스가 있어야 한다.** 마스터를 쥔 마지막 프로세스가 죽으면 슬레이브 쪽에는 EOF 와 SIGHUP 이 가고 세션 안의 프로그램은 끝난다.

터미널 창에서 `kyu attach` 를 실행한 프로세스가 마스터를 쥐면, 그 창을 닫는 순간 세션도 죽는다. 그러면 detach 라는 개념이 성립하지 않는다.

**그러므로 "붙었다 떼었다 할 수 있는 세션"은 마스터를 쥔 채 사용자의 터미널과 무관하게 사는 프로세스를 반드시 요구한다.** tmux 서버가 하던 일이 정확히 이것이고, 걷어낸 자리에 같은 성질의 것이 들어가지 않으면 세션 생존 자체가 사라진다.

이 결론은 우회 가능한 종류가 아니다. 3절의 대안들은 전부 "그 프로세스를 무엇으로 둘 것인가"에 대한 서로 다른 답이지, "그 프로세스 없이 가능한가"에 대한 답이 아니다.

---

## 3. 검토한 대안과 기각 사유

이 절은 **다른 세션에서 "이거 그냥 X 쓰면 되는 거 아냐?" 를 다시 묻지 않기 위한 기록**이다(선행 문서 3절과 같은 목적).

### 3.1 세션별 감독 프로세스 — 기각하지 않았다, 확정이 필요하다

중앙 데몬 하나 대신 **세션마다 감독 프로세스 하나**를 두는 안이다. 각 감독은 자기 PTY 마스터와 자기 스크롤백만 쥐고, 자기 소켓 하나(`.../sessions/<이름>.sock`)를 듣는다. `List()` 는 소켓 디렉토리를 훑고 각각에 붙어보는 것이 된다.

**5절의 중앙 데몬 대비 나은 점**

| 항목 | 중앙 데몬 | 세션별 감독 |
|---|---|---|
| 크래시 반경 | 데몬이 죽으면 세션 전부 | 한 세션만 |
| 기동 경쟁 | 첫 create 두 개가 동시에 오면 잠금이 필요(5.6) | `net.Listen` 이 이미 배타적이라 바인딩 실패가 곧 `ErrSessionExists` |
| 자동 종료 정책 | 마지막 세션이 끝나면 언제 죽을지 정해야 함(10절 3번) | 자기 세션이 끝나면 죽는다 — 정할 것이 없다 |
| 업그레이드 중 프로토콜 불일치 | 전부 아니면 전무 | 세션별로 갈린다 — 옛 세션은 옛 감독이 계속 서빙 |
| 로그 | 한 파일에 섞임 | 세션당 한 파일 |

**중앙 데몬이 나은 점**

- `List()` 가 왕복 한 번이다. 세션별이면 N 번 연결한다. GUI 대시보드가 3 초마다 `kyu list` 를 부르므로(`desktop/.../terminal/SessionEntryPlan.kt:31` 의 주석) N=5 면 3 초에 5 연결인데, 유닉스 소켓 연결 비용을 생각하면 실질 차이는 없다. 다만 "세션 목록"이라는 사실을 아무도 소유하지 않고 매번 디렉토리에서 재구성하는 구조가 된다.
- 죽은 감독이 남긴 소켓 파일을 누가 치우는지가 세션 수만큼 늘어난다(중앙 데몬은 한 곳).
- tmux 의 심상(서버 하나 + 세션 여럿)과 같아서, 지금 코드와 문서를 읽어온 사람에게 설명 비용이 낮다.

**판단**: 기술적으로는 세션별 감독이 10절의 열린 질문 중 셋(크래시 반경·자동 종료·기동 경쟁)을 설계에서 지워버린다는 점에서 앞선다. 다만 사용자가 정한 골격은 중앙 데몬이고, 두 안은 1 단계의 코드가 상당 부분 갈린다. **구현 착수 전에 이 한 가지를 확정한 뒤 시작한다.** 이 문서의 나머지는 중앙 데몬을 전제로 쓰였고, 세션별로 갈 경우 5.6(생명주기)과 5.2(경로)만 다시 쓰면 나머지 절은 그대로 성립한다.

### 3.2 screen · dtach · abduco 위임

tmux 대신 다른 세션 프로그램에 위임하는 안.

**기각 사유**: **1.2 의 문제가 하나도 풀리지 않는다.** 설치 요구는 그대로이고(오히려 tmux 보다 덜 깔려 있다), 윈도우 네이티브는 여전히 막히고, detach 키는 여전히 남의 것이다. 바꾸는 값에 비해 얻는 것이 없다.

세부적으로도 손해다. `dtach`·`abduco` 는 가볍지만 스크롤백을 아예 갖지 않아서(붙는 순간부터의 출력만 본다) 재접속하면 화면이 비어 있다. 지금 tmux 로 되는 것이 안 되게 된다. `screen` 은 스크롤백이 있지만 tmux 보다 배포판 커버리지가 낫지 않고 유지보수도 느리다.

### 3.3 GUI 가 세션을 보유

데스크톱 앱이 직접 PTY 를 열어 세션을 소유하고, CLI 는 앱에 붙는 안. 앱은 이미 pty4j 로 PTY 를 연다(`desktop/.../terminal/pty/PtyKyuSessionTerminalAttacher.kt:81`).

**기각 사유**: **선행 문서 원칙 5("도구가 죽어도 작업이 안 끊긴다") 정면 위반이다.** 앱을 끄면 세션이 죽는다. README 임베디드 터미널 절이 "터미널 닫기·다른 카드로 이동·앱 종료 — 전부 kill 이 아니라 detach 다"라고 약속한 것이 그 자리에서 거짓이 된다.

더해서 CLI 만 쓰는 사람은 GUI 를 띄워야 세션을 가질 수 있게 된다. 표시 계층이 "교체 가능한 껍데기"라는 3층 구조(선행 문서 5.1)가 뒤집혀서, 껍데기가 핵심 자산을 소유하게 된다.

**원칙 5 의 "도구"는 CLI 와 GUI 를 가리키지 세션 계층을 가리키지 않는다.** 세션 계층은 세션이 사는 곳이므로 세션과 생사를 같이하는 것이 정의상 맞다 — tmux 서버가 죽으면 tmux 세션이 죽는 것과 같은 성질이다(5.9).

### 3.4 systemd user service / launchd agent

데몬을 직접 기동하지 않고 OS 의 사용자 서비스 관리자에 맡기는 안.

**기각 사유**: **플랫폼별로 완전히 다른 것을 두 번 만들게 된다.** systemd 는 리눅스 전용이고 macOS 는 launchd 다. 대상 플랫폼이 macOS 와 리눅스 둘인데 그 둘이 공유하는 것이 없으므로, 선행 문서 원칙 4("플랫폼 의존을 한 겹에 가둔다")를 지키려고 하는 일이 오히려 플랫폼 의존을 한 겹 더 만든다.

리눅스 쪽만 봐도 비용이 있다. 사용자 서비스는 로그인 세션에 묶여서, SSH 로 붙어 세션을 띄우고 나갔을 때 `loginctl enable-linger` 없이는 로그아웃과 함께 정리된다. 그것을 도구가 사용자 대신 켜는 것은 실행 권한 범위를 넘는다(tmux 자동 설치를 하지 않는 것과 같은 이유 — `internal/session/tmux.go:12` 의 주석).

**setsid 로 직접 떼어내는 것이 두 플랫폼에서 같은 코드다.** 그것이 5.6 의 선택이다.

### 3.5 SCM_RIGHTS 로 PTY 마스터 fd 를 클라이언트에 넘기기

데몬은 PTY 를 열어두기만 하고, attach 할 때 유닉스 소켓의 fd 전달로 마스터 파일 디스크립터를 클라이언트에게 건네는 안. 그러면 데이터가 데몬을 경유하지 않아 프로토콜의 attach 스트림(5.4)이 통째로 사라진다.

**기각 사유**: **스크롤백의 주인이 없어진다.** 마스터에서 읽어간 바이트는 사라지므로, 클라이언트가 읽은 것을 데몬은 보지 못하고 링 버퍼에 쌓을 수 없다. 재접속하면 화면이 빈다(3.2 의 dtach 와 같은 결말).

두 번째로 **두 클라이언트가 동시에 붙을 수 없다.** 같은 마스터를 두 프로세스가 읽으면 바이트가 무작위로 갈린다. 지금은 한 번에 하나만 붙지만(README 임베디드 터미널 절), 그 제약이 정책이 아니라 물리가 되어 되돌릴 수 없게 된다.

fd 전달 자체는 흥미로운 수단이지만 이 문제에 맞지 않는다.

### 3.6 nohup · setsid 로 백그라운드 실행 + 출력을 파일로

세션을 그냥 백그라운드 프로세스로 띄우고 출력을 로그 파일에 적는 안.

**기각 사유**: **대화가 안 된다.** 세션 안에서 도는 것은 사람과 주고받는 대화형 프로그램이라 표준 입력이 필요하고, 파일로 리다이렉트된 출력에는 터미널이 없어 그 프로그램은 화면 그리기를 포기한다(`isatty` 가 거짓이 되는 순간 색도 커서 이동도 멈춘다). 붙었다 떼는 것도 불가능하다 — 붙을 대상이 없다.

이 안은 "세션"이 아니라 "배치 실행"이다. 다른 문제를 푼다.

### 3.7 tmux 를 동봉

데스크톱 패키지가 엔진 `kyu` 를 동봉하듯 tmux 바이너리도 함께 넣는 안(README "앱은 엔진을 들고 온다").

**기각 사유**: **윈도우가 안 풀린다.** 1.2 의 (2)가 이 작업의 큰 이유 중 하나인데, tmux 를 동봉해도 윈도우 네이티브에는 넣을 것이 없다.

부수적으로도 무겁다. tmux 는 C 로 짜여 libevent 와 ncurses 에 링크되므로, 플랫폼·아키텍처마다 정적 링크 빌드를 만들고 그 빌드를 CI 에서 유지해야 한다. Go 단일 바이너리로 얻은 배포 단순성(선행 문서 8.1)을 도로 내놓는 셈이다. CLI 만 쓰는 사람(`go install` 로 받는 경로)에게는 동봉할 자리조차 없다.

---

## 4. 설계 원칙

선행 문서 4절의 여섯 원칙을 그대로 상속한다. 이 문서가 더하는 것은 셋이다.

7. **tmux 를 지우지 않는다, 강등한다** — 검증된 폴백을 손에 남긴다. 데몬 백엔드가 어떤 환경에서 무너져도 사용자는 환경변수 하나로 돌아갈 자리가 있다. 지우는 것은 두 백엔드가 오래 같이 산 뒤에 따로 판단한다.
8. **의미론을 새로 정하지 않는다** — 데몬 백엔드는 tmux 백엔드가 하던 것과 **같게** 동작해야 한다. 더 낫게 만들 여지가 보여도 이번에는 하지 않는다. 동등성이 증명되지 않은 백엔드는 기본값이 될 수 없고, "같음"은 다름보다 증명하기 쉽다.
9. **데몬은 세션 안을 모른다** — 원칙 3("세션 내부에 개입하지 않는다")이 데몬에 오면 이 형태가 된다. 데몬은 바이트를 나르고 화면 크기를 전하고 프로세스를 지켜볼 뿐, 출력을 해석해 무언가를 판단하지 않는다. 5.5 의 스크롤백이 **바이트 링**이지 화면 모델이 아닌 것이 이 원칙의 직접적인 결과다.

---

## 5. 아키텍처

### 5.1 배치

3층 구조(선행 문서 5.1)는 그대로다. 세션 계층 안이 갈라질 뿐이다.

```
┌─────────────────────────────────────────────────────────┐
│ [표시 계층]  CLI · 데스크톱 앱                            │  ← 안 바뀐다 (7절)
├─────────────────────────────────────────────────────────┤
│ [조율 계층]  WorkDir 스캔 · plan 파싱 · 상태 추론          │  ← 안 바뀐다
├─────────────────────────────────────────────────────────┤
│ [세션 계층]  SessionBackend                              │
│    ├── TmuxBackend    (기존 — 폴백으로 남는다)            │
│    └── DaemonBackend  (신규 — 프로토콜 클라이언트)        │
└─────────────────────────────────────────────────────────┘
                            │ 유닉스 도메인 소켓
                            ▼
┌─────────────────────────────────────────────────────────┐
│ kyu 세션 데몬 (사용자당 하나, setsid 로 떼어진 프로세스)   │
│   세션 레지스트리 · PTY 마스터 · 스크롤백 링 · 클라이언트  │
└─────────────────────────────────────────────────────────┘
```

**데몬은 `kyu` 바이너리 자신이다.** 별도 실행 파일을 내지 않는다 — 숨은 하위 명령(`kyu __daemon`)으로 자기를 다시 실행한다. 릴리스 자산이 늘지 않고, 앱이 동봉하는 엔진 하나에 데몬이 이미 들어 있고, 클라이언트와 데몬의 프로토콜 코드가 같은 패키지에 있어 어긋날 자리가 없다.

숨은 하위 명령은 `usage` 에 싣지 않는다. 사용자가 부를 명령이 아니고, 부를 필요도 없다 — 기동은 5.6 이 알아서 한다.

### 5.2 런타임 경로

```
$XDG_RUNTIME_DIR/kyu/          없으면 → $HOME/.local/state/kyu/
    daemon.sock                제어 · attach 연결을 함께 받는 소켓
    daemon.lock                기동 경쟁을 가르는 잠금 파일 (5.6)
    daemon.log                 생명주기 기록 (5.6)
```

**`$XDG_RUNTIME_DIR` 을 먼저 보는 이유**: 리눅스에서 이 디렉토리는 정확히 이 용도로 존재한다 — 사용자 소유 0700, tmpfs, 로그인 세션이 끝나면 정리된다. 재부팅 후 남은 소켓 파일을 우리가 치울 일이 없다.

**폴백이 예외가 아니라 macOS 의 정상 경로다.** macOS 에는 `XDG_RUNTIME_DIR` 이 없다. 그래서 폴백 경로는 "드문 경우"가 아니라 두 대상 플랫폼 중 하나의 기본값이고, 그 경로에서 벌어지는 일(재부팅 후에도 소켓 파일이 남는다)이 오히려 흔한 쪽이다. **죽은 소켓 처리(5.6)를 예외 취급하면 안 되는 것이 이 때문이다.**

`$TMPDIR` 을 쓰지 않는다. macOS 의 `$TMPDIR` 은 사용자별로 갈려 있어(`/var/folders/…/T/`) 그 점은 맞지만, 시스템 정리 작업이 오래된 파일을 지운다. 잠금 파일과 로그가 함께 사는 자리로는 맞지 않는다.

**`KYU_RUNTIME_DIR` 로 이 경로를 덮어쓸 수 있게 한다.** 테스트가 사용자의 데몬을 건드리지 않고 자기 데몬을 띄우기 위한 것이다 — `internal/session/tmux_test.go:19` 의 `TMUX_TMPDIR` 격리와 정확히 같은 자리다. 그 테스트가 격리에 실패하면 사용자의 작업 중인 세션을 죽인다는 사실이 주석에 적혀 있고, 데몬 쪽도 사정이 같다.

디렉토리는 0700, 소켓 파일은 0600 으로 만든다. 유닉스 소켓의 접근 제어는 파일 권한으로 이뤄지고, 이 소켓에 연결할 수 있다는 것은 **임의의 명령을 임의의 디렉토리에서 실행시킬 수 있다는 뜻**이다(`create` 가 하는 일이 그것이다). 같은 머신의 다른 사용자에게 열어줄 이유가 없다.

### 5.3 프로토콜 — 제어 연결

**newline-delimited JSON.** 한 줄에 요청 하나, 한 줄에 응답 하나. 연결은 짧게 산다 — 요청 하나를 보내고 응답을 받고 닫는다.

```
→ {"v":1,"op":"create","name":"kyu-featureX-proj-a","cwd":"/abs/path","cmd":["claude","--add-dir","/abs/x"],"env":{"FOO":"bar"}}
← {"v":1,"ok":true}

→ {"v":1,"op":"list"}
← {"v":1,"ok":true,"names":["kyu-featureX-proj-a","kyu-featureX-main"]}

→ {"v":1,"op":"alive","name":"kyu-featureX-proj-a"}
← {"v":1,"ok":true,"alive":true}

→ {"v":1,"op":"kill","name":"kyu-featureX-proj-a"}
← {"v":1,"ok":true}

← {"v":1,"ok":false,"code":"exists","msg":"세션이 이미 존재합니다"}
```

**길이 프리픽스가 아니라 개행을 고른 이유**

- 제어 메시지는 전부 작은 구조체다. 큰 페이로드도, 바이너리도 없다. 길이 프리픽스가 푸는 문제("어디까지가 한 메시지인가")를 JSON 인코더가 이미 풀어준다 — Go 의 `json.Encoder` 는 값 하나마다 정확히 한 줄을 쓰고 문자열 안의 개행은 이스케이프하므로, 값의 경계와 줄의 경계가 어긋나지 않는다.
- 손으로 들여다볼 수 있다. `socat - UNIX-CONNECT:$XDG_RUNTIME_DIR/kyu/daemon.sock` 에 한 줄 붙여넣으면 데몬이 답한다. 프로토콜을 디버깅하는 도구를 따로 만들 필요가 없고, 이것은 학습자가 코드를 읽고 따라갈 수 있어야 한다는 이 프로젝트의 성향과도 맞는다.
- 길이 프리픽스는 손으로 짠 프레이밍 코드를 하나 더 만든다. attach 연결에는 그것이 꼭 필요하지만(5.4), 필요하지 않은 곳까지 같은 것을 쓸 이유는 없다.

**`code` 는 안정된 문자열이고 `msg` 는 아니다.** 클라이언트는 `code` 로 센티널 에러를 되살린다 — `"exists"` → `ErrSessionExists`(`internal/session/backend.go:14`). `msg` 는 사람이 읽을 문장이라 언제든 다듬을 수 있어야 하고, 그것으로 분기하면 문구를 고칠 때 조용히 깨진다. tmux 백엔드가 stderr 문자열 매칭 대신 `has-session` 으로 미리 판정한 것과 같은 판단이다(`internal/session/tmux.go:48` 의 주석).

**`v` 는 프로토콜 버전이다.** 왜 필요한지는 5.6 의 업그레이드 시나리오에 있다.

### 5.4 프로토콜 — attach 연결

attach 는 **전용 연결**이다. 같은 소켓에 새로 연결하고, 첫 프레임이 attach 요청이면 그 연결은 그때부터 그 세션의 스트림이 된다.

```
[type:1][len:4 big-endian][payload:len]
```

| 방향 | type | 이름 | payload |
|---|---|---|---|
| 클라이언트 → 데몬 | `0x01` | ATTACH | `{"v":1,"name":"...","cols":120,"rows":40}` |
| 클라이언트 → 데몬 | `0x02` | INPUT | 키 입력 원시 바이트 |
| 클라이언트 → 데몬 | `0x03` | RESIZE | `cols:2` + `rows:2` (big-endian) |
| 데몬 → 클라이언트 | `0x81` | OUTPUT | 세션 출력 원시 바이트 |
| 데몬 → 클라이언트 | `0x82` | EXIT | `{"code":0}` — 세션 안 프로그램이 끝났다 |
| 데몬 → 클라이언트 | `0x83` | ERROR | `{"code":"nosession","msg":"..."}` |

**왜 attach 만 프레임을 쓰는가 (제어는 JSON 인데)**

- **입력과 리사이즈는 순서가 있다.** 같은 스트림에 섞여 순서대로 처리되어야 한다. 리사이즈를 다른 연결로 보내면 "화면이 커진 뒤에 도착해야 할 키 입력"과 "키 입력 뒤에 도착해야 할 리사이즈"를 구별할 방법이 없고, 어긋나면 세션 안 프로그램이 틀린 크기로 다시 그린다.
- **끝을 구별해야 한다.** 세션 안 프로그램이 정상 종료한 것(EXIT)과 데몬이 죽은 것과 소켓이 깨진 것은 사용자에게 다른 말을 해야 하는 사건이다. 연결을 그냥 닫으면 셋이 전부 "연결이 끊겼다"로 뭉개진다.
- **원시 바이트를 JSON 에 담을 수 없다.** 담으려면 base64 인데, 키를 한 번 누를 때마다 JSON 값 하나를 인코딩·디코딩하고 출력은 4/3 로 부풀린다. 이 경로는 사람이 타이핑하는 지연이 직접 보이는 자리다.

**왜 다중화가 아니라 전용 연결인가**

한 연결에 제어와 attach 를 섞으면 제어 요청이 attach 의 뒤에 줄을 서게 된다. GUI 대시보드는 3 초마다 `kyu list` 를 부르는데, 붙어 있는 터미널이 출력을 늦게 읽고 있으면(사용자가 스크롤을 잡고 있거나 창이 가려져 있으면) 그 역압이 목록 조회까지 멈춰 세운다. 연결을 나누면 역압이 그 attach 하나에만 머문다 — 있어야 할 자리다.

그리고 **연결 자체가 곧 붙어 있음이다.** detach 메시지가 필요 없다 — 클라이언트가 연결을 닫으면 그것이 detach 다. 클라이언트가 죽어도(터미널 창을 강제 종료해도) 커널이 연결을 닫아주므로 같은 경로로 처리된다. 상태를 따로 관리하지 않는다.

**중복 attach**: 이미 붙은 클라이언트가 있는 세션에 새 클라이언트가 붙으면 **앞의 것을 끊고 새 것이 받는다.** tmux 와 다른 선택이다(tmux 는 여럿이 함께 보게 한다). 이유는 README 임베디드 터미널 절이 이미 "한 번에 하나만 연다"고 정해두었고, 여럿이 볼 때 화면 크기를 어느 쪽에 맞출지가 곧바로 문제가 되기 때문이다. 여럿이 보는 것이 실제로 필요해지면 그때 정한다(선행 문서 원칙: 두 번째 사용처가 보일 때 만든다).

### 5.5 스크롤백과 재접속 화면 — 이 설계에서 가장 큰 위험

**데몬은 세션마다 출력 원시 바이트의 링 버퍼를 쥔다.** PTY 마스터에서 읽은 바이트를 붙어 있는 클라이언트에게 보내는 그 자리에서 링에도 함께 적는다. attach 하면 링의 내용을 먼저 흘려보내고 그 뒤부터 실시간이다.

**여기에 정직하게 적어둘 문제가 있다.** 원시 바이트 재생은 화면 복원이 아니다.

터미널 출력은 문자와 제어 시퀀스가 섞인 스트림이고, 화면의 최종 모습은 그 스트림을 처음부터 해석한 결과다. 링은 뒤쪽 일부만 갖고 있으므로, 재생은 **중간부터 해석을 시작하는 일**이 된다. 두 가지가 어긋난다.

1. **시퀀스 중간에서 시작할 수 있다.** 링이 오래된 바이트를 버릴 때 하필 `ESC [ 3 8 ; 5 ; 2 0 4 m` 의 한가운데를 잘라내면, 재생을 받은 터미널은 남은 조각을 문자로 찍는다.
2. **화면 상태가 이어지지 않는다.** 대체 화면 버퍼에 들어가 있었는지, 커서가 어디였는지, 어떤 색이 걸려 있었는지는 링이 시작하기 전의 바이트가 정한 것이라 재생에 없다.

**대응 (1) — 링의 버림 지점을 시퀀스 경계에 맞춘다.** 출력을 링에 적으면서 최소한의 ANSI 상태 기계를 함께 돌린다. 이 기계가 알아야 하는 것은 "지금 시퀀스 안인가"뿐이다 — CSI·OSC·DCS 의 시작과 끝만 알면 되고, 각 시퀀스가 무슨 뜻인지는 몰라도 된다(원칙 9). 링은 그 기계가 ground 상태였던 마지막 지점까지만 버린다. 그러면 재생은 항상 온전한 시퀀스에서 시작한다.

**대응 (2) — 붙는 순간 다시 그리게 한다.** 재생이 끝난 뒤 데몬이 새 클라이언트의 크기를 PTY 에 적용하면(`TIOCSWINSZ`) 커널이 세션의 포그라운드 프로세스 그룹에 `SIGWINCH` 를 보내고, 화면을 다 쓰는 프로그램은 대개 그 신호에 전체를 다시 그린다.

**대응 (2) 의 한계를 적어둔다**: 커널은 크기가 **실제로 바뀔 때만** `SIGWINCH` 를 보낸다. 떼었다가 같은 크기의 터미널로 다시 붙으면 신호가 나가지 않고, 화면은 재생된 바이트가 만든 모습에 머문다. 크기를 한 칸 줄였다 되돌리는 수법은 신호를 두 번 만들지만 사용자에게 화면이 한 번 출렁이는 것으로 보인다.

**그래서 남는 것**: 줄 단위로 흐르는 출력(셸, 빌드 로그)은 원시 링 재생으로 충분하다. 화면 전체를 그리는 프로그램은 **같은 크기 재접속**에서 온전할지 확실하지 않다.

**결정**: v1 은 원시 링 + 대응 (1)(2) 로 간다. 데몬 안에 화면 모델(가상 터미널 파서 + 셀 격자)을 두는 것이 tmux 가 하는 일이고 확실한 해법이지만, 그것은 이 작업의 크기를 몇 배로 올린다. **다만 이것은 "나중에 보자"로 미룰 항목이 아니라 기본값 전환을 막는 관문이다** — 9절 4 단계의 선행 조건에 "빈 터미널에서 `claude` 를 띄우고, 같은 크기의 터미널로 떼었다 붙었을 때 화면이 온전한지 실측"을 넣는다. 실측이 실패하면 4 단계 전에 화면 모델을 만들거나, 만들 때까지 tmux 가 기본값으로 남는다.

**링 크기 기본값**은 10절 2번으로 넘긴다.

### 5.6 데몬 생명주기

**기동 — `create` 에서만 한다.**

```
DaemonBackend.Create
  ├ 소켓에 연결 시도
  │   성공 → 요청을 보낸다
  │   실패 → 아래로
  ├ daemon.lock 을 flock(LOCK_EX) 으로 잡는다
  ├ 다시 연결 시도 (잠금을 기다리는 동안 남이 띄웠을 수 있다)
  │   성공 → 잠금 풀고 요청을 보낸다
  ├ 죽은 소켓이면(연결이 ECONNREFUSED 로 거절) 파일을 지운다
  ├ kyu __daemon 을 setsid 로 띄운다
  ├ 소켓이 연결을 받을 때까지 기다린다 (마감 시각을 두고)
  └ 잠금을 푼다
```

**읽기만 하는 요청은 데몬을 띄우지 않는다.** `List`·`IsAlive`·`Kill` 은 연결에 실패하면 그것을 답으로 삼는다 — 각각 빈 목록, `false`, `nil`. **데몬이 없다는 것은 세션이 없다는 뜻이지 오류가 아니다.** tmux 백엔드가 이미 같은 규칙으로 동작한다(`internal/session/tmux.go:116` 의 "tmux 서버는 마지막 세션이 죽으면 함께 종료된다 — 즉 세션 0개의 정상적인 표현이 서버 없음이다").

이 규칙이 없으면 GUI 대시보드가 3 초마다 부르는 `kyu list` 가 워크디렉토리를 열어보기만 해도 데몬을 띄우게 된다. 사용자가 세션을 하나도 만들지 않았는데 프로세스가 도는 것은 놀랄 일이다.

**죽은 소켓의 구별**: 파일이 없으면(`ENOENT`) 데몬이 없는 것이고, 파일은 있는데 연결이 거절되면(`ECONNREFUSED`) 데몬이 죽으면서 파일을 남긴 것이다. 지우는 것은 후자뿐이고, 그것도 잠금 안에서만 한다 — 잠금 밖에서 지우면 방금 뜬 남의 데몬 소켓을 지울 수 있다. 5.2 에서 적었듯 macOS 경로에서는 재부팅이 이 상황을 흔하게 만든다.

**종료 — 마지막 세션이 끝나면 데몬도 끝난다.** tmux 서버와 같은 성질이다. 여기에는 유예를 둘지의 선택이 있고 10절 3번으로 넘긴다.

**로그 — 생명주기 사건만 적는다.** 데몬 기동·종료, 세션 생성·종료(종료 코드 포함), attach·detach, 오류. **입출력 바이트는 절대 적지 않는다.** 세션의 내용은 사용자의 작업이고 도구의 관심사가 아니며(원칙 3), 적기 시작하면 로그가 무한히 자라 회전 장치가 필요해진다. 사건만 적으면 하루에 몇 킬로바이트라 그 장치가 아예 필요 없다.

**업그레이드 중 프로토콜 불일치**: `kyu` 를 새 판으로 갈아도 돌고 있던 데몬은 옛 판이다. 새 클라이언트가 `v` 를 보고 다른 버전을 만나면 **거절하고 사실을 알린다** — 조용히 옛 프로토콜을 흉내 내면 어긋남이 세션 안에서 드러난다. 무엇을 알리고 무엇을 권할지는 10절 5번이다.

### 5.7 세션 생명주기

**생성**: PTY 쌍을 열고, 슬레이브를 자식의 stdin/stdout/stderr 로 물리고, `setsid` + `TIOCSCTTY` 로 자식이 그 슬레이브를 제어 터미널로 갖는 새 세션·프로세스 그룹의 리더가 되게 한다. cwd 는 요청이 준 것, env 는 요청이 준 것에 데몬이 다음을 더한 것이다.

| 환경변수 | 값 | 왜 |
|---|---|---|
| `KYU_SESSION` | 세션 이름 | 중첩 판정의 표식 — 아래 |
| `TERM` | 클라이언트가 알려준 값, 없으면 `xterm-256color` | 없으면 세션 안 프로그램이 화면 그리기를 포기한다 |

**중첩 판정**: `ErrNestedSession`(`internal/session/backend.go:21`) 은 데몬 백엔드에서 `KYU_SESSION` 의 존재로 판정한다. tmux 가 `TMUX` 를 심는 것과 같은 기전이다(`internal/session/tmux.go:20`) — 데몬이 세션을 만들 때 심으므로 그 안에서 실행되는 모든 프로세스가 물려받고, 그 안에서 `kyu attach` 를 부르면 표식이 보인다.

tmux 의 `TMUX` 가 소켓 경로인 것과 달리 값을 **세션 이름**으로 둔다. 그러면 안내가 "이 터미널은 이미 세션 안입니다" 에서 "이 터미널은 이미 `kyu-featureX-proj-a` 세션 안입니다" 로 나아간다. 사용자가 어느 세션인지 모르는 채로 여기 도착하는 경우가 실제로 있다.

**중첩은 백엔드 안에서만 성립한다.** tmux 세션 안에서 데몬 백엔드로 `kyu attach` 를 부르는 것은 중첩이 아니다 — 서로 다른 두 계층이라 겹치지 않는다. 판정을 백엔드에 두면 이것이 저절로 맞는다. 표시 계층이 판정했다면 두 표식을 다 알아야 하고 그 조합을 손으로 관리하게 된다.

**종료**: `Kill` 은 자식의 **프로세스 그룹 전체**에 `SIGHUP` 을 보내고, 유예 뒤에도 남아 있으면 `SIGKILL` 한다. 그룹에 보내는 것이 중요하다 — 세션 안의 프로그램이 자식을 띄워두었으면 리더만 죽여서는 그 자식이 PTY 를 쥔 채 남는다. 없는 세션을 죽이는 것은 `nil` 이다(멱등 — `internal/session/tmux.go:98` 과 같은 계약).

**자연사**: 세션 안 프로그램이 스스로 끝나면 데몬은 PTY 마스터에서 EOF 를 보고, 자식을 거두고(좀비를 남기지 않는다), 붙어 있는 클라이언트에 EXIT 프레임을 보낸 뒤 레지스트리에서 지운다. 그 세션이 마지막이었다면 5.6 의 종료 정책이 이어진다.

### 5.8 SessionBackend 계약 — 다섯은 그대로, 하나가 는다

다섯 함수(`internal/session/backend.go:25`)의 시그니처와 의미는 **바뀌지 않는다.** `DaemonBackend` 가 같은 계약을 채우고 같은 센티널을 반환한다.

**하나가 는다.**

```go
// DetachKey 는 이 백엔드의 세션에서 빠져나오는 키다.
DetachKey() string
```

`internal/cli/attach.go:17` 의 주석이 이 순간을 미리 적어두었다 — *"백엔드가 아직 하나뿐이어서다. 두 번째 백엔드가 생기면 그때 세션 계층이 답하도록 옮긴다."* 상수 `detachKeyBinding`(`internal/cli/attach.go:22`) 은 사라지고, 그것을 쓰던 두 자리(`attach.go:53` 의 안내, `attach.go:58` 의 중첩 오류)가 백엔드에게 묻는다.

**키를 돌려주고 문장은 돌려주지 않는다.** 무엇을 어떻게 말할지는 표시 계층의 몫이다 — 백엔드가 완성된 문장을 주면 CLI 와 GUI 가 같은 문장을 쓰게 되는데, 두 곳은 사용자가 있는 자리가 달라 할 말도 다르다.

**데몬 백엔드의 키는 `Ctrl-\` 다.**

| 후보 | 판단 |
|---|---|
| `Ctrl-\` (0x1c) | **선택.** 대화형 프로그램이 거의 쓰지 않는다. `dtach`·`abduco` 의 관례이기도 하다. raw 모드에서는 `ISIG` 를 끄므로 SIGQUIT 이 되지 않는다 |
| tmux 처럼 접두 키 + `d` | 기각. tmux 의 `Ctrl-b` 는 readline·emacs 계열에서 "한 글자 뒤로" 라 세션 안 프로그램에게서 그 키를 뺏는다. 다른 접두 키를 골라도 상태를 가진 키 파싱이 하나 생긴다 |
| `Ctrl-a d` | 기각. `Ctrl-a` 는 "줄 처음으로" 다. 위와 같은 이유로 더 나쁘다 |

단일 키의 대가: 그 키를 세션 안으로 **보낼 수 없다.** 두 번 누르면 통과시키는 탈출 규칙을 둘 수 있지만 v1 은 하지 않는다 — `Ctrl-\` 를 세션 안에서 써야 하는 상황이 실제로 나타나면 그때 만든다.

### 5.9 의미론 대응표

원칙 8("의미론을 새로 정하지 않는다")이 요구하는 것을 표로 적어둔다. 3 단계의 동등성 테스트가 확인할 목록이기도 하다.

| 성질 | tmux 백엔드 | 데몬 백엔드 | 같은가 |
|---|---|---|---|
| 세션 생존의 근거 | tmux 서버 프로세스 | kyu 데몬 프로세스 | ✅ 같은 성질 |
| 서버/데몬이 죽으면 | 세션 전부 죽음 | 세션 전부 죽음 | ✅ |
| 세션 0 개의 표현 | 서버 없음 | 데몬 없음 | ✅ |
| detach | 클라이언트 종료 | 클라이언트 연결 종료 | ✅ |
| detach 키 | `Ctrl-b d` | `Ctrl-\` | ⚠️ 다르다 — 5.8 이 인터페이스로 흡수 |
| 중첩 표식 | `TMUX` 환경변수 | `KYU_SESSION` 환경변수 | ✅ 같은 기전 |
| 중복 이름 생성 | `ErrSessionExists` | `ErrSessionExists` | ✅ |
| 없는 세션 kill | `nil` (멱등) | `nil` (멱등) | ✅ |
| 세션 명명 | `kyu-<workdir>-<repo>` | 동일 | ✅ `internal/session/name.go` 그대로 |
| `--all` 접두사 필터 | `WorkDirSessionPrefix` | 동일 | ✅ `internal/session/name.go:30` 그대로 |
| 이름의 `.`·`:` 치환 | tmux 타깃 문법 때문에 필요 | 데몬에는 불필요하지만 유지 | ⚠️ 아래 |
| 세션 안 명령 변경 | 불가 (생성 시 확정) | 불가 (생성 시 확정) | ✅ `internal/cli/start.go` 의 경고가 그대로 맞다 |

**이름 치환을 유지하는 이유**: `tmuxTargetSyntaxReplacer`(`internal/session/name.go:19`) 는 tmux 의 타깃 문법을 피하려고 만든 것이라 데몬 백엔드에는 이유가 없다. 그런데도 유지한다 — 백엔드를 바꿨을 때 **같은 워크디렉토리가 다른 세션 이름을 갖게 되면** 사용자가 tmux 로 되돌렸을 때 자기 세션을 잃어버린 것처럼 보인다. 치환 규칙은 이름을 만드는 순수 함수이고 백엔드에 딸린 것이 아니므로, 한쪽에 이유가 남아 있는 한 양쪽이 같은 규칙을 쓴다.

---

## 6. 백엔드 선택과 전환

**`KYU_SESSION_BACKEND=tmux|daemon`.** 읽는 곳은 `cmd/kyu/main.go:151` 의 `withSessionBackend` 한 군데다. 그 함수의 주석이 이미 그 역할을 적어두었다 — *"어느 플랫폼 구현을 쓸지는 진입점의 결정이다. 그래야 internal/cli 가 tmux 를 모른 채로 남고, 백엔드가 늘어날 때 바뀌는 파일이 이 하나로 끝난다."*

| 시기 | 기본값 | 없는 백엔드를 지정하면 |
|---|---|---|
| 1~3 단계 | `tmux` | 데몬은 환경변수로만 |
| 4 단계 이후 | `daemon` | tmux 미설치 안내는 `KYU_SESSION_BACKEND=tmux` 일 때만 나온다 |

`KYU_SESSION_BACKEND`(선택)와 `KYU_SESSION`(중첩 표식, 5.7)은 이름이 앞부분을 공유한다. 둘 다 정확히 일치하는 이름으로만 조회하므로(`os.LookupEnv`) 섞이지 않지만, 읽는 사람이 한 번 멈칫할 자리라 여기 적어둔다.

**tmux 를 지우지 않는 이유**(원칙 7): 검증된 폴백이 있는 것과 없는 것은 사용자가 감수하는 위험이 다르다. 데몬이 어떤 터미널·어떤 셸에서 무너지면, 사용자는 자기 작업을 멈추지 않고 환경변수 하나로 돌아갈 수 있어야 한다. 코드 비용도 거의 없다 — `TmuxBackend` 는 이미 완성되어 테스트까지 있고, 3 단계 이후로는 동등성 매트릭스가 두 백엔드를 함께 돌리므로 방치되어 썩지도 않는다.

지우는 판단은 두 백엔드가 오래 같이 산 뒤에 따로 한다. 이 문서는 그것을 정하지 않는다.

---

## 7. GUI 에 닿는 것

**앱은 PTY 를 열어 그 안에서 `kyu attach` 를 실행한다**(`desktop/.../terminal/pty/PtyKyuSessionTerminalAttacher.kt:81`). `kyu attach` 가 tmux 클라이언트에서 데몬 클라이언트로 바뀌어도 **그 구조는 그대로다** — 앱이 부르는 것은 여전히 `kyu` 이고, `kyu` 가 여전히 TTY 를 요구하며, PTY 가 여전히 그것을 준다.

그래도 손볼 곳이 셋 있다. **"GUI 무변경"이 문자 그대로 참이 아니므로 적어둔다.**

| 자리 | 무엇 | 왜 |
|---|---|---|
| `desktop/.../terminal/SessionEntryPlan.kt:62`·`71` | `TMUX` 만 지우던 것을 `KYU_SESSION` 도 지우도록 | 앱을 세션 안에서 띄웠을 때 물려받은 표식은 앱 안 PTY 의 사실이 아니다. 표식이 바뀌었으니 지울 것도 바뀐다 |
| `desktop/.../terminal/pty/KyuAttachTtyConnector.kt:31` | 주석만 — "이 한 줄이 tmux 까지 닿는다" | 실제로는 데몬까지 닿는다. 경로가 한 홉 길어진다(아래) |
| `desktop/.../terminal/EmbeddedTerminalStateHolder.kt:17` | 주석만 — tmux 가 보장한다는 서술 | 보장하는 주체가 바뀐다 |

**리사이즈 경로가 한 홉 길어진다.** 지금은 PTY 의 winsize 변경 → `SIGWINCH` → tmux 클라이언트가 세션 창을 다시 그린다. 데몬에서는 PTY winsize 변경 → `SIGWINCH` → **`kyu attach` 클라이언트가 그 신호를 받아 RESIZE 프레임을 보낸다** → 데몬이 세션 PTY 에 적용 → 세션 안 프로그램에 `SIGWINCH`. 가운데 굵은 부분이 새로 만들어야 하는 것이다. **`kyu attach` 클라이언트에 `SIGWINCH` 핸들러를 두는 일이 2 단계에 들어간다** — tmux 클라이언트가 대신 해주던 일이라 지금은 없다.

**앱의 검증 항목**(2 단계 완료 확인에 넣는다):

1. 카드를 눌러 터미널이 열리고 키 입력·색·스크롤백이 터미널 창에서 `kyu attach` 한 것과 같다
2. 창 크기를 바꾸면 세션 화면이 따라 바뀐다 (위의 새 경로)
3. 터미널을 닫고 다른 카드로 갔다가 돌아오면 하던 자리가 나온다 (detach → 재접속 → 스크롤백 재생, 5.5)
4. 앱을 껐다 켜도 세션이 살아 있다 (원칙 5)
5. 앱을 tmux 세션 안에서 띄워도 카드가 열린다 (표식 제거의 첫 줄)

앱이 `kyu` 에게서 받는 `--json` 문서(README 기계용 표면)는 하나도 바뀌지 않는다. 세션이 어디서 사는지는 그 문서에 없는 사실이다.

---

## 8. 윈도우 전망

**이 작업이 로드맵 v4(윈도우 네이티브)의 전제다.** 3층 구조에서 플랫폼에 의존하는 곳은 세션 계층뿐이고(선행 문서 5.1), 지금 그 자리를 tmux 가 채우고 있어서 윈도우에는 채울 것이 없었다. 데몬을 우리가 갖고 나면 남는 것은 **같은 데몬의 두 부품을 윈도우 것으로 바꾸는 일**이다.

| 부품 | 유닉스 | 윈도우 |
|---|---|---|
| 의사 터미널 | PTY (`/dev/ptmx` 계열) | ConPTY (`CreatePseudoConsole`) |
| 클라이언트 통로 | 유닉스 도메인 소켓 | 명명 파이프 `\\.\pipe\kyu-<user>` |
| 접근 제어 | 파일 권한 0600 | 파이프의 DACL 을 현재 사용자로 |
| 크기 변경 | `TIOCSWINSZ` | `ResizePseudoConsole` |
| 종료 | 프로세스 그룹에 `SIGHUP` | Job Object 로 트리째 |
| 떼어내기 | `setsid` | `DETACHED_PROCESS` |

**바뀌지 않는 것**: 프로토콜(5.3·5.4), 스크롤백(5.5), 세션 명명, `SessionBackend` 계약, 조율 계층 전부, 표시 계층 전부. 프레임 정의와 JSON 은 바이트라서 통로가 무엇이든 같다.

윈도우도 AF_UNIX 소켓을 지원하므로 통로를 그대로 쓸 수 있을 가능성이 있다. 다만 접근 제어가 파일 권한으로 이뤄지는 부분이 윈도우에서 같은 뜻인지 확인되지 않았고, 소켓에 붙을 수 있다는 것이 임의 명령 실행을 뜻하는 이상(5.2) 확인 없이 고를 항목이 아니다. **명명 파이프를 기본으로 두고 AF_UNIX 는 확인 뒤에 판단한다**(10절 6번).

이 절은 전망이지 계획이 아니다. 윈도우는 별도 작업이고 9절의 5 단계로 떼어둔다.

---

## 9. 구현 단계

각 단계가 **PR 하나**이고, 각 단계 끝에 손으로 확인 가능한 상태가 된다(선행 문서 9.2 와 같은 자세).

| 단계 | 내용 | 완료 확인 |
|---|---|---|
| **1** | 런타임 경로(5.2) · 제어 프로토콜(5.3) · 데몬 골격 · `DaemonBackend` 의 create/list/kill/alive · 기동과 종료(5.6) · 세션 생성과 종료(5.7) | `KYU_SESSION_BACKEND=daemon kyu start proj-a` 로 세션이 뜨고, `kyu list` 에 `RUNNING` 으로 보이고, `kyu kill` 로 죽는다. 마지막 세션이 죽으면 데몬도 사라진다. **attach 는 아직 안 된다** |
| **2** | attach 프레임(5.4) · 스크롤백 링과 경계 처리(5.5) · raw 모드 클라이언트 · `SIGWINCH` 핸들러(7절) | 터미널에서 붙어 작업하고 `Ctrl-\` 로 빠져나왔다가 다시 붙으면 하던 자리가 나온다. 창 크기를 바꾸면 세션 화면이 따라 바뀐다. **앱의 검증 5 항목(7절)이 전부 통과한다** |
| **3** | `DetachKey` 인터페이스 확장(5.8) · 동등성 테스트 매트릭스 · CI 가 두 백엔드를 돈다 | 같은 테스트 본문이 두 백엔드에서 통과한다. 5.9 표의 각 줄에 대응하는 테스트가 있다 |
| **4** | 기본값을 `daemon` 으로 · README 요구사항 표에서 tmux 를 선택으로 강등 · `install.sh` 안내 정리 | 환경변수 없이, **tmux 가 설치되지 않은 머신에서** 워크디렉토리를 만들고 세션을 띄우고 붙어 작업하고 나오는 전 과정이 돈다 |
| **5** (별도) | 윈도우 ConPTY + 명명 파이프(8절) | 윈도우 네이티브에서 세션이 뜨고 붙는다. 릴리스에 msi 가 붙는다 |

**4 단계의 선행 조건 — 이것이 통과하지 않으면 기본값을 바꾸지 않는다**

빈 터미널에서 `claude` 를 세션으로 띄우고, 대화를 몇 번 주고받아 화면을 채운 뒤

1. 떼었다가 **크기가 다른** 터미널로 다시 붙는다 → 화면이 온전한가
2. 떼었다가 **크기가 같은** 터미널로 다시 붙는다 → 화면이 온전한가

2 번이 5.5 가 지목한 위험이다. 실패하면 데몬 안에 화면 모델을 만드는 작업이 4 단계 앞에 하나 더 들어가고, 그때까지 tmux 가 기본값으로 남는다. **이 실측은 2 단계 PR 의 본문에 결과를 적는다** — 4 단계에 가서 처음 알게 되면 이미 늦다.

**1 단계가 먼저인 이유**: 선행 문서 9.2 의 이유가 여기서도 같다. 이 작업에서 예상 못 한 문제가 있는 곳은 프로세스를 떼어내고 소켓을 붙잡는 부분이지 프레이밍이 아니다. 다만 순서가 뒤집히지는 않는다 — attach 없이 뜨는 세션은 반쪽이지만 `list`·`kill` 로 확인은 되고, 프레임 설계를 세션 생명주기 위에서 하는 편이 그 반대보다 낫다.

**의존 하나**: PTY 할당에 `github.com/creack/pty` 를 쓴다. 직접 짜면 리눅스는 `/dev/ptmx` + `TIOCGPTN`, macOS 는 `TIOCPTYGRANT` 계열로 OS 마다 다른 ioctl 상수를 손으로 맞춰야 하는데, 그것을 검증할 하드웨어가 두 종류 다 필요하다. cgo 없이 도는 작은 라이브러리이고, 앱 쪽이 이미 같은 이유로 pty4j 에 기대고 있다(`PtyKyuSessionTerminalAttacher.kt`). raw 모드는 새 의존이 필요 없다 — `golang.org/x/term` 이 이미 직접 의존이다(`go.mod`).

---

## 10. 열린 질문

구현을 막지는 않지만 해당 단계에서 정해야 하는 것들이다.

1. **데몬이 죽으면 세션도 죽는다** — 이것은 결함이 아니라 성질이다. 마스터를 쥔 프로세스가 사라지면 세션이 끝나는 것은 2절의 결론이고, tmux 서버와 정확히 같다(5.9). 그러므로 "복구"라는 것은 없다 — 있을 수 있는 것은 **줄이는 일**뿐이다. 3.1 의 세션별 감독은 반경을 하나로 줄인다. 중앙 데몬으로 간다면 그 대신 데몬을 단순하게 유지하는 것(원칙 9 — 출력을 해석하지 않는다)이 가장 큰 방어다. **되살리는 기능을 만들지 않는다는 것을 명시해 둔다.**

2. **스크롤백 링 크기 기본값** — tmux 는 창당 2000 줄(`history-limit`)이다. 원시 바이트 링이라 줄이 아니라 바이트로 재야 하는데, 색과 커서 이동이 섞인 출력은 한 줄이 수백 바이트가 되기도 한다. 후보: 세션당 1 MiB(가볍다, 화면 전체를 그리는 프로그램에는 모자랄 수 있다) / 4 MiB(세션 5 개면 20 MiB) / 설정 가능. **2 단계의 실측에서 `claude` 의 한 화면 다시 그리기가 몇 바이트인지 재고 정한다** — 링이 그 한 번을 담지 못하면 5.5 의 대응이 무의미하다.

3. **자동 종료 정책** — 마지막 세션이 끝났을 때 (가) 즉시 종료 (나) 짧은 유예 뒤 종료. (가)는 업그레이드 뒤 옛 데몬이 남아 있는 시간을 최소로 만들지만, 마침 연결하려던 클라이언트와 경합한다(클라이언트가 재시도하면 풀린다). (나)는 그 경합을 없애지만 유예 시간이라는 숫자가 하나 생긴다. **(가) + 클라이언트 재시도를 추천한다** — 숫자를 하나 덜 만드는 쪽이고, 5.6 의 기동 절차가 이미 재시도를 하고 있다. 3.1 로 가면 이 질문 자체가 사라진다.

4. **소켓 권한 0600 은 확정, 검사 여부가 열려 있다** — 만드는 것은 0600 이다(5.2). 문제는 **연결받을 때 소켓 파일의 권한을 다시 확인할 것인가** 다. `umask` 나 사용자의 손에 의해 파일 모드가 열려 있을 수 있다. 확인하고 열려 있으면 거절하는 편이 안전하지만, 정상적인 사용자를 막을 실패 모드가 하나 생긴다. **데몬 기동 때 한 번 확인하고 열려 있으면 뜨기를 거부하는 안**을 추천한다 — 매 연결마다 검사하는 것보다 싸고, 실패가 기동 시점에 몰려 진단하기 쉽다.

5. **프로토콜 버전 불일치의 처방** — `v` 로 감지하는 것은 정해졌다(5.3). 만났을 때 사용자에게 무엇을 권할지가 열려 있다. 옛 데몬을 내리면 그 위의 세션이 전부 죽으므로 도구가 마음대로 할 수 없다. 후보: (가) "돌고 있는 세션은 옛 판 데몬의 것입니다. 정리하려면 `kyu kill --all` 뒤 다시 실행하세요" 라고 알리고 멈춘다 (나) 옛 데몬을 그대로 두고 새 데몬을 다른 소켓에 띄운다(세션이 두 무리로 갈려 `kyu list` 가 둘 다 물어야 한다). **(가)를 추천한다** — (나)는 tmux 조차 하지 않는 일이고, 세션이 어디 붙어 있는지 사용자가 알 수 없게 된다.

6. **윈도우 통로: 명명 파이프인가 AF_UNIX 인가** — 8절. 윈도우에서 AF_UNIX 소켓의 접근 제어가 파일 권한과 같은 뜻인지 확인되지 않았다. 5 단계에서 확인한다.

7. **`creack/pty` 의 윈도우 지원 범위** — 유닉스 계열이 본령인 라이브러리다. ConPTY 를 어디까지 다루는지 확인되지 않았고, 다루지 않으면 윈도우 쪽 의사 터미널은 따로 쓰게 된다. 5 단계 전에 확인한다. 1~4 단계에는 영향이 없다.

8. **소켓 경로 길이** — 유닉스 도메인 소켓의 경로는 `sun_path` 에 들어가고 그 크기가 macOS 104 · 리눅스 108 바이트다. `$HOME` 이 깊으면 폴백 경로가 이 한도에 닿을 수 있다. 닿으면 소켓 디렉토리로 `chdir` 한 뒤 상대 경로로 바인딩하는 길이 있다. **1 단계에서 한도를 넘는 경로를 만나면 알아볼 수 있는 오류로 끊는다** — 조용히 실패하면 원인을 찾는 데 오래 걸리는 종류의 문제다.

---

## 부록 A. 확인된 사실

구현 중 다시 조사하지 않도록 근거를 모아둔다.

| 사실 | 근거 |
|---|---|
| PTY 마스터를 쥔 마지막 프로세스가 죽으면 슬레이브 쪽에 EOF·SIGHUP 이 간다 — 그래서 세션 생존은 마스터를 쥔 프로세스를 반드시 요구한다 | 2절 |
| tmux 서버는 마지막 세션이 죽으면 함께 종료되고, 그때 tmux 명령은 종료 코드 1 로 끝난다. 즉 "세션 0 개"의 정상적 표현이 "서버 없음"이다 | `internal/session/tmux.go:116`~`127` 의 실측 주석 |
| tmux 는 `TMUX` 가 설정돼 있으면 `TMUX_TMPDIR` 을 무시하고 그 소켓에 붙는다 | `internal/session/tmux_test.go:31` 의 실측 주석 |
| tmux 의 `-t <name>` 은 기본이 접두사 매칭이라 정확 일치를 위해 `=` 를 붙여야 한다 | `internal/session/tmux.go:154` |
| 세션이 실행할 명령은 만드는 순간 확정되고 이후 바꿀 수 없다 | `internal/cli/start.go` 의 `warnRunningSessionKeepsTheCommandItWasCreatedWith` |
| GUI 는 PTY 안에서 `kyu attach` 를 실행하고, PTY 가 아니면 TTY 요구를 채우지 못한다 | `desktop/.../terminal/pty/PtyKyuSessionTerminalAttacher.kt` 클래스 주석 |
| GUI 는 자식 환경에서 `TMUX` 를 덜어낸다 — 물려받은 표식이 앱 안 PTY 의 사실이 아니기 때문 | `desktop/.../terminal/SessionEntryPlan.kt:62` |
| GUI 대시보드가 아는 상태는 최대 3 초 묵은 것이다 | `desktop/.../terminal/SessionEntryPlan.kt:31` 의 주석 |
| detach 키를 세션 계층으로 옮기는 것은 두 번째 백엔드가 생길 때로 예정되어 있었다 | `internal/cli/attach.go:17`~`22` |
| 백엔드 선택은 진입점 한 군데의 결정이다 | `cmd/kyu/main.go:141`~`152` |
| CI 는 tmux 를 설치하고 `tmux -V` 로 설치를 확인한다 — 세션 테스트가 tmux 없으면 `t.Skip` 이라 조용히 통과하는 것을 막기 위해서다 | `.github/workflows/ci.yml:24`~`32` |
| 커널은 `TIOCSWINSZ` 로 크기가 **실제로 바뀔 때만** `SIGWINCH` 를 보낸다 — 같은 크기 재접속은 다시 그리기를 유발하지 못한다 | 5.5 · 2 단계에서 실측 |
| 유닉스 도메인 소켓 경로 한도는 macOS 104 · 리눅스 108 바이트다 | `sun_path` 크기 · 10절 8번 |
| `golang.org/x/term` 은 이미 직접 의존이라 raw 모드에 새 의존이 필요 없다 | `go.mod` |
| 윈도우 네이티브가 막혀 있는 이유는 바이너리가 아니라 tmux 다 | `desktop/.../engine/githubrelease/EngineReleaseTarget.kt:7` · `.github/workflows/release.yml:132` |

## 부록 B. 용어

| 용어 | 의미 |
|---|---|
| **세션 데몬** | 사용자당 하나 도는 `kyu` 프로세스. 세션의 PTY 마스터와 스크롤백을 쥐고 클라이언트를 받는다 |
| **PTY 마스터 / 슬레이브** | 커널이 만드는 의사 터미널 쌍. 슬레이브는 세션 안 프로그램의 터미널이고, 마스터는 그 반대편이다 |
| **제어 연결** | create·list·kill·alive 를 주고받는 짧은 연결. NDJSON (5.3) |
| **attach 연결** | 한 세션에 붙어 있는 동안 유지되는 연결. 프레임 (5.4). 연결 자체가 곧 붙어 있음이다 |
| **스크롤백 링** | 세션 출력 원시 바이트의 고정 크기 링 버퍼. 붙을 때 재생된다 (5.5) |
| **중첩 표식** | 세션 안에서 도는 프로세스가 물려받는 환경변수. tmux 는 `TMUX`, 데몬은 `KYU_SESSION` (5.7) |
| **죽은 소켓** | 데몬이 죽으면서 남긴 소켓 파일. 연결이 `ECONNREFUSED` 로 거절되는 것으로 구별한다 (5.6) |
| **동등성 테스트** | 같은 테스트 본문을 두 백엔드에서 돌려 5.9 표의 각 줄을 확인하는 매트릭스 (9절 3 단계) |
